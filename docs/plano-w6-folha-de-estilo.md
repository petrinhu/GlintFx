# Plano da onda W6: a folha de estilo inteira, do seletor ao bloco

STATUS: proposto, 15/09/2026 16:44:09 (hora real de `date`). Autor: CTO (fable). **Não é modo autônomo:** toda decisão que muda o que o autor da folha observa vai ao líder (L-10 do projeto); as decisões internas de desenho são minhas e estão marcadas para ele reverter. Só planejamento. Nada implementado, nada commitado, `TODO.md` não tocado.

**EMENDADO em 15/09/2026 16:58:47 (hora real de `date`) com as quatro decisões do líder, relatadas pelo orquestrador (não são verbatim dele; a forma verbatim, se existir, mora em `ESCOPO.md`):** D1 o nome é **`:where()`**; D2 a lista dentro dele é **estrita**; D3 `:scope` seguido de irmão é **aviso**, não erro; D4 o atalho que anula longhand já escrito **avisa**. Mais um achado do orquestrador que vira fatia (S-0): `ESCOPO.md:453` afirma como vigente a decisão 5 de 21/08, revogada. **Conserto do orquestrador, 15/09/2026 (segunda revisão do plano): S-0 também é quem grava as quatro decisões de hoje (D1-D4) em `ESCOPO.md`**: elas existiam só dentro deste plano, e plano não é canon; quem abrisse `ESCOPO.md` daqui a um mês leria o estado errado (D-W6-15 estendida, §3 e §4 linha 0). **Nada mais está pendente dele para esta onda** (§8). As decisões D-W6-12 a D-W6-15 da §3 registram isso; o texto anterior que dizia "pendente" foi substituído, não arquivado (L-67 global).

Este plano segue a forma de `docs/plano-w6b-placa-e-laco.md` e `docs/plano-w6a-janela.md`. O `TODO.md` é do orquestrador: este plano **propõe** uma linha nova (§7) e não a escreve.

Frase-guarda (L-21 global), que entra verbatim em todo briefing de agente desta onda: **produto para distribuição, base de consumidores aberta e desconhecida. Nada sai do escopo porque "o jogo do líder não usa".** Regra de paridade: se o padrão (CSS Selectors 4, CSS Cascade 3, CSS Syntax 3) aceita, o `gfss` aceita; divergência só com razão escrita e diagnóstico.

Regra de leitura deste documento, herdada das duas ondas anteriores e cobrada por portão (`tests/tools/check_plan_scope_diff.py`): **todo caminho entre crases na coluna "Nasce / muda" da tabela da §4 é promessa**, conferida contra a árvore no fechamento. Caminho que não nascer precisa de linha datada em `tests/parity_absences.txt` com item dono, ou a onda não fecha. Toda frase que diz "medido", "idêntico" ou "nunca" cita o teste que prova, ou não entra.

Insumo obrigatório desta onda, lido inteiro antes de fatiar (L-43/L-44 do projeto): `/var/tmp/glintfx-plan/dores-comunidade-w6.md` (31 fontes, FATO/INFERÊNCIA marcado linha a linha). A §1 responde ao dossiê item por item; nada dele é decorativo. Onde discordo, cito arquivo:linha.

---

## 0. O estado, medido agora contra `main` em `3d5ab89` (não de memória)

| # | Fato medido | Onde | O que muda no plano |
|---|---|---|---|
| F1 | **O seletor completo já parseia.** A AST tem `head` + `rest[]` com os quatro combinadores (`descendant`, `child`, `next_sibling`, `subsequent_sibling`); `:not()` é recursivo (`not_selectors`, limite `k_max_not_nesting_depth = 10`); `:scope` está na lista de 14 pseudo-classes simples; 5 funcionais; 2 pseudo-elementos. **Nenhum seletor de especificidade-zero existe**: `:where` só aparece num comentário. | `src/gfss/selector_ast.hpp` (structs), `src/gfss/selector_pseudo_vocabulary.hpp` (14+5+2), `src/gfss/selector_parse.cpp:93` (o comentário), `:373` (o limite) | `GFSS-SPECIFICITY` e `GFSS-MATCH-COMBINE` partem da AST pronta; o seletor novo (§7) é vocabulário a mais no mesmo mecanismo do `:not()`, não parser novo. |
| F2 | **O casamento composto devolve três respostas**, `matched`/`rejected`/`deferred`, e **`deferred` é o que hoje sai para `:not()`, `:scope`, `:placeholder-shown` e pseudo-elemento**. Só recebe um `gfss_compound_selector`; `rest` nunca é visto. | `src/gfui/compound_match.hpp:77` (o enum), `compound_match.cpp` (`note_pseudo_class_selector`, `note_pseudo_function_selector`, `match_compound`) | `GFSS-MATCH-COMBINE` é a camada de cima que consome o veredito composto e resolve `:not()` e `:scope`; `:placeholder-shown` e `::before/::after` **continuam `deferred`** (lacuna 1 do contrato de oito fatos e `LAYOUT-PSEUDO-BOXES`, W13). |
| F3 | **O atalho já é lido e guardado CRU.** `gfss_declaration{is_shorthand, shorthand_name, important, form = raw, raw_tokens}`; `parse_declaration_list(cursor)` tokeniza do cursor até o EOF e fatia por `;` de topo (`nesting_depth`), sem parar em `}`; o cursor carrega linha/coluna da folha (provado por `diagnostic_line_and_column_are_the_sheets_own_not_the_blocks`). | `src/gfss/declaration_ast.hpp`, `declaration_parse.cpp:130` (`build_shorthand`), `declaration_list_parse.cpp`, `declaration_split.cpp` | `GFSS-SHORTHAND` expande **no `fold` da lista**, no índice do atalho; `GFSS-SHEET-PARSE` entrega ao leitor de bloco um cursor cujo `source` é **só o miolo entre chaves** (é o leitor de folha quem acha a chave que fecha, nunca o de bloco). |
| F4 | **Onze atalhos aceitos e dez nomes recusados são listas fechadas**, e o diagnóstico da recusa já nomeia as longhands em `detail` (identificadores separados por espaço, nunca frase; R7 de `docs/api-conventions.md`). | `src/gfss/shorthand_name_vocabulary.hpp`, `src/gfss/refused_property_names.hpp`, `tests/gfss_declaration_parse_test.cpp:172` | A decisão 2 do líder (diagnóstico em inglês, nomeando o vocabulário do autor) **já é a forma da casa**: `expected` é identificador do vocabulário fechado, `detail` lista os nomes aceitos. Esta onda **acrescenta entradas**, não muda a forma. |
| F5 | **O tipo público de diagnóstico tem quatro campos** (`line`, `column`, `expected`, `detail`) e **não tem gravidade** (decisão do líder de 09/09: entra só na revisão de API de `GFSS-API`, W10). Já existe o precedente de "aceito com aviso": `has_reserved_notice` na declaração e `reserved_notice_count` no resultado. | `include/glintfx/gfss/token.hpp`, `src/gfss/declaration_ast.hpp` | Aviso (seletor morto, §1 item 5.3) **não pode** ser "diagnóstico com gravidade warning". Entra pelo precedente: seletor aceito, marcado, contado, com diagnóstico numa lista **separada** da de rejeição (D-W6-6). |
| F6 | **Os cinco `expected` do casamento de seletor e os doze da declaração vivem numa lista X-macro só**, com produtor por entrada e teste que reprova palavra duplicada. | `src/gfss/diagnostic_vocabulary.hpp` (35 entradas), `tests/gfss_selector_parse_test.cpp:1245` | Cada fatia acrescenta as suas entradas com produtor próprio (`sheet_parse`, `shorthand_expand`); o teste de duplicata já morde. |
| F7 | **Todo teste de `gfss`/`gfui` compila o `.cpp` interno uma segunda vez** (`target_sources` PRIVATE), porque `detail` não é exportado. | `tests/CMakeLists.txt:416-438` | Cada `.cpp` novo entra em `src/gfss/CMakeLists.txt` **e** na lista de cada teste que o linka. É a coluna "muda" de toda fatia; esquecer é erro de link, não silêncio. |
| F8 | **A gramática dos longhands que os onze atalhos alimentam está fixada no registro**: lados (comprimento, %, `auto` só em margem), cantos (comprimento, %, **um valor**), largura de borda (comprimento ou `thin/medium/thick`), estilo (`none/solid`), cor, `row-gap`/`column-gap`, `overflow-x/-y` (`visible/hidden`), `flex-direction`/`flex-wrap`. `border-*-radius` aceita **um** valor por canto. | `docs/gfss-property-registry-v1.md` §6 linhas 160-226, `src/gfss/property_value_contract.hpp` (`property_value_contract_for`) | A expansão **reusa** `check_declaration_value` com o contrato do longhand; não nasce validação nova. A forma elíptica `a / b` do `border-radius` **não é representável** (um valor por canto) e é recusada com diagnóstico (D-W6-9). |
| F9 | **O bloco de animação é `@keyframes`** no registro: `animation-name` aceita `<nome de @keyframes>`. É decisão [CTO] de 05/09 em modo autônomo, não do líder. | `docs/gfss-property-registry-v1.md:269` | `GFSS-SHEET-PARSE` reconhece `@keyframes` e guarda cru. **Registrado em §8 para confirmação retroativa**, porque a palavra da at-rule é vocabulário do autor. |
| F10 | **Árvores falsas para teste já existem**: arena, ligada e **contadora de chamadas** (conta quantas vezes cada fato foi perguntado). | `tests/fake/fake_arena_tree.hpp`, `fake_linked_tree.hpp`, `fake_counting_tree.hpp` | A prova de que o casamento anda **da direita para a esquerda** (dossiê §5.1) é contagem de chamadas, não leitura de código: com o sujeito reprovado, nenhum fato do pai é perguntado. |
| F11 | **Nada da trilha `gfss` é anunciado antes de `GFSS-API` (W10)**; o `README.md` não muda nesta onda. | `TODO.md` seção "A trilha GFSS" | A fatia de fechamento **não toca** README. `docs/node-view-and-matching.md` (interno, inglês, leitor é quem estende a camada) ganha a seção de combinadores. |
| F12 | **Teste puro roda nos cinco sistemas com o mesmo nome** (`ctest`, sem `#if`), então o portão de paridade vê "mesmo nome" sem apelido nem exceção. | `tests/tools/check_test_parity.py`, os 16 testes `gfss_*`/`gfui_*` já existentes | Coluna "Par no portão" = `mesmo nome` em toda fatia; L-04 do projeto satisfeita por construção, provada pelo job de cada sistema. |

---

## 1. Resposta ao dossiê, item por item

Legenda: **ENTRA** (vira prova ou decisão nesta onda), **JÁ COBERTO** (a árvore já faz; cito onde), **DISCORDO** (com arquivo:linha), **FORA** (não é desta onda; digo de quem é).

| § do dossiê | Achado | Resposta |
|---|---|---|
| 2.1 | `[id="x"]` pesa como classe | **ENTRA** em S-1: caso enumerado; `expected` nada, é comportamento-padrão. Documentado em `docs/node-view-and-matching.md`. |
| 2.2 | `:not()` conta o sub-seletor mais específico | **JÁ DECIDIDO** (`TODO.md:401`); **ENTRA** em S-1 como regra + 3 casos (um arg, vários args, `:not` aninhado). |
| 2.3 | Encadear `:not()` infla; CSS criou `:where()` | **ENTRA** como fatia nova S-4 (§7), por ordem do líder de hoje, com o nome `:where()` (D1) e lista estrita (D2), divergência deliberada registrada em D-W6-7. |
| 2.4 | Empate por ordem de fonte | **ENTRA** em S-5: cada regra guarda o índice de ordem na folha; a comparação vive em `GFSS-CASCADE` (W10), **fora** desta onda. |
| 2.5 | RmlUi calcula igual ao CSS | **JÁ COBERTO** pela decisão de 2.2; confirma, não muda. |
| 2.6 | Seletor mais à direita deve ter id/classe/tag (dica de indexação) | **FORA**: índice por chave de seletor é otimização de `GFSS-API`/cascata; nesta onda o casamento é por regra, sem índice. Registrado como candidato à INBOX (`GFSS-KEY-INDEX`), não decidido. |
| 3.1 | Atalho reseta em silêncio o omitido | **ENTRA** em S-3 como comportamento-padrão (todo longhand omitido nasce com o valor inicial do registro) **e** em S-3b como aviso de "reset acidental" quando o mesmo bloco já tinha o longhand explícito antes do atalho (decisão do líder D4, 15/09/2026, D-W6-14). |
| 3.2 | `border-radius` não é sentido horário | **ENTRA** em S-3: família própria (`shorthand_four_corners`), ordem TL, TR, BR, BL, caso enumerado para 1, 2, 3 e 4 valores; nunca compartilha código com a família de lados. |
| 3.3 | `!important` no atalho marca todas as sub-propriedades | **ENTRA** em S-3: matriz inteira (11 × 2). |
| 3.4 | Expandir preservando a POSIÇÃO original | **ENTRA** em S-3 como invariante com teste de índice (D-W6-3). |
| 3.5 | Qt sem `!important` é dor | **JÁ DECIDIDO** (decisão 9 de 26/08); confirma. |
| 3.6 | Sem dor de shorthand em RmlUi/Godot/Qt | Ausência declarada; nada a fazer. |
| 4.1-4.2 | Recovery por construção, at-rule desconhecida ignorada | **ENTRA** em S-5, é o desenho da linha (`TODO.md:414`). |
| 4.3 | LightningCSS é estrito por padrão | **DISCORDO da leitura como alternativa**: `ESCOPO.md:452` (decisão 4 do líder) fixa "ignorada com diagnóstico", e a régua de legibilidade (`ESCOPO.md:437`) exige o diagnóstico. Recovery incondicional **com** diagnóstico por construção descartada; nada silencioso. |
| 4.4 | At-rule crua para processar depois | **ENTRA** em S-5: `@keyframes` guardado cru, byte a byte. |
| 4.5 | Chave faltando derrubou todas as regras seguintes (Firefox) | **ENTRA** em S-5 como caso obrigatório: chave não fechada **dentro** do bloco cru e fora dele; regras seguintes contadas. |
| 4.6 | Aninhamento dentro de at-rule ainda é sutil | **ENTRA** em S-5: o limite do bloco cru conta chaves aninhadas com `nesting_depth`; `{`/`}` dentro de string é token `string`, nunca chave (caso enumerado). |
| 5.1 | Casar da direita para a esquerda | **ENTRA** em S-2 como desenho **e** prova por contagem (F10). |
| 5.2 | `:scope` divergiu entre motores | **ENTRA** em S-2: os dois contextos decididos e testados (D-W6-5). |
| 5.3 | `:scope` + irmão nunca casa | **ENTRA** como aviso (D-W6-6, decisão do líder D3, 15/09/2026), átomo próprio em S-5. |
| 5.4 | Recursão de `:not()` estourou a pilha em produção | **JÁ COBERTO**: limite 10 em `selector_parse.cpp:373`, provado nos dois lados. S-4 **compartilha** o mesmo orçamento (D-W6-8). |
| 5.5 | `:not()` com descendente casa por outro caminho | **ENTRA** em S-2 como caso: `body :not(table) a` casa o `a` dentro da tabela. É comportamento-padrão; a documentação avisa. |
| 5.6 | Custo de combinador amplo | **FORA** (medição de desempenho é pós-`GFSS-API`); o pior caso do retrocesso fica declarado em §6. |
| 6 | Falha silenciosa é o padrão transversal | **ENTRA** como princípio: toda construção aceita-mas-inerte desta onda produz diagnóstico numa lista contada (avisos), nunca silêncio. |
| 7.1-7.3 | Perguntas ao líder | 7.1 respondida hoje (entra, como `:where()`, lista estrita); 7.2 vira caso obrigatório de S-5; 7.3 respondida hoje (vocabulário do autor, em inglês) e já é a forma da casa (F4). |

---

## 2. A ordem, a dependência real, e por que só um implementador por vez

```
S-0 canon: ESCOPO.md decisão 5 e os dois gêmeos (só documento; L-67: vem ANTES de S-5, que depende do bloco de animação ser reconhecido)
S-1 SPECIFICITY (puro, só AST) ─┐
S-2 MATCH-COMBINE (gfui, só AST + compound_match) ─┤→ S-4 :where() (parser + S-1 + S-2; nome e lista decididos, DESBLOQUEADA)
S-3 SHORTHAND (gfss, só declaração) → S-3b aviso de reset acidental (lista de avisos no resultado do bloco) ─┘
S-5 SHEET-PARSE (usa S-1 para anexar especificidade; usa S-3/S-3b para que o bloco saia só com longhands e com os avisos; usa S-4 no dead check)
S-6 fechamento (CI-VERDE-W6)
```

- **S-0 primeiro, e é barata**: a correção da decisão 5 revogada (três linhas de canon) mais a seção nova com as quatro decisões de hoje (D1-D4) em `ESCOPO.md`, duas varreduras contadas, zero código. A L-67 global manda: canon desatualizado bloqueia o trabalho que dele depende, e S-5 depende exatamente do que a linha 453 nega; S-4 (`:where()`) depende de D1/D2 estarem no canon, não só no plano.
- **S-1, S-2 e S-3 não dependem uma da outra** (medido: S-1 lê só `selector_ast.hpp`; S-2 lê `selector_ast.hpp` + `compound_match.hpp`; S-3 lê só os headers de declaração). Ainda assim, **um implementador por vez**: as três tocam `src/gfss/CMakeLists.txt` ou `src/gfui/CMakeLists.txt`, `tests/CMakeLists.txt` e S-3/S-3b/S-5 tocam `src/gfss/diagnostic_vocabulary.hpp`. Memória da casa (dois agentes na mesma árvore): esses arquivos colidem sempre.
- **S-3b vem logo depois de S-3** e antes de S-5: a lista de avisos nasce no resultado do **bloco** (`declaration_list_parse_result`), e S-5 só a recolhe. Assim o aviso vale também para o texto inline (`inline_style_text_parses_with_the_same_function`), que nunca passa pelo leitor de folha.
- **S-4 entra depois de S-2** (precisa do casamento "algum" e da especificidade zero), antes de S-5 (o átomo de seletor morto de S-5 recursa para dentro do `:where()`).
- **S-5 fecha o miolo**: é quem entrega uma folha inteira parseada, com regras em ordem de fonte e especificidade anexada, o insumo exato que `GFSS-CASCADE` (W10) declara na coluna de dependência.
- **S-6 é sempre a última** (`CI-VERDE-W6`, `TODO.md:417`).

**Caminho principal da L-32:** a onda W6 é a trilha `gfss`, o slot único de trilha paralela decidido pelo líder em 22/08 ("rcss primeiro"). Nada de mapa, nada de layout aqui.

---

## 3. As decisões desta onda (D-W6-1 a D-W6-11), separadas por quem decide

Coluna **Quem**: **LÍDER** = muda o que o autor da folha observa ou o que a biblioteca aceita; vai por `AskUserQuestion` antes de a fatia ser despachada (lista consolidada em §8). **CTO** = desenho interno; aplicada, registrada para ele reverter.

| # | Decisão | Opções na mesa | Escolhida e porquê | Quem | Mão única |
|---|---|---|---|---|---|
| D-W6-1 | **Forma da especificidade** | (a) um inteiro empacotado (`ids*10000 + classes*100 + types`); (b) três contadores com comparação lexicográfica; (c) três contadores de 8 bits | **(b)**, três `std::uint32_t` com **saturação** (nunca estouro): (a) mente com 100 classes (`.a` × 100 vira um id, o defeito clássico que a CSS-Tricks documenta), (c) satura cedo demais para folha gerada por máquina. Comparação `operator<=>` lexicográfica `(ids, classes, types)`. Pseudo-elemento conta em `types` (CSS Selectors 4 §17), atributo e pseudo-classe em `classes`, `*` e combinadores em nada. | CTO | Não (interno; `GFSS-API` decide se vira público) |
| D-W6-2 | **Direção e forma do casamento complexo** | (a) esquerda para direita (como o autor lê); (b) direita para esquerda com retrocesso em `descendant`/`~` | **(b)**. Motivo medido pela comunidade (dossiê §5.1) e a nossa régua: o sujeito é quem mais reprova, então é quem se olha primeiro; provado por contagem de chamadas (F10). `>` e `+` são passo único (pai/irmão anterior); `descendant` e `~` iteram candidatos e **retrocedem** (caso `.a > .b .c` sobre `.a > .b > .b > .c`, que a versão gulosa reprova errado). | CTO | Não |
| D-W6-3 | **Onde e como o atalho expande** | (a) num passe separado que o leitor de folha chama; (b) dentro do `fold` do leitor de lista, no índice do atalho; (c) no leitor de uma declaração | **(b)**. (c) faria `parse_declaration` devolver N declarações e quebraria o contrato "uma declaração lida"; (a) deixaria o texto inline (`inline_style_text_parses_with_the_same_function`) sem expansão. Em (b) a posição é preservada por construção: os longhands entram **no lugar** do atalho, e o invariante "nenhuma declaração com `is_shorthand` sobrevive na lista" ganha teste. `!important` do atalho copia para todos os longhands; `has_reserved_notice` de cada longhand vem do registro. | CTO | Não |
| D-W6-4 | **Longhand omitido pelo atalho** | (a) nasce com o valor inicial do registro (padrão CSS); (b) nasce "ausente" (não emitido) | **(a)**, é o padrão e é o que `GFSS-CASCADE` espera (um atalho **é** as N declarações). O aviso de "reset acidental" é D-W6-14, aditivo, não muda (a). | CTO | Não |
| D-W6-5 | **O que `:scope` ancora** | (a) sempre o nó passado como escopo; sem escopo, nunca casa; (b) o nó passado; **sem escopo, a raiz** (CSS Selectors 4 §3.5: "if no scoping root, `:scope` is equivalent to `:root`"); (c) sempre a raiz | **(b)**, é o padrão, e é o que resolve a divergência do dossiê §5.2 **por escrito e por teste nos dois contextos**. A função interna recebe o escopo explícito (`gltfx_node_view`, nulo = sem escopo); `GFSS-API` decide a forma pública. | CTO (aplica o padrão) | Não |
| D-W6-6 | **Seletor válido que nunca casa** (`:scope` seguido de `+` ou `~`) | (a) silêncio (padrão CSS); (b) recusar como erro; (c) **aceitar, e emitir aviso numa lista separada**, contado | **(c), DECIDIDO PELO LÍDER em 15/09/2026 (D3)**, conforme recomendado. (a) é a falha silenciosa que o dossiê §6 nomeia como padrão transversal; (b) diverge do padrão sem ganho (o CSS aceita). O aviso usa o mecanismo que já existe (`reserved_notice`, F5): a regra entra na folha, é inerte, e `notices` diz por quê, com linha, coluna e `expected = descendant_or_child_combinator_after_scope`. Sem gravidade no tipo (decisão de 09/09); a lista separada **é** a gravidade. Recursão: a regra vale dentro de `:not()` e de `:where()` (o `:not(:scope + a)` é sempre-verdadeiro, igualmente enumerável). | **LÍDER, decidido** | Não |
| D-W6-7 | **Lista de seletores dentro de `:where()`: tolerante ou estrita?** | (a) tolerante como o `:where()` do CSS (entrada inválida descartada em silêncio); (b) **estrita como o nosso `:not()`** (uma inválida reprova a REGRA INTEIRA com diagnóstico) | **(b), DECIDIDO PELO LÍDER em 15/09/2026 (D2)**, conforme recomendado. ⚠️ **Com D1 (D-W6-12), isto é DIVERGÊNCIA DELIBERADA do CSS real, decisão consciente do líder, datada 15/09/2026:** mesmo nome `:where()`, comportamento de erro oposto (o CSS engole o seletor inválido por especificação; aqui a regra inteira é recusada, com diagnóstico). Quem vier do CSS tropeça exatamente uma vez, e o diagnóstico existe para que seja uma só. **Três obrigações, exigência do orquestrador:** (a) esta linha é o registro; (b) o diagnóstico desse caso **diz a divergência com todas as letras, em inglês**, na forma que R7 de `docs/api-conventions.md` permite (identificador, nunca frase; ver D-W6-12 para a forma exata); (c) o caso é enumerado na prova de S-4, vermelho antes de verde. | **LÍDER, decidido** | **Sim** (mudar depois de aceitar folha é quebra de dado) |
| D-W6-8 | **Orçamento de aninhamento do grupo** | (a) contador próprio; (b) **o mesmo `k_max_not_nesting_depth = 10`, compartilhado** | **(b)**: o vetor de ataque é a pilha (dossiê §5.4), e a pilha é uma só; dois contadores independentes permitiriam 20 níveis. A constante é renomeada para `k_max_nested_selector_list_depth` e o campo `not_selectors` para `nested_selectors` (o nome antigo mentiria com dois donos, L-17 "o nome é o árbitro"). | CTO | Não |
| D-W6-9 | **`border-radius` com `/` (raios elípticos)** | (a) aceitar e guardar cru; (b) **recusar com diagnóstico** nomeando a forma aceita | **(b)**: o registro dá **um valor** por canto (F8); aceitar `/` seria prometer o que a caixa não representa. `expected = corner_radius_without_slash`, `detail` = os quatro longhands. Se um dia o registro ganhar raio elíptico, entra como acréscimo compatível. | CTO (aplica o registro [CTO] de 05/09; confirmação retroativa em §8) | Não |
| D-W6-10 | **Forma do resultado da folha** | (a) uma lista só de "coisas" com tag; (b) **listas separadas**: `rules` (estilo, na ordem da folha, cada uma com `selectors[]`, `specificities[]` paralelo, `declarations[]` já expandidas, `source_order`, linha/coluna), `raw_blocks` (`@keyframes`: nome, corpo cru como `string_view` byte a byte, linha/coluna), `rejected` (regra ou declaração descartada, com diagnóstico), `ignored_at_rules` (`@media` e desconhecidas, com diagnóstico), `notices` (D-W6-6), e as contagens de cada lista | **(b)**: cada lista tem um leitor diferente (cascata, animação, quem mostra erro, quem mostra aviso), e uma lista só obrigaria todo leitor a filtrar. `string_view` para a folha inteira: a folha é imutável pós-parse (decisão 2 de 21/08) e quem a mantém viva é `GFSS-API` (W10), escrito no header. | CTO | Não (interno até W10) |
| D-W6-11 | **Como o leitor de folha entrega o bloco ao leitor de declarações** | (a) muda `parse_declaration_list` para parar em `}`; (b) **entrega um cursor cujo `source` é o miolo entre as chaves**, com linha/coluna do primeiro byte do miolo | **(b)**: não toca contrato já provado (F3) e mantém um átomo por assunto: quem acha a fronteira da regra é `rule_boundary`, quem lê o bloco é o leitor que já existe. | CTO | Não |
| D-W6-12 | **O nome do seletor de especificidade-zero** | `:without-specificity()` (recomendado pelo CTO), `:where()`, `:zero-specificity()` | **`:where()`, DECIDIDO PELO LÍDER em 15/09/2026 (D1)**, contra a recomendação do CTO e a do orquestrador; executado sem discussão. Consequência com D-W6-7: divergência deliberada, registrada ali. **Identificador do diagnóstico corrigido pelo orquestrador (15/09/2026, segunda revisão): o proposto `valid_selector_inside_where_which_is_strict_unlike_css` (53 caracteres, 8 sublinhados = 9 palavras) foi RECUSADO por violar R7 ("nunca uma frase"):** ⚠️ **correção de medição, não só de nome**: a régua original citava "12 identificadores, o mais longo `internal_tokenizer_defect` (25 caracteres, 2 sublinhados), média 0,9 sublinhado", medida por grep de STRING LITERAL (`grep -hoE '"[a-z][a-z0-9_]{4,}"'`); rodei esse comando e ele pega só resquício de PROSA de comentário (`"length"`, `"swept"`, nenhum dos dois é identificador, ver `diagnostic_vocabulary.hpp:131` e `:165`), porque os dois arquivos migraram para X-macro (`X(nome, produtor)`, sem aspas) e a string literal não é mais como o vocabulário se lê. **Medição real, técnica X-macro, reconferida pelo orquestrador (15/09/2026, quarta revisão): o revisor disse 50, não 49: os dois números estão certos, por definições diferentes, e o plano precisa dizer qual.** `grep -c -oE 'X\([a-z_0-9]+(?:,|\))' src/gfss/diagnostic_vocabulary.hpp src/gfss/color_diagnostic_vocabulary.hpp` devolve **35 + 15 = 50 entradas X-macro** (linhas da tabela, uma por arquivo); dessas, `closing_parenthesis` aparece em AMBOS os arquivos (dois enums independentes, mesmo nome), então a contagem de **strings distintas** é **49**. O plano usa 49 (a pergunta que importa aqui é "qual o nome mais comprido/mais raro que já existe", questão sobre FORMA de string, e contar a mesma string duas vezes não muda a distribuição); o revisor mediu 50 (linhas da tabela), também correto, para outra pergunta. O mais longo continua `semicolon_or_end_of_declaration` (31 caracteres, 4 sublinhados = 5 palavras) nos dois jeitos de contar; a média sai **1,94 sublinhado sobre as 49 únicas** ou **1,92 sobre as 50 linhas cruas** (≈2,9 palavras nos dois casos), não 12/25/0,9. A conclusão do conserto não muda (53 caracteres/9 palavras seguem muito acima do que a casa escreve), só o número que a prova cita. **Escolhido `known_where_selector`** (20 caracteres, 2 sublinhados = 3 palavras): casa byte a byte em forma com os **7** identificadores `known_*` já existentes (`known_pseudo_class` 18, `known_property_name` 19, `known_color_keyword` 19, `known_dimension_unit`/`known_color_function`/`known_pseudo_element` 20, `known_pseudo_function` 21 caracteres, todos com exatamente 2 sublinhados), o precedente mais forte da lista inteira. Nomeia O QUE falhou (um seletor dentro de `:where()` não era conhecido/válido), nunca a comparação com o CSS: a comparação é explicação, e mora na prosa de `docs/node-view-and-matching.md` (não no token). **A forma do diagnóstico, sob R7 (vocabulário de token, nunca frase, sem catálogo de mensagem, decisão permanente do líder):** `expected = known_where_selector`, `detail` = os `expected` do seletor de dentro que falhou (ex.: `known_pseudo_class`), com linha e coluna **de dentro** do argumento. **Citação corrigida pelo orquestrador (15/09/2026, terceira revisão): a citação anterior estava invertida.** Conferido em `src/gfss/selector_parse.cpp:402`, o próprio comentário do `:not()` diz que ele *"Propagates the NESTED diagnostic UNCHANGED"*: `:not()` repassa o diagnóstico de dentro SEM casca, o oposto do desenho embrulhado proposto aqui. **O precedente real do embrulho (`expected` fixo + `detail` com o dado de dentro) é `src/gfss/refused_property_names.hpp` + `src/gfss/property_name_lookup.cpp:68-73`:** toda propriedade recusada sai com `expected = k_expected_renamed_property_name` (identificador FIXO, não o de dentro) e `detail = entry.detail` (o dado específico, de uma tabela fechada), a mesma forma que `known_where_selector` (fixo) + `detail` (o `expected` de dentro) propõe aqui. O embrulho continua sendo a escolha certa (a divergência com o CSS é o que o consumidor precisa ver primeiro; um `expected` fixo é o que se filtra e se documenta, o de dentro varia por caso), só a citação que o sustentava estava errada. **A frase completa que explica a divergência, cobrada por documento+seção+portão nomeados (conserto do orquestrador, 15/09/2026; a promessa antiga ("mora na documentação pública, DOCS-PUB/wiki, W10") não tinha cobrança nenhuma dentro desta onda):** mora em `docs/node-view-and-matching.md`, seção nova `## Why :where() here is strict, unlike CSS` (doc interno, inglês, já estendido por S-2 com a seção de combinadores; a tradução pública continua sendo obrigação de `DOCS-PUB`/wiki em W10, mas o texto nasce AQUI, nesta onda, onde o código nasce; W10 traduz o que já existe, não escreve do zero). **Portão novo, nasce em S-4, com auto-teste no mesmo commit (L-36):** `tests/tools/check_where_divergence_documented.py` (varre `docs/node-view-and-matching.md`, exige o cabeçalho exato da seção, exige que o corpo contenha o identificador `known_where_selector` **e** as palavras `strict` e `unlike CSS`, imprime a contagem, reprova em zero, L-40). **Citação de técnica corrigida pelo orquestrador (15/09/2026, terceira revisão): não é "forma de `check_dash_pubdoc.py`".** Aquele portão varre o REPOSITÓRIO INTEIRO atrás de um caractere proibido, técnica distinta de checar UMA seção num arquivo fixo; o que os dois compartilham é só o esqueleto de `--selftest` com fixture positiva e negativas, a mesma forma que `check_dash_pubdoc.py:327` em diante já usa (`make_positive_fixture`, fixtures negativas, `Capture`/`capture_stderr`). **Sete controles do autoteste, enumerados, piso não-vazio (L-40: zero controles rodados reprova), corrigido pelo orquestrador (15/09/2026, quarta revisão): o corpo exigido é uma CONJUNÇÃO de três requisitos (o identificador, a palavra `strict`, a expressão `unlike CSS`), e um requisito conjuntivo se testa tirando um de cada vez, e o espaço é pequeno e fechado: então se enumera inteiro, não amostrado.** (1) seção AUSENTE do documento, reprova; (2) seção PRESENTE, cabeçalho certo, corpo VAZIO (só o título, sem prosa), reprova (o caso mais provável de escapar: título certo não é conteúdo); (3) corpo com `strict` e `unlike CSS`, mas SEM o identificador `known_where_selector`, reprova; (4) corpo com o identificador e `strict`, mas SEM `unlike CSS`, reprova; (5) corpo com o identificador e `unlike CSS`, mas SEM `strict`, reprova; (6) a seção existe, mas no ARQUIVO ERRADO (ex.: `docs/gfss-property-registry-v1.md` em vez de `docs/node-view-and-matching.md`), reprova; (7) **controle POSITIVO**: seção presente, cabeçalho certo, corpo com os três (identificador, `strict`, `unlike CSS`), PASSA (sem este controle, o portão pode estar reprovando sempre e ninguém percebe). Autoteste roda contra fixtures em `tests/tools/fixtures/`, provado vermelho nos seis primeiros e verde no sétimo antes de o documento real existir. ⚠️ **Tensão declarada ao orquestrador:** se "com todas as letras" exigir frase dentro do próprio diagnóstico em tempo de execução, isso quebra R7, e R7 é do líder; o plano fica no identificador em tempo de execução e na frase em documentação, nunca misturados. | **LÍDER, decidido** (nome e estrutura); identificador e portão são desenho do CTO/orquestrador | **Sim** (vocabulário do formato) |
| D-W6-13 | **Onde vive a lista de avisos** | (a) só no resultado da folha (S-5); (b) **no resultado do bloco** (`declaration_list_parse_result.notices`), recolhida pela folha | **(b)**: o aviso de reset acidental (D-W6-14) é fato do bloco, e o texto inline nunca passa pela folha. A folha soma as listas dos blocos à sua (D-W6-10) e acrescenta as suas próprias (seletor morto). Mesmo tipo de diagnóstico, campo `expected` do vocabulário fechado. | CTO | Não |
| D-W6-14 | **Aviso de reset acidental por atalho** | (a) silêncio (padrão CSS); (b) **avisar** quando um atalho anula um longhand que o mesmo bloco já escreveu antes | **(b), DECIDIDO PELO LÍDER em 15/09/2026 (D4)**, conforme recomendado. O comportamento **não muda** (o atalho continua apagando o omitido, D-W6-4); o que muda é que o autor fica sabendo: um aviso por longhand anulado, com linha e coluna do atalho, `expected = longhand_not_overridden_by_later_shorthand`, `detail` = o nome do longhand anulado. Só o **mesmo bloco** (a cascata entre regras é da W10, e ali "última vence" é o desenho, não acidente). S-3b deixa de ser opcional e é fatia normal da onda. | **LÍDER, decidido** | Não |
| D-W6-15 | **O canon que nega o bloco de animação** (`ESCOPO.md:453`, achado do orquestrador) | (a) consertar depois, em onda de manutenção; (b) átomo dentro de S-5; (c) **fatia própria S-0, antes de tudo** | **(c)**: pela régua de fronteira da L-17 é assunto próprio (canon, não código) com nome honesto ("a decisão 5 deixa de afirmar o que não vale"); dentro de S-5 misturaria dois leitores (quem lê `ESCOPO.md` e quem lê `sheet_parse.cpp`); depois é o que a L-67 proíbe (canon desatualizado bloqueia o que depende dele). **Medido em 15/09/2026, corrigido na quarta revisão do plano: a varredura de fechamento é só sobre `ESCOPO.md` e `TODO.md`, nomeados** (a versão anterior citava um glob `docs/*.md`, que se contamina com a prosa deste próprio plano assim que ele for commitado; ver §4 linha 0 para a causa completa). Sobre os dois arquivos nomeados: 3 passagens afirmam o texto morto como vigente (`ESCOPO.md:411`, `ESCOPO.md:453`, `TODO.md:53`), 4 são o registro da revogação e **ficam** (`ESCOPO.md:536`, `ESCOPO.md:540`, `TODO.md:84`, `TODO.md:414`, este último a própria linha da fatia `GFSS-SHEET-PARSE`, achado do revisor adversarial, ausente da conta original); a varredura ampla de investigação (que incluía `CLAUDE.md`/`GODS_LAWS.md`/`docs/*.md`) já havia medido zero fora dos dois nomeados. O que o líder revoga é **apagado**, não arquivado (L-67): a linha 453 passa a dizer que a decisão 5 foi revogada em 26/08 e aponta para a linha 536; as linhas 411 e `TODO.md:53` deixam de listar animação entre as decisões "de pé". **Conserto do orquestrador (15/09/2026, segunda revisão): a mesma fatia S-0 também grava D1-D4 (D-W6-12, D-W6-7, D-W6-6, D-W6-14) em `ESCOPO.md`**, achado do orquestrador de que as quatro decisões de hoje existiam só dentro deste plano, numa seção nova `## Ordem de produto de 15/09/2026: :where(), especificidade zero, e mais duas decisões da folha de estilo`, na forma `### Decisão N` (mesma forma da seção "As seis decisões de 28/08/2026", `ESCOPO.md:1019`), cada entrada com `**Origem:** relato do orquestrador (docs/plano-w6-folha-de-estilo.md, D-W6-*), 15/09/2026, sem verbatim do líder capturado nesta sessão` (a mesma marca de proveniência que o topo deste plano já usa, linha 5). **Medido ANTES (15/09/2026):** `grep -c "15/09/2026" ESCOPO.md` = 0, `grep -c ":where" ESCOPO.md` = 0. **Verde DEPOIS exigido:** `grep -c -E "D-W6-12|D-W6-7\b|D-W6-6\b|D-W6-14" ESCOPO.md` = 4 (uma citação por decisão, piso não-vazio, L-40) e `grep -c ":where" ESCOPO.md` ≥ 1. | CTO (forma); a revogação é do líder (26/08); as quatro decisões D1-D4 são do líder (15/09/2026) | Não |

**O que estas decisões NÃO reabrem (do líder, e não toco):** os onze atalhos e os dois derrubados (`ESCOPO.md` §4, "o que a régua derrubou"; E3 do registro); `!important` na v1 (decisão 9); `@media` fora, com diagnóstico (decisão 4); animação reaberta e bloco guardado cru (decisões 23-24, `TODO.md:414`); gravidade só na revisão de API (09/09); folha imutável pós-parse; diagnóstico em inglês nomeando o vocabulário do autor (hoje); R7 de `docs/api-conventions.md` (vocabulário de token, sem catálogo de mensagem).

---

## 4. As fatias, cada uma com o teste vermelho, e a coluna que o portão cobra

Legenda: **Prova** é nome de `ctest`, igual nos cinco sistemas (F12). **Par no portão** é sempre `mesmo nome`: nenhuma fatia desta onda tem `#if` por sistema, e o portão de paridade vê o mesmo executável dos dois lados. **Vermelho de estreia**: onde possível é falha de **asserção em tempo de execução** contra a árvore de hoje, não falha de compilação (compilação vermelha só prova que o header não existe).

| # | Fatia | Lado | Nasce / muda | Teste vermelho de estreia (L-20, o que falha ANTES) | Prova | Par no portão |
|---|---|---|---|---|---|---|
| 0 | **S-0 canon: a decisão 5 deixa de afirmar o que não vale** (fatia dentro de `GFSS-SHEET-PARSE`, sem linha própria; o orquestrador decide se abre uma; L-67 global) | comum (documento) | Muda `ESCOPO.md` (linha 453: a decisão 5 passa a ler "REVOGADA em 26/08/2026, ver as 8 decisões sobre movimento e luz abaixo", sem o texto morto; linha 411: a lista de "o que não mudou" perde "animação e transição fora" e ganha a remissão), `TODO.md` (linha 53, seção "A trilha GFSS": a lista das cinco decisões perde "animação/transição/`@keyframes` fora" e diz que a quinta foi revogada, apontando para a linha 84). **Conserto do orquestrador (15/09/2026, quarta revisão do plano): a varredura se contava a si mesma.** O padrão casa **2 vezes dentro deste próprio plano** (nas passagens que descrevem o texto morto para mandar apagá-lo); o plano ainda está fora do controle de versão, e no instante em que for commitado a conta sobre um glob `docs/*.md` passaria de 7 para 9, e o "depois" de 4 viraria 6, reprovando uma S-0 perfeita pela mesma causa raiz da rodada anterior. **A varredura de fechamento passa a nomear só o canon, `ESCOPO.md` e `TODO.md`, um a um, nunca um glob sobre `docs/*.md`:** o assunto de S-0 é o canon, e prosa de PLANO que descreve o texto morto para apagá-lo não é o texto morto; lista explícita não se alarga sozinha quando alguém cria um arquivo novo em `docs/`, padrão se alarga (e teria contaminado a conta de novo). Sobre os dois arquivos nomeados, o grep devolve `ESCOPO.md:411`, `ESCOPO.md:453`, `ESCOPO.md:536`, `ESCOPO.md:540`, `TODO.md:53`, `TODO.md:84`, `TODO.md:414` (sete, contadas linha a linha, nunca por intervalo: "536-540" não é uma linha, são duas). **Dois números do revisor adversarial que a medição não confirma:** `docs/plano-w6a-janela.md`, `docs/plano-w6b-placa-e-laco.md` e `docs/plano-loop-callbacks.md` (os três rastreados) casam **zero** vezes cada um, não estão contaminados; e o piso de saída é EXATO em 4, nunca "pelo menos 4" (piso frouxo perde a capacidade de detectar sobra, que é para o que a varredura existe). Destino de cada uma das sete, sem exceção: `ESCOPO.md:411` **REESCREVE** (perde "animação e transição fora" da lista "o que não mudou", ganha a remissão); `ESCOPO.md:453` **REESCREVE** (a decisão 5 passa a "REVOGADA em 26/08/2026, ver linha 536"); `ESCOPO.md:536` **PRESERVA** (cita o texto morto **como citação**, é o registro); `ESCOPO.md:540` **PRESERVA** (diz "está revogado", é o registro); `TODO.md:53` **REESCREVE** (a lista das cinco decisões perde "animação/transição/`@keyframes` fora", ganha a remissão para a linha 84); `TODO.md:84` **PRESERVA** (registro da revogação, já correto); `TODO.md:414` **PRESERVA** (achado do revisor: é a própria linha da fatia `GFSS-SHEET-PARSE`, já REESCRITA em 26/08/2026 (L-28) para refletir a revogação e reconhecer o bloco de animação cru; não precisa de nenhum toque nesta onda, e passa a constar explicitamente aqui em vez de ausente do plano). **A mesma fatia também GANHA uma seção nova em `ESCOPO.md`**: `## Ordem de produto de 15/09/2026: :where(), especificidade zero, e mais duas decisões da folha de estilo`, com quatro `### Decisão N` (D1 nome do seletor, D2 lista estrita, D3 `:scope`+irmão aviso, D4 reset acidental aviso), cada uma citando o código `D-W6-*` correspondente (D-W6-12, D-W6-7, D-W6-6, D-W6-14) e marcada `**Origem:** relato do orquestrador, não verbatim do líder`. Nenhum arquivo de código nasce; só as duas seções de `ESCOPO.md` e as duas linhas de `TODO.md` mudam. | **Vermelho ANTES:** a varredura que GOVERNA o fechamento de S-0 é `grep -n -i -E 'anima[çc][aã]o.{0,40}fora|keyframes.{0,30}fora' ESCOPO.md TODO.md` (só os dois arquivos nomeados, nunca `docs/*.md`; a razão está acima), medida sobre os arquivos RASTREADOS por `git ls-files`. Devolve **7** linhas hoje (medido em 15/09/2026, linha a linha: 3 vigentes [`ESCOPO.md:411`, `ESCOPO.md:453`, `TODO.md:53`] mais 4 registro [`ESCOPO.md:536`, `ESCOPO.md:540`, `TODO.md:84`, `TODO.md:414`]). **Verde DEPOIS:** devolve exatamente **4**, as do registro listadas acima, nenhuma a mais nem a menos: número EXATO, nunca "pelo menos". Piso não-vazio (L-40): **zero linhas depois reprova**, porque o registro da revogação tem de existir; a contagem antes e depois entra no relatório da fatia, sempre citando o comando e o par de arquivos sobre o qual rodou. **A varredura ampla usada na investigação do gêmeo** (`ESCOPO.md TODO.md CLAUDE.md GODS_LAWS.md docs/*.md`, a mesma que rodadas anteriores citaram) **não é o critério de fechamento**: serviu, uma vez, para provar que não havia texto morto fora de `ESCOPO.md`/`TODO.md` (achado: zero em `CLAUDE.md`/`GODS_LAWS.md`/resto de `docs/`, e zero em `docs/plano-w6a-janela.md`, `docs/plano-w6b-placa-e-laco.md`, `docs/plano-loop-callbacks.md`); ela nunca decide se S-0 fecha, porque um glob se alarga sozinho, e este próprio plano é a prova, casando 2 vezes dentro de si mesmo. **Segunda medição desta fatia, decisões novas:** ANTES (15/09/2026) `grep -c "15/09/2026" ESCOPO.md` = 0 e `grep -c ":where" ESCOPO.md` = 0; DEPOIS exigido `grep -c -E "D-W6-12|D-W6-7\b|D-W6-6\b|D-W6-14" ESCOPO.md` = 4 e `grep -c ":where" ESCOPO.md` ≥ 1, piso não-vazio nas duas, cada uma reprovando em zero. | (não é `ctest`; a prova é a varredura contada acima, executada pelo revisor; `check_dup_laws.py` continua verde porque nenhum bloco `DUP-BLOCK` é tocado) | nenhum necessário (documento) |
| 1 | **S-1 `GFSS-SPECIFICITY`** | comum (5 sistemas) | Nasce `src/gfss/specificity.hpp` (`gfss_specificity{ids, classes, types}`, `std::uint32_t` cada, `operator<=>` lexicográfico, `saturating_increment`), `src/gfss/specificity.cpp` (`specificity_of_simple(const gfss_simple_selector&)` = a coluna que UM seletor simples toca; `specificity_of_compound`; `specificity_of_complex` = soma por compostos, combinador soma zero; `:not()` = **máximo** entre os `not_selectors` (nome de hoje; conserto do orquestrador, 15/09/2026, achado do revisor adversarial: S-1 é push 1 e não pode assumir o nome `nested_selectors`, que só nasce em S-4/D-W6-8, push 3), sem contar a si), `tests/gfss_specificity_test.cpp`. Muda `src/gfss/CMakeLists.txt`, `tests/CMakeLists.txt` (o teste linka `selector_parse.cpp`, `anb_parse.cpp`, `specificity.cpp`). | O teste parseia `#a.b c` e assevera `(1,1,1)`: hoje não há função, vermelho por construção; **o vermelho que vale** é o de mutação obrigatória na revisão: trocar `max` por soma em `:not()` reprova `not_contributes_the_most_specific_argument_never_the_sum`; trocar a ordem das colunas reprova `comparison_is_lexicographic_ids_then_classes_then_types` (`(0,2,0) < (1,0,0)` e `(0,0,9) < (0,1,0)`). | `gfss_specificity_test`: **enumeração fechada dos 8 tipos de seletor simples** (`gfss_simple_selector_kind`, contagem impressa = 8; tipo sem linha na tabela reprova em `static_assert`), cada um com a coluna que toca (`*` toca nenhuma; `[id="x"]` toca `classes`, dossiê §2.1; `::before` toca `types`); os 4 combinadores somam zero (contagem = 4); `:not()` 3 casos (um arg, lista, aninhado até 10); comparação 6 casos; **saturação**: `#a` repetido 4 294 967 295 vezes não cabe em texto, então a prova é sobre o átomo `saturating_increment` na fronteira e um passo além (memória da casa: um passo além da borda). | mesmo nome |
| 2 | **S-2 `GFSS-MATCH-COMBINE`** | comum | Nasce `src/gfui/match_verdict.hpp` (o enum `match_verdict{matched, rejected, deferred}`, que **sai** de `compound_match.hpp` e passa a servir os dois níveis; `compound_match_verdict` deixa de existir), `src/gfui/combinator_step.hpp` + `src/gfui/combinator_step.cpp` (átomo: dado um combinador e um nó, o **próximo candidato** à esquerda: `child` = pai, `next_sibling` = irmão anterior, `descendant` = pai (iterável até a raiz), `subsequent_sibling` = irmão anterior (iterável até o primeiro); devolve nó nulo quando não há), `src/gfui/deferred_simple_match.hpp` + `src/gfui/deferred_simple_match.cpp` (átomo: julga num nó, com escopo, os simples que `compound_match` adia e **esta** fatia possui: `:not()` = nenhum dos `not_selectors` casa com o nó como sujeito (gêmeo do conserto de S-1, mesmo motivo: S-2 também é push 1); `:scope` = D-W6-5; `:placeholder-shown` e pseudo-elemento continuam `deferred`), `src/gfui/complex_match.hpp` + `src/gfui/complex_match.cpp` (`match_complex(const gfss_complex_selector&, node, scope) -> match_verdict`: sujeito = último composto; anda `rest` de trás para a frente; `>`/`+` passo único; `descendant`/`~` com retrocesso; um composto `rejected` reprova; `deferred` do composto passa por `deferred_simple_match`; sobrando adiado, o veredito é `deferred`), `tests/gfui_complex_match_test.cpp`. Muda `src/gfui/compound_match.hpp` (usa `match_verdict`), `src/gfui/compound_match.cpp` (`:scope` e `:not` continuam marcando adiado, o comentário aponta para o novo dono), `tests/gfui_compound_match_test.cpp` e `tests/gfui_match_struct_test.cpp` (nome do enum), `src/gfui/CMakeLists.txt`, `tests/CMakeLists.txt`, `docs/node-view-and-matching.md` (seção "How a complex selector gets judged", inglês, e a lacuna de `:scope` sai da lista de lacunas). | **Vermelho em tempo de execução contra a árvore de hoje**: `match_compound` de `div:not(.x)` sobre um `div` sem `.x` devolve `deferred`; o teste novo exige `matched` de `match_complex`. Mutações obrigatórias na revisão: (i) andar da esquerda para a direita reprova `subject_is_judged_first_and_the_parent_is_never_asked_when_it_fails` (árvore contadora, F10: zero chamadas a `tag_name` do pai); (ii) `descendant` guloso sem retrocesso reprova `descendant_backtracks_when_the_nearest_candidate_leads_to_a_dead_end` (`.a > .b .c` sobre `.a > .b > .b > .c`); (iii) `:scope` sem escopo casando nada reprova `scope_without_a_scoping_root_is_the_root`. | `gfui_complex_match_test`: **matriz fechada dos 4 combinadores × {casa, não casa, precisa retroceder}** (12 células, contagem impressa; `>`/`+` não têm retrocesso e a célula declara "não se aplica", contada, nunca omitida); `:not()` recursivo 4 casos (um arg, lista, aninhado a 10, `body :not(table) a` casa o `a` dentro da tabela, dossiê §5.5); `:scope` 3 casos (com escopo = só ele; sem escopo = raiz; `a + :scope` casa); adiamento preservado 2 casos (`:placeholder-shown`, `::before` devolvem `deferred`, nunca `matched`); custo por contagem 1 caso. | mesmo nome |
| 3 | **S-3 `GFSS-SHORTHAND`** | comum | Nasce `src/gfss/shorthand_longhand_table.hpp` (a tabela **fechada**: nome do atalho → família + longhands na ordem de expansão; `static_assert(size == k_shorthand_name_count)`; 11 linhas, 5 famílias), `src/gfss/shorthand_component_split.hpp` + `src/gfss/shorthand_component_split.cpp` (átomo: parte os `raw_tokens` em componentes por espaço de topo, `nesting_depth`-ciente, para `rgb( 1 , 2 , 3 )` ser um componente; recusa `/` fora de função, D-W6-9), `src/gfss/shorthand_four_sides.hpp` + `.cpp` (margin, padding, border-width, border-style, border-color: 1→4, 2→(v,h), 3→(t,h,b), 4→TRBL), `src/gfss/shorthand_four_corners.hpp` + `.cpp` (border-radius: 1→4, 2→(TL+BR, TR+BL), 3→(TL, TR+BL, BR), 4→TL,TR,BR,BL; **arquivo próprio, nunca reutiliza o de lados**, dossiê §3.2), `src/gfss/shorthand_axis_pair.hpp` + `.cpp` (gap: row [column]; overflow: x [y]), `src/gfss/shorthand_border_parts.hpp` + `.cpp` (border, outline: 1 a 3 componentes, **cada um classificado pelo que é** com os contratos dos longhands: largura = comprimento ou `thin/medium/thick`; estilo = `none/solid`; cor = cor; cada tipo no máximo uma vez; ordem livre; `border` expande para 12 longhands, `outline` para 3, `outline-offset` **não** é tocado, como no CSS), `src/gfss/shorthand_flex_flow.hpp` + `.cpp` (1 a 2 palavras de conjuntos disjuntos, ordem livre), `src/gfss/shorthand_expand.hpp` + `src/gfss/shorthand_expand.cpp` (o despachante: dado um `gfss_declaration` com `is_shorthand`, devolve `{ok, longhands[], diagnostic}`; palavra universal (`inherit`/`initial`/`unset`) sozinha expande para todos os longhands com a palavra; cada longhand passa por `check_declaration_value` com `property_value_contract_for`; `important` copiado; `has_reserved_notice` do registro), `tests/gfss_shorthand_expand_test.cpp`. Muda `src/gfss/declaration_list_parse.cpp` (`fold_into_result` expande e insere no lugar), `src/gfss/diagnostic_vocabulary.hpp` (produtor `shorthand_expand`; entradas: `one_to_four_side_values`, `one_to_four_corner_values`, `corner_radius_without_slash`, `one_or_two_axis_values`, `border_part_width_style_or_color`, `each_border_part_at_most_once`, `flex_direction_or_flex_wrap_word`; cada `detail` nomeia os longhands ou as palavras aceitas, em inglês, decisão do líder de hoje), `src/gfss/CMakeLists.txt`, `tests/CMakeLists.txt`, `tests/gfss_declaration_parse_test.cpp` (o teste dos onze passa a asseverar também que **nenhuma** declaração com `is_shorthand` sobrevive a `parse_declaration_list`). | **Vermelho em tempo de execução contra a árvore de hoje**: `parse_declaration_list("margin: 1px")` devolve 1 declaração com `is_shorthand == true`; o teste exige 4 longhands e zero atalhos. Mutações obrigatórias: (i) anexar a expansão no fim reprova `expansion_keeps_the_shorthand_position_so_last_declaration_wins`; (ii) não copiar `important` reprova a metade `important` da matriz; (iii) omitir o longhand não escrito (`margin: 1px` produzindo 1 em vez de 4) reprova `omitted_longhands_are_reset_to_the_registry_initial`. | `gfss_shorthand_expand_test`: **matriz fechada 11 atalhos × {sem `!important`, com} × {sozinho, precedido pelo longhand explícito, seguido pelo longhand explícito}** = 66 células, cada uma asseverando a lista resultante **com índice** (quem vem antes, quem vem depois) e a flag em cada longhand (contagem impressa = 66); **contagem de componentes por família**: lados 1-4 (4 casos), cantos 1-4 (4), pares 1-2 (2 × 2), partes de borda 1-3 em ordem livre (as 6 permutações de 3 + os 3 de 1 + os 6 de 2, para `border` e `outline`), flex-flow (4 direções × 3 quebras + cada sozinha); **recusas** (5 valores em `margin`, `/` em `border-radius`, cor duas vezes em `border`, palavra fora do conjunto em `flex-flow`, `inherit` misturado com valor) cada uma com o `expected` conferido; o invariante da lista (zero `is_shorthand` sobrevivente) num caso com os 11 juntos num bloco. | mesmo nome |
| 4 | **S-4 `GFSS-SEL-ZERO-SPECIFICITY`: o `:where()`** (item NOVO, §7; **DESBLOQUEADA**: nome D1 e lista estrita D2 decididos pelo líder em 15/09/2026, D-W6-12 e D-W6-7) | comum | Nasce o portão `tests/tools/check_where_divergence_documented.py` mais as sete fixtures de auto-teste (seção ausente, corpo vazio, corpo sem o identificador, corpo sem `strict`, corpo sem `unlike CSS`, seção no arquivo errado, e o controle positivo com os três presentes), em `tests/tools/fixtures/` (conserto do orquestrador, D-W6-12; L-36: vermelho de estreia nos seis primeiros, verde no sétimo, antes do documento real existir); fora disso, nada de arquivo novo além de casos de teste (é vocabulário a mais nos mecanismos das três fatias anteriores; arquivo só de casos seria fragmentação, L-17 contraparte). Muda `src/gfss/selector_pseudo_vocabulary.hpp` (a 6ª funcional, `X("where")`; a contagem impressa do teste de vocabulário passa de 21 para 22), `src/gfss/selector_ast.hpp` (`not_selectors` → `nested_selectors`, D-W6-8; o comentário de `pseudo_function` nomeia os dois donos), `src/gfss/selector_parse.cpp` (`attach_not_argument` vira `attach_nested_selector_list` e serve os dois nomes; `k_max_not_nesting_depth` → `k_max_nested_selector_list_depth`, valor 10, compartilhado; **lista estrita, D-W6-7: o comentário de cabeçalho da linha 93, que hoje descreve o `:where()` do CSS como tolerante, passa a dizer que o NOSSO é estrito por decisão do líder de 15/09/2026, com a razão**), `src/gfss/diagnostic_vocabulary.hpp` (`not_recursion_limit` → `nested_selector_list_depth_limit`; entrada nova `known_where_selector`, D-W6-12), `src/gfss/specificity.cpp` (`:where()` contribui `(0,0,0)` **sempre**, independente do argumento), `src/gfui/deferred_simple_match.cpp` (`:where()` casa se **algum** dos `nested_selectors` casa com o nó como sujeito), `src/gfui/compound_match.cpp` (`where` marca adiado, como `not`), `src/gfss/selector_dead_check.cpp` (S-5; a regra de `:scope` + irmão recursa também para dentro do `:where()`), `docs/node-view-and-matching.md` (seção nova `## Why :where() here is strict, unlike CSS`, D-W6-12), `tests/gfss_selector_parse_test.cpp`, `tests/gfss_specificity_test.cpp`, `tests/gfui_complex_match_test.cpp`, `tests/gfss_selector_dead_check_test.cpp`, `tests/gfss_sheet_parse_test.cpp` (S-5: o caso da REGRA inteira recusada), `tests/CMakeLists.txt` (o portão novo). | **Vermelho em tempo de execução contra a árvore de hoje**: `parse_selector_list(":where(.a)")` devolve `ok == false` com `expected == known_pseudo_function`; o teste exige `ok`. Mutações obrigatórias: (i) especificidade do `:where()` = a do argumento reprova `where_contributes_zero_specificity_whatever_its_argument` (`:where(#a#b#c)` = `(0,0,0)`; e `#x:not(#y)` = `(2,0,0)` contra `#x:where(#y)` = `(1,0,0)`, a válvula do dossiê §2.3 medida); (ii) casar por "nenhum" em vez de "algum" reprova `where_matches_when_any_argument_matches`; (iii) contador de profundidade próprio reprova `not_and_where_share_one_nesting_budget` (5 `:not(` + 5 `:where(` aceitos; 6 + 5 reprovados); **(iv) a divergência (exigência c do orquestrador): tornar a lista tolerante (descartar `:bogus` e aceitar `.x`) reprova `where_is_strict_unlike_css_and_says_so`: `:where(:bogus, .x)` devolve `ok == false`, `expected == known_where_selector`, `detail == "known_pseudo_class"`, linha/coluna apontando para `:bogus`; e, na folha (S-5), `a:where(:bogus, .x) { color: red } b { color: blue }` produz `rejected.size() == 1`, `rules.size() == 1` (a regra inteira caiu, a seguinte sobreviveu). Vermelho antes de verde nos dois níveis.** **(v) o portão de documentação (D-W6-12): apagar a seção nova de `docs/node-view-and-matching.md`, ou o identificador dela, reprova `check_where_divergence_documented.py` (contagem = 0, piso não-vazio).** | Casos novos nos quatro testes existentes mais o de S-5, cada um com contagem impressa: parser 5 (lista, aninhado com `:not`, vazio reprova, inválido dentro reprova com o `expected` da divergência e o de dentro em `detail`, profundidade compartilhada); especificidade 3; casamento 3; morto 1; folha 1 (regra inteira recusada, seguinte sobrevive); portão de documentação 7 (ausente, corpo vazio, sem identificador, sem `strict`, sem `unlike CSS`, arquivo errado, controle positivo). | mesmo nome |
| 3b | **S-3b aviso de reset acidental** (fatia dentro de `GFSS-SHORTHAND`, sem linha própria; D-W6-14, decisão do líder D4) | comum | Nasce `src/gfss/shorthand_reset_notice.hpp` + `src/gfss/shorthand_reset_notice.cpp` (átomo puro sobre a lista já expandida: dado o índice onde um atalho foi expandido e a lista de longhands que ele produziu, devolve um aviso por longhand que **já aparecia** antes daquele índice no mesmo bloco; `expected = longhand_not_overridden_by_later_shorthand`, `detail` = o nome do longhand na grafia da folha, linha/coluna do atalho), `tests/gfss_shorthand_reset_notice_test.cpp`. Muda `src/gfss/declaration_ast.hpp` (`declaration_list_parse_result` ganha `std::vector<gltfx_gfss_diagnostic> notices` e `notice_count`, D-W6-13), `src/gfss/declaration_list_parse.cpp` (o `fold` chama o átomo depois de expandir), `src/gfss/diagnostic_vocabulary.hpp` (a entrada nova, produtor `shorthand_expand`), `src/gfss/CMakeLists.txt`, `tests/CMakeLists.txt`. | **Vermelho em tempo de execução** contra a árvore com S-3 fechada: `parse_declaration_list("margin-top: 8px; margin: 0")` devolve `notices` vazio; o teste exige 1 aviso nomeando `margin-top`. Mutações obrigatórias: (i) avisar também quando o longhand vem **depois** do atalho reprova `longhand_after_the_shorthand_is_not_a_reset` (ali "última vence" é o desenho); (ii) avisar por atalho e não por longhand reprova `one_notice_per_overridden_longhand` (`margin-top: 1px; margin-left: 2px; margin: 0` = 2 avisos); (iii) mudar o valor (em vez de só avisar) reprova a matriz de S-3, que continua asseverando o valor inicial. | `gfss_shorthand_reset_notice_test`: **matriz fechada 11 atalhos × {longhand antes (avisa), longhand depois (não avisa), nenhum (não avisa)}** = 33 células, contagem impressa; `border` com os 12 longhands antes = 12 avisos (contado); `!important` no longhand anterior **também** avisa (o valor some do bloco do mesmo jeito; quem decide entre `!important` e normal é a cascata, W10, e isto é dito no comentário); texto inline recebe o mesmo aviso (o caminho de `inline_style_text_parses_with_the_same_function`). | mesmo nome |
| 5 | **S-5 `GFSS-SHEET-PARSE`** | comum | Nasce `src/gfss/sheet_ast.hpp` (D-W6-10: `gfss_style_rule`, `gfss_raw_block`, `gfss_sheet_parse_result` com as cinco listas e as contagens), `src/gfss/at_rule_vocabulary.hpp` (lista **fechada** de at-rules que a v1 reconhece, com o destino de cada: `keyframes` → cru; `media` → fora da v1, ignorada com diagnóstico; contagem mecânica), `src/gfss/rule_boundary.hpp` + `src/gfss/rule_boundary.cpp` (átomo: a partir de um índice de token, o índice do `{` que abre e do `}` que fecha **contando aninhamento** com `nesting_depth` (string é um token, `}` dentro dela nunca conta); devolve "não fechou" quando chega ao EOF, com linha/coluna do `{` órfão; **iterativo, nunca recursivo**), `src/gfss/at_rule_dispatch.hpp` + `src/gfss/at_rule_dispatch.cpp` (átomo: dado um `at_keyword` e a fronteira, decide cru / ignorada / desconhecida, e monta o diagnóstico: `expected = supported_at_rule`, `detail` = os nomes que a v1 aceita, para desconhecida; identificador **distinto** para `@media`, com `detail` que a nomeia, para o teste distinguir os dois), `src/gfss/selector_dead_check.hpp` + `src/gfss/selector_dead_check.cpp` (átomo puro sobre a AST: `:scope` num composto seguido de `+`/`~` → aviso com linha/coluna do combinador e `expected = descendant_or_child_combinator_after_scope`; recursa em `nested_selectors`, D-W6-6), `src/gfss/sheet_parse.hpp` + `src/gfss/sheet_parse.cpp` (o laço de topo: pula espaço/`cdo`/`cdc`; `at_keyword` → despacho; `}` solto → diagnóstico e segue; senão prelúdio até `{` → `parse_selector_list` sobre o texto do prelúdio; falhou → `rejected` e pula ao `}` que fecha; ok → cursor sobre o miolo (D-W6-11) → `parse_declaration_list` → regra com `source_order`, especificidade por seletor (S-1) e avisos (dead check); **guarda de progresso**: cada volta avança pelo menos um token, senão `internal_defect` e EOF, o mesmo remédio de `token_progress_recovery.hpp`), `tests/gfss_sheet_parse_test.cpp`, `tests/gfss_selector_dead_check_test.cpp`. Muda `src/gfss/diagnostic_vocabulary.hpp` (produtor `sheet_parse`; entradas: `supported_at_rule`, `at_rule_media_is_outside_v1`, `opening_curly_brace_after_selector`, `closing_curly_brace`, `keyframes_name`, `style_rule_or_at_rule` para o `}` solto; produtor `selector_dead_check`: `descendant_or_child_combinator_after_scope`), `src/gfss/CMakeLists.txt`, `tests/CMakeLists.txt`. | Vermelho por construção no primeiro caso (não há leitor de folha); **o vermelho que vale** são as mutações na revisão: (i) contar chaves sem aninhamento reprova `unclosed_brace_inside_a_raw_keyframes_block_never_swallows_the_rules_after_it` e `nested_braces_inside_keyframes_are_part_of_the_raw_body`; (ii) recuperar pulando ao próximo `;` em vez do `}` reprova `a_bad_selector_drops_only_its_own_rule_and_the_next_rule_survives`; (iii) guardar o corpo cru com espaço aparado reprova `raw_keyframes_body_is_byte_identical` (comparação `memcmp` contra a fatia original da folha); (iv) anexar especificidade da regra em vez de por seletor reprova `each_selector_of_a_rule_carries_its_own_specificity` (`#a, .b { }`). | `gfss_sheet_parse_test`: **matriz fechada de recuperação**, uma linha por forma (contagem impressa, zero reprova): regra boa; seletor inválido (descarta só ela); declaração inválida dentro de regra boa (a regra fica, a declaração vai para `rejected`); `@media` com bloco (ignorada, diagnóstico próprio, regras depois sobrevivem); at-rule desconhecida com bloco e sem bloco (`@import url(x);`); `@keyframes nome { 0% { a: 1 } 100% { a: 2 } }` (cru, byte a byte, **sem** diagnóstico, `raw_blocks.size() == 1`); `@keyframes` sem nome; `@keyframes` sem bloco; chave não fechada em regra de estilo no meio da folha (a regra vai para `rejected` com `closing_curly_brace`, **nenhuma** regra depois é lida porque não existe "depois" sem o `}`, e isso é dito no diagnóstico com a linha do `{`); chave não fechada **dentro** do `@keyframes` (idem, dossiê §4.5); `}` solto no topo; `{` dentro de string no prelúdio e no corpo cru (dossiê §4.6); comentário entre regras; `<!--`/`-->`; folha vazia (zero regras, **contado e impresso**, e o teste declara que zero aqui é resultado, não varredura vazia: a varredura é de casos, não de regras); ordem de fonte (`source_order` 0..N-1 sem buraco, com uma rejeitada no meio **não** consumindo índice, decidido aqui: índice é de regra aceita). `gfss_selector_dead_check_test`: enumeração das 4 posições (`:scope +`, `:scope ~`, `a + :scope` limpo, `:scope > a` limpo) × {topo, dentro de `:not()`} = 8, contagem impressa. | mesmo nome |
| 6 | **S-6 fechamento (`CI-VERDE-W6`)** | comum | Muda `docs/node-view-and-matching.md` (já em S-2; aqui só se confere), `TODO.md` pelo orquestrador (as quatro linhas existentes + a nova de §7 para 🔍 no commit de cada fatia; ✅ só depois do servidor), `tests/parity_absences.txt` (só se algum caminho desta tabela não nascer, cada linha com item dono). `check_plan_scope_diff.py` rodado **em container** (L-09: é julgador em Python) contra **cada linha** desta tabela (marcador: o nome em negrito da fatia), contagem impressa. **`README.md` não é tocado** (F11). | (o próprio portão) | `tools/preci.sh` completo verde em container; `gh run view <id> --json conclusion,jobs` com todos os trabalhos verdes (meça o total, não leia daqui) | (o próprio portão) |

**Ordem de trabalho, em lotes de servidor:** push 1 = S-0 + S-1 + S-2 (três commits, um por fatia, revisão adversarial entre os de código). Push 2 = S-3 + S-3b. Push 3 = S-4. Push 4 = S-5 e fechamento. **Na segunda reprovação pelo mesmo motivo, L-42 (busca antes da terceira).**

---

## 5. Critérios de aceitação, fixados ANTES do dado existir (L-43 global)

Por fatia, o que o revisor confere **antes** de olhar o código, e o que reprova:

| Fatia | Critério fixo | Reprova se |
|---|---|---|
| S-1 | 8 tipos de seletor simples enumerados por `static_assert` sobre `gfss_simple_selector_kind`; 4 combinadores enumerados; comparação lexicográfica provada por 6 pares; `:not()` = máximo; saturação na fronteira e um passo além | qualquer contagem impressa diferente da enumeração do enum; pseudo-elemento fora de `types`; atributo fora de `classes` |
| S-2 | matriz 4 × 3 = 12 células, todas presentes (as "não se aplica" contadas); contagem de chamadas prova direita→esquerda; retrocesso provado pelo caso `.a > .b .c`; `:scope` nos dois contextos; `deferred` preservado para os dois casos que não são desta fatia | célula omitida; qualquer `matched` para `:placeholder-shown` ou `::before`; teste que monta o mundo (janela, arquivo) para provar casamento |
| S-3 | 66 células da matriz posição × `!important`; contagem de componentes por família enumerada inteira (não amostra); 5 recusas com `expected` conferido; invariante "zero `is_shorthand` sobrevivente" | expansão fora do índice original; longhand omitido ausente em vez de inicial; validação de valor reimplementada em vez de reusar `check_declaration_value`; `border-radius` compartilhando código com lados |
| S-0 | varredura de fechamento sobre `ESCOPO.md TODO.md`, nomeados (nunca `docs/*.md`, que se contaminaria com a prosa do próprio plano), contada antes (**7**, corrigido na quarta revisão) e depois (**4 exatas**, só o registro da revogação, `ESCOPO.md:536`, `ESCOPO.md:540`, `TODO.md:84`, `TODO.md:414`); `check_dup_laws.py` verde; as quatro decisões D1-D4 gravadas em `ESCOPO.md` numa seção datada, citando D-W6-12/D-W6-7/D-W6-6/D-W6-14 (contagem = 4) | contagem antes diferente de 7 ou depois diferente de 4 (inclusive "4 ou mais", piso frouxo não vale); a linha 453 ou a 411 ou `TODO.md:53` ainda afirmando o texto morto; qualquer edição em `ESCOPO.md:536`, `ESCOPO.md:540`, `TODO.md:84` ou `TODO.md:414`; zero citação de `D-W6-*` na seção nova depois de fechar; varredura de fechamento rodada sobre `docs/*.md` em vez dos dois arquivos nomeados |
| S-3b | 33 células da matriz antes/depois/nenhum; um aviso por longhand anulado; valor do longhand inalterado (a matriz de S-3 continua verde); aviso presente no caminho inline | aviso quando o longhand vem depois; aviso por atalho em vez de por longhand; qualquer mudança de valor |
| S-4 | 22 entradas no vocabulário de pseudo; especificidade zero provada **contra** o `:not()` equivalente; "algum" provado contra "nenhum"; orçamento único de 10 provado nos dois lados da fronteira; **a divergência estrita×tolerante provada nos dois níveis** (seletor e regra inteira) com o identificador que a nomeia; seção `docs/node-view-and-matching.md` presente e não-vazia, provada por `check_where_divergence_documented.py` com os sete controles do autoteste (ausente, corpo vazio, sem identificador, sem `strict`, sem `unlike CSS`, arquivo errado, controle positivo) | lista tolerante; diagnóstico da divergência sem o identificador `known_where_selector`; diagnóstico em frase (R7); comentário de `selector_parse.cpp:93` ainda descrevendo o nosso `:where()` como tolerante; seção de documentação ausente ou sem o identificador/`strict`/`unlike CSS` (portão em zero); qualquer um dos sete controles do autoteste faltando, inclusive o controle positivo |
| S-5 | matriz de recuperação com todas as linhas da tabela §4; corpo cru `memcmp`-idêntico; especificidade por seletor; `source_order` sem buraco; `notices` separada de `rejected`; guarda de progresso com autoteste (folha que faz o laço não avançar produz `internal_defect`, nunca trava) | recursão na busca de chave; regra seguinte perdida depois de chave fechada; `@media` e desconhecida com o mesmo identificador; qualquer diagnóstico cuja `expected` não esteja na lista X-macro |
| todas | teste nunca visto falhando não conta (L-20): o relatório do implementador traz a saída **vermelha** e a **verde**; a revisão adversarial executa as mutações nomeadas na coluna "Teste vermelho" e grava as cinco perguntas da L-17 por unidade nova | relatório só com verde; unidade criada sem as cinco perguntas respondidas |

---

## 6. Onde esta onda tem a forma "passa verde e está quebrado", e o que torna cada uma vermelha

| # | Forma do risco | Como passa verde | O que o torna vermelho |
|---|---|---|---|
| 1 | **Expansão de atalho anexada no fim do bloco** | Toda asserção "os 4 longhands existem" passa | A matriz 66 assevera **índice**, não só presença (S-3) |
| 2 | **`!important` perdido num longhand** | Sem cascata ainda (W10), ninguém lê a flag | A metade `important` da matriz lê a flag em **cada** longhand |
| 3 | **Chave contada sem aninhamento** | Toda folha de teste sem `@keyframes` passa | O caso com quadros aninhados e o caso Firefox (§4.5) |
| 4 | **Corpo cru "limpo"** (espaço aparado, comentário removido) | `ANIM-KEYFRAMES` só chega na W10 | `memcmp` contra a fatia original (S-5) |
| 5 | **Casamento esquerda→direita que dá a resposta certa** | Resultado booleano é igual nos casos simples | Contagem de chamadas à árvore (F10) |
| 6 | **`:scope` que ninguém ancora** | `deferred` continua sendo devolvido e nenhum teste exige `matched` | Os 3 casos de `:scope` em S-2 exigem `matched`/`rejected` |
| 7 | **Aviso de seletor morto na lista de rejeição** | A contagem de diagnósticos bate | `notices.size()` e `rejected.size()` asseverados **separadamente** |
| 8 | **Orçamento de profundidade duplicado** (S-4 com contador próprio) | 10 `:not` continuam reprovando na 11ª | O caso misto 6 + 5 |
| 9 | **`string_view` pendurada** (regra aponta para folha que morreu) | Em teste a folha é literal estático | O header de `sheet_ast.hpp` declara o dono; `GFSS-API` (W10) herda a obrigação de provar com ASan (o job `sanitizer` já roda; a folha de teste da W10 nasce em `std::string` de escopo curto, escrito aqui para não ser esquecido) |
| 10 | **Retrocesso exponencial** (`a a a a a a a` sobre árvore funda) | Nenhuma folha de teste é hostil assim | **Declarado, não resolvido nesta onda**: é o mesmo pior caso de todo motor com casamento direita→esquerda; a mitigação é índice por chave (`GFSS-KEY-INDEX`, candidato à INBOX) ou limite de passos; entra no `AUDITORIAS.md` capítulo de entrada hostil como item aberto |
| 11 | **Diagnóstico em frase** ("expected a declaration") | Lê-se bem no relatório | `diagnostic_detail_is_empty_by_default_and_never_a_sentence` e o teste de vocabulário já reprovam espaço em `expected`; os novos `detail` são conferidos por não conter espaço fora de separador |
| 12 | **Zero regras lidas como "tudo bem"** | Folha vazia devolve sucesso | Cada teste imprime a contagem de **casos**; a de regras é resultado, e o caso "folha vazia" declara isso explicitamente (L-40) |
| 13 | **`:where()` que engole em silêncio** (alguém "corrige" para o comportamento do CSS) | Toda folha copiada do CSS passa a funcionar, e ninguém reclama | `where_is_strict_unlike_css_and_says_so` nos dois níveis (S-4), e o identificador do vocabulário que nomeia a divergência; a decisão datada em D-W6-7/D-W6-12 |
| 14 | **Aviso de reset que muda o valor** (o "conserto" que ninguém pediu) | O aviso aparece e o teste do aviso passa | A matriz de S-3 continua asseverando o valor inicial depois de S-3b; as duas matrizes rodam juntas |
| 15 | **Texto morto consertado num lugar e vivo no gêmeo** (`ESCOPO.md:411`, `TODO.md:53`) | A linha 453 fica certa e quem lê a 411 lê o estado errado | A varredura contada de S-0 sobre a superfície inteira, antes e depois, com o registro da revogação como piso não-vazio |

---

## 7. A linha nova para o `TODO.md` (proposta; o orquestrador escreve)

**ID:** `GFSS-SEL-ZERO-SPECIFICITY`. **Onda:** W6. **Grupo:** GFSS. **Prerrequisitos:** `GFSS-SEL-PARSE-NOT`, `GFSS-SPECIFICITY`, `GFSS-MATCH-COMBINE`. **Dificuldade:** Baixa. **Status inicial:** `⏳ Pendente` (nome e lista decididos pelo líder em 15/09/2026; nada mais pendente dele).

**WSJF proposto: 11,00** (S; CoD 11 = Valor 5 + Criticidade 3 + Redução de Risco 3), derivado pela régua de `docs/lente-produto.md` §1 e calibrado pela âncora `GFSS-SEL-PARSE-NOT` 10,00 (S, o mecanismo irmão): Valor 5 porque o autor da folha ganha a única válvula contra a inflação de `:not()` (dossiê §2.3, medida no caso `#x:not(#y)` = dois ids); Criticidade 3 porque a onda W6 não fecha sem ela (ordem do líder de 15/09) e `GFSS-API` (W10) congela o vocabulário logo depois; Redução de Risco 3 porque troca uma armadilha silenciosa (regra impossível de sobrescrever sem o autor saber) por construção explícita, e o custo de reverter é zero enquanto nenhuma folha externa a usa. Tamanho S: vocabulário a mais nos três mecanismos que S-1, S-2 e o `:not()` já entregam.

**Descrição técnica (proposta de texto):**

> `:where()`, o seletor de especificidade-zero: agrupa uma lista de seletores complexos, casa quando **algum** deles casa, e contribui **`(0,0,0)`** à especificidade, seja qual for o argumento. **Ordem do líder de 15/09/2026:** entra na v1, nesta onda, **com o nome do CSS, `:where()`** (decisão dele, contra a recomendação do CTO e a do orquestrador). Motivo medido (dossiê `dores-comunidade-w6.md` §2.2-2.3): `:not()` conta o sub-seletor mais específico, encadear infla, e a regra do autor fica silenciosamente impossível de sobrescrever; o CSS criou a válvula por isso, e o `gfss` tinha a armadilha sem a válvula. **Lista de argumento ESTRITA, decisão do líder de 15/09/2026: uma inválida recusa a REGRA INTEIRA com diagnóstico. É DIVERGÊNCIA DELIBERADA do CSS, onde `:where()` é tolerante por especificação: mesmo nome, comportamento de erro oposto; quem vem do CSS tropeça uma vez, e o diagnóstico nomeia o que falhou como identificador do vocabulário (`known_where_selector`, R7), com linha e coluna de dentro do argumento; a explicação completa da divergência mora em `docs/node-view-and-matching.md`, cobrada por `check_where_divergence_documented.py`.** Orçamento de aninhamento **compartilhado** com `:not()` (10, um só, porque a pilha é uma só). Prova (L-40): especificidade zero provada **contra** o `:not()` equivalente; "algum casa" provado contra "nenhum casa"; orçamento único provado nos dois lados da fronteira (5+5 passa, 6+5 reprova); a divergência provada nos dois níveis (seletor recusado com o identificador; regra inteira recusada na folha e a seguinte sobrevive), vermelho antes de verde; contagem do vocabulário de pseudo impressa = 22; zero varridos reprova. Plano: `docs/plano-w6-folha-de-estilo.md` §4 linha 4, D-W6-7 e D-W6-12.

---

## 8. O que o líder decidiu em 15/09/2026, e o que ainda é confirmação retroativa

**Nada bloqueia mais nenhuma fatia.** As quatro perguntas que este plano levou a ele voltaram decididas (relato do orquestrador; a forma verbatim, se houver, mora em `ESCOPO.md`, e o orquestrador a registra lá):

| # | Pergunta | Decisão dele | Onde entrou no plano |
|---|---|---|---|
| D1 | O nome do seletor de especificidade-zero | **`:where()`**, o nome do CSS, contra a recomendação do CTO (`:without-specificity()`) e a do orquestrador | D-W6-12; §4 linha 4; §7 |
| D2 | Lista dentro dele: estrita ou tolerante | **Estrita**: seletor inválido recusa a **regra inteira**, com diagnóstico | D-W6-7 (com a divergência deliberada do CSS registrada e datada); prova em S-4, nos dois níveis |
| D3 | `:scope` seguido de irmão | **Aceita, com aviso** em lista separada; não é erro | D-W6-6; átomo `selector_dead_check` em S-5 |
| D4 | Atalho que anula longhand já escrito | **Avisa**; o comportamento não muda, o autor fica sabendo | D-W6-14; S-3b vira fatia normal da onda |

**Confirmações retroativas (não bloqueiam; ele reverte se quiser):**

- **`@keyframes`** como a palavra da at-rule do bloco de animação (F9): é o nome do padrão, decidido pelo CTO em 05/09 no registro; a onda o aplica.
- **`border-radius` com `/`** recusado com diagnóstico (D-W6-9): consequência do registro dar um valor por canto.
- **`:scope` sem escopo = raiz** (D-W6-5): aplicação do padrão.
- **A forma do diagnóstico da divergência do `:where()`** (D-W6-12): identificador do vocabulário, não frase, porque R7 é dele. Se ele quiser a frase dentro do diagnóstico, é R7 que muda, e isso é pergunta separada.
- **S-0 como fatia própria** (D-W6-15): o achado é do orquestrador; a revogação (26/08) é dele; a forma (apagar o texto morto de três lugares, manter o registro em dois) é do CTO.

---

## 9. O que esta onda NÃO faz, declarado

- **Não cascateia.** Especificidade é calculada e anexada; quem compara duas declarações é `GFSS-CASCADE` (W10), inclusive `!important`.
- **Não herda nem resolve** (`GFSS-INHERIT`, W8; `GFSS-RESOLVE`, W9-B).
- **Não parseia o miolo do `@keyframes`** (`ANIM-KEYFRAMES`, W10): guarda cru, byte a byte.
- **Não julga `:placeholder-shown`** (lacuna 1 do contrato de oito fatos, sem decisão de produto) **nem `::before`/`::after`** (`LAYOUT-PSEUDO-BOXES`, W13): os dois continuam `deferred`, e o teste prova que continuam.
- **Não promove nada a `include/glintfx/`** e não exporta símbolo novo: tudo nasce em `src/gfss/` e `src/gfui/`, `detail`, interno (L-19; `GFSS-API` decide).
- **Não indexa seletores por chave** (dossiê §2.6, §5.6): candidato à INBOX, não decidido.
- **Não anuncia recurso** no README (F11).
- **Não decide gravidade de diagnóstico** (09/09: revisão de API de `GFSS-API`).

---

## 10. Leis aplicadas neste plano, e como

- **L-21 global / LEI ZERO:** a frase-guarda está no topo; a regra de paridade ("se o padrão aceita, aceitamos") governa D-W6-4, D-W6-5, D-W6-9; as duas divergências (D-W6-6 aviso, D-W6-7 estrita) estão declaradas, com razão, e vão ao líder.
- **L-28/L-39 do projeto:** o nome do seletor novo é julgado pela régua de legibilidade e vai ao líder com a régua citada; nenhum encurtamento é proposto; os identificadores de diagnóstico nomeiam o vocabulário do autor.
- **L-22 do projeto:** nada cruza a API pública nesta onda; erro continua saindo por diagnóstico em resultado, sem exceção.
- **L-17/L-34 do projeto:** cada arquivo da coluna "Nasce" tem um assunto e um nome que o diz; S-4 **não** cria arquivo só de casos (fragmentação); `border-radius` tem família própria porque a ordem é outra (monolito evitado pelo nome, não pela contagem).
- **L-20/L-35:** cada fatia tem o vermelho em tempo de execução contra a árvore de hoje onde isso é possível, e as mutações que a revisão executa.
- **L-36/L-40:** toda prova é enumeração fechada com contagem impressa; zero reprova; o caso "folha vazia" declara por que zero regras não é varredura vazia.
- **L-43 global:** critérios na §5, fixados antes de existir código.
- **L-04 do projeto:** teste puro, mesmo nome, cinco sistemas; nenhum `#if`.
- **L-24 global / L-11 do projeto:** a definição de fechamento está em §4 linha 6 e §5; CI verde é condição, não critério.
- **L-27 do projeto:** FATO (arquivo:linha, dossiê com fonte) separado de INFERÊNCIA (as decisões D-W6-*) em todo o documento.
- **L-63:** cada commit cita o ID da fatia e toca o `Status` no mesmo commit (implementação → 🔍).
- **L-09:** nenhum teste, portão ou julgador roda na sessão viva; `check_plan_scope_diff.py` e `preci.sh` rodam em container no fechamento. A varredura de S-0 é `grep` de leitura, fora da regra.
- **L-67 global:** canon desatualizado bloqueia o trabalho que dele depende, por isso S-0 vem antes de S-5; o que o líder revoga é apagado, não arquivado, por isso a linha 453 perde o texto morto e o registro da revogação (536-540) fica intacto.
- **L-17 global (gêmeo):** o texto morto foi procurado na superfície inteira antes de a fatia nascer; achou dois gêmeos (`ESCOPO.md:411`, `TODO.md:53`) além da linha apontada.

---

## 11. Decisão 8 do líder (16/09/2026): `GFUI-VERDICT-RESOURCE-EXHAUSTED` reabre o cabeçalho que S-2 acabou de escrever

**Conserto do orquestrador, escrito depois do fechamento do texto acima; não muda nenhuma das onze decisões D-W6-1 a D-W6-11 nem a ordem das fatias já commitadas (S-0, S-1, S-2).** Registro da decisão inteira, com o verbatim relatado, mora em `ESCOPO.md`, seção "Ordem de produto de 15/09/2026", Decisão 8; esta seção só responde à pergunta que o líder deixou aberta ali: onde o trabalho novo entra nesta onda.

**O fato que abre a pergunta:** `match_complex()` (nasceu em S-2, linha 126 da tabela do §4 acima) é `noexcept` **e** aloca (`std::vector<frame>` da pilha explícita que substituiu a recursão); falha de alocação sob `noexcept` chama `std::terminate()` e mata o processo do consumidor. `deferred_simple_match()`, também de S-2, herda o mesmo risco por chamar `match_complex()`. **Isto já está publicado** (S-2 é `d1e677b`/`90330c0`, ambos em `main`), então o risco não é hipotético a caminho, é vivo hoje.

**Por que isto NÃO é um sexto valor barato no enum:** `match_verdict` (D-W6-10, S-2) é consumido por S-4 (`deferred_simple_match.cpp`, linha 128 da tabela) e por S-5 (a lista `rejected` da folha, linha 130); mudar a forma do veredito depois que as duas já leem o valor de três estados custa duas migrações em vez de uma. É também mudança de **forma pública** (`ESCOPO.md`, Decisão 8): "o veredito do casamento hoje tem três valores... todo ponto que consome esse resultado passa a ter de tratar o desfecho novo", e o congelamento formal da forma pública já tem dono e data, `GFSS-API` em W10 (`TODO.md`, linha do item, coluna Onda).

**Proposta de colocação (do redator deste plano; não é decisão tomada, nem pelo líder nem pelo CTO):** entrar como fatia **antes de S-4**, ou seja, entre o push 2 (S-3 + S-3b) e o push 3 (S-4) da ordem de trabalho do §4. Duas razões:

1. **S-4 toca os dois arquivos que carregam o defeito** (`src/gfui/deferred_simple_match.cpp` e, por composição, `match_complex()`): construir o `:where()` em cima do contrato `noexcept`-que-termina de hoje significa tocar os mesmos dois arquivos duas vezes na mesma onda, um risco de conflito que a memória da casa (dois agentes na mesma árvore, §2) já nomeia para os arquivos de CMake e vocabulário.
2. **S-5 lê o resultado do casamento para preencher `rejected`** (D-W6-10): se o quarto estado nascer depois de S-5 estar escrita, o leitor da folha também precisa de um segundo toque.

**O que esta fatia NÃO faz, para não invadir W10:** não decide a forma pública final do veredito de quatro estados (isso é congelamento de `GFSS-API`, revisão de API dedicada, W10); resolve só o defeito interno, `noexcept` sobre função que aloca, com um mecanismo interno (`detail`, ainda sem símbolo exportado, mesma régua que toda fatia desta onda já segue, F11 e §9). Quando `GFSS-API` chegar, ela herda a forma interna já corrigida em vez de herdar o `std::terminate()`.

**O que fica para o CTO decidir, e não é chamado aqui:** o nome exato da fatia (`GFUI-VERDICT-RESOURCE-EXHAUSTED` é o ID proposto em `TODO.md`, não fixado como fatia `S-*` desta onda), se ela nasce dentro da onda W6 ou como onda própria logo em seguida, e o WSJF final (proposto em `TODO.md`, não ratificado).
