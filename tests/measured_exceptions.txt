# SPDX-License-Identifier: AGPL-3.0-or-later
#
# MEASURED key exceptions (achado do time-lead, 06/09/2026, conserto
# da mesma tarde - GODS_LAWS.md L-04/L-40). Este arquivo e' uma
# TERCEIRA coisa, distinta das outras duas irmas desta casa:
#
#   - tests/parity_exceptions.txt: ausencia PERMANENTE OU COM ITEM de
#     um TESTE inteiro do inventario `ctest -N` de um sistema - lido e
#     validado por tests/tools/check_test_parity.py, cujo formato
#     (`nome|plataforma|razao|item`) e' CONTRATO: para item=SEM-
#     PENDENCIA o campo de par tem de vir "nenhum", nunca uma razao em
#     prosa - a tentativa de reusar aquele arquivo para uma exceCao de
#     CHAVE (nao de teste) foi o que reprovou check_test_parity.py no
#     run 34023787584, exatamente por violar essa forma.
#   - tests/parity_aliases.txt: pares de NOMES de teste diferentes
#     provando o mesmo efeito.
#   - ESTE arquivo: uma chave MEASURED especifica ("dono.chave", nunca
#     um nome de ctest) que fica permanentemente ou temporariamente so'
#     de um lado (OU diverge entre os dois por fato de ambiente, nunca
#     por defeito de medicao - achado do CTO, 06/09/2026: ver `ambos`
#     abaixo), mesmo quando o TESTE que a produz ja tem par (mesmo nome
#     ou apelido) em qualquer um dos dois arquivos acima. Lido SO' por
#     tests/tools/check_measured_parity.py, nunca por check_test_
#     parity.py - as duas gates leem fontes diferentes de proposito.
#
# Forma: chave|lado|razao|item
#   chave  - "dono.chave" exata, como aparece numa linha MEASURED real
#            (tests/tools/collect_measured.py's proprio token).
#   lado   - "linux" ou "windows": o lado do qual esta chave e'
#            esperada AUSENTE (mesmo sentido do campo "plataforma" de
#            tests/parity_exceptions.txt, aplicado aqui a uma chave em
#            vez de a um teste inteiro); ou "ambos" quando a chave e'
#            medida NOS DOIS lados e a PROPRIA DIVERGENCIA entre os
#            valores e' o que esta linha declara (fato de ambiente
#            medido, nunca defeito de medicao da biblioteca).
#   razao  - texto livre, nunca vazio, e tem de responder DUAS
#            perguntas (achado do CTO, 06/09/2026 - "sem (b) nao e'
#            sentinela, e' carimbo"): (a) POR QUE o ambiente difere, e
#            (b) QUAL TESTE prova o comportamento da BIBLIOTECA (nao do
#            ambiente) no lugar desta chave. Uma linha que so' responde
#            (a) esconde um defeito real atras da palavra "ambiente".
#   item   - um ID ABERTO em TODO.md, ou SEM-PENDENCIA (ausencia ou
#            divergencia permanente, ex.: um fato de timing ou de
#            hardware de uma so' plataforma/executor que nao tem
#            conceito equivalente na outra em principio).
#
# MESMA REGRA DE MORTE do irmao desta casa: uma chave cujo item ja
# esta CONCLUIDO em TODO.md reprova check_measured_parity.py - o
# trabalho que fecharia a lacuna ja terminou, e esta linha deveria ja
# ter sido apagada. Uma chave permanente continua LISTADA, com
# contagem, nunca escondida (das secoes "herdada" ou "divergentes
# declaradas", conforme o "lado").

# win32_window_close_request_test's own sentinel (src/platform/win32/
# display_adapter.cpp's proprio comentario, corrigido no mesmo commit
# que esta linha): mediu, no run 34020376320 (commit 093a22e), ZERO
# mensagens WM_SIZE chegando durante CreateWindowExW para uma janela
# WS_OVERLAPPEDWINDOW sem WS_VISIBLE - refutando a premissa anterior
# daquele comentario. O teste ja tem par (tests/parity_aliases.txt);
# esta CHAVE especifica e' que fica sem lado Linux possivel em
# principio (fato de timing da bomba de mensagens do Windows). Um
# valor diferente de zero num run futuro e' o que reabriria esta linha.
win32_window_close_request_test.wm_size_during_create|linux|sentinela permanente - ver src/platform/win32/display_adapter.cpp's proprio comentario; nenhum lado Linux e' possivel em principio (fato de timing da bomba de mensagens do Windows)|SEM-PENDENCIA

# two_displays_test existe nos dois lados, mesmo nome (P-0) - a chave
# comum que os dois genuinamente compartilham e' `two_displays_test.
# both_opened` (tests/two_displays_test.cpp e tests/container/two_
# displays_test.cpp's proprios comentarios, "duas precisam de nome
# comum"), que NAO precisa de linha aqui - ela e' medida igual nos dois
# lados. Estas duas chaves especificas (contagem do catalogo de globals
# Wayland) nao tem conceito equivalente no Windows em principio, mesmo
# raciocinio que ja justifica registry_smoke's proprio SEM-PENDENCIA em
# tests/parity_exceptions.txt.
two_displays_test.first_global_count|windows|contagem do catalogo de globals Wayland (global_catalog) - sem conceito equivalente no Windows|SEM-PENDENCIA
two_displays_test.second_global_count|windows|mesmo motivo do first_global_count acima|SEM-PENDENCIA

# seat_test.pointer: presenca de dispositivo e' fato do EXECUTOR, nao da
# biblioteca - o kwin_wayland --virtual do container nao anuncia ponteiro,
# a VM windows-latest tem mouse (sonda do executor: raw_input_mouse_count=1).
# O que a biblioteca promete (traduzir o anuncio do sistema para
# seat_capabilities) e' provado nos dois lados pelo par
# seat_adapter_listener_test|win32_seat_translation_test. Sentinela
# permanente: se um dia os dois lados coincidirem, e' o executor que mudou.
seat_test.pointer|ambos|presenca de dispositivo do executor, nao comportamento da biblioteca|SEM-PENDENCIA

# seat_test.capability_events - ate 06/09/2026 as duas imprimiam sob o
# MESMO nome "last_change" e a tabela comparou um contador de eventos
# Wayland com o codigo do ultimo WM_INPUT_DEVICE_CHANGE - grandezas
# diferentes, nao sistemas divergentes. Renomeadas para dizer o que
# medem; a chave passou a existir DOS DOIS LADOS (WIN-SEAT SF-2, D-WS-4,
# win-seat.md sec. 3, D-090918, 09/09/2026) mas continua NAO comparavel
# por igualdade - Wayland conta wl_seat.capabilities+wl_seat.name no
# bind (2, medido); Win32 conta a leitura inicial sincrona de open() (1)
# mais um por WM_DEVICECHANGE roteado - mesma grandeza (anuncios do
# sistema), valor de mecanismo, nunca igual por construcao. seat_test.
# last_change_kind (Windows) foi APAGADA (nao renomeada, revogada -
# GODS_LAWS.md L-67): media o wParam de WM_INPUT_DEVICE_CHANGE, um
# mecanismo que nao existe mais desde SF-1.
seat_test.capability_events|ambos|Wayland conta capabilities+name no bind (2), Win32 conta a leitura inicial de open() mais um por WM_DEVICECHANGE roteado - mesma grandeza, valor de mecanismo, nunca comparado por igualdade|SEM-PENDENCIA

# gl_context_parity_test (docs/plano-w6b-fatias-5.md, GL-CONTEXT fatia
# 5, D-W6b-29): oito chaves MEDIDAS E IMPRESSAS, nunca asseveradas
# IGUAIS entre os dois lados - cada uma ja carrega, no proprio teste,
# um piso/teto por PLATAFORMA (ex.: vsync_off_60_swaps_ms <= 700ms em
# cada lado, first_presented_attempt >= 1 em cada lado); o que esta
# linha declara e' so' que o NUMERO em si (ou a capacidade relatada)
# nao precisa bater entre Linux e Windows, por (a) o executor e (b) o
# que prova o comportamento da biblioteca em vez do ambiente.
gl_context_parity_test.gl_major|ambos|(a) llvmpipe (Linux, kwin --virtual) e Mesa D3D12/WARP (Windows, X-GL-0) concedem versoes GL diferentes acima do piso 3.3 que este MESMO teste ja assevera igual nos dois lados; (b) a assercao >= 3.3 core, alguns paragrafos acima nesta mesma execucao, prova o contrato da biblioteca|SEM-PENDENCIA
gl_context_parity_test.gl_minor|ambos|mesmo motivo de gl_major acima|SEM-PENDENCIA
gl_context_parity_test.renderer_hash|ambos|(a) GL_RENDERER e' "llvmpipe (LLVM ...)" no Linux e "D3D12 (Microsoft Basic Render Driver)" no Windows (F4 do plano) - strings de driver diferentes por desenho, nunca comparadas por texto (D-W6b-13); (b) gpu.hpp's own gltfx_gpu_info.name e' testado por resolucao (nao-vazio), nunca pelo hash|SEM-PENDENCIA
gl_context_parity_test.vsync_on_60_swaps_ms|ambos|(a) cadencia do compositor virtual (kwin --virtual) x DWM da VM Windows - dois pacers reais completamente diferentes; (b) vsync_off_60_swaps_ms, chave irma nesta mesma execucao, e' o que prova D-W6b-27 (intervalo EGL zero) com piso <=700ms em CADA lado|SEM-PENDENCIA
gl_context_parity_test.vsync_off_60_swaps_ms|ambos|(a) o "quanto" exato depende do executor (CPU/IO do runner), nunca comparado entre Linux/Windows por valor; (b) o piso <=700ms, aplicado EM CADA lado dentro do proprio teste, e' o que prova o comportamento da biblioteca (D-W6b-27) - um bloqueio por quadro custaria ~1000ms em qualquer um dos dois|SEM-PENDENCIA
gl_context_parity_test.vsync_adaptive_support|ambos|(a) D-W6b-18: Wayland recusa `adaptive` por desenho (sem equivalente EGL), Windows depende de WGL_EXT_swap_control_tear existir no driver do executor; (b) a assercao code=unsupported/rejected_value=vsync quando recusado, no proprio teste, prova que a recusa e' honesta, nunca silenciosa|SEM-PENDENCIA
gl_context_parity_test.msaa_support|ambos|(a) capacidade de MSAA e' do driver/executor (llvmpipe x Mesa D3D12/WARP), nao promessa da biblioteca; (b) a varredura non-empty de option_support(), no proprio teste, e' o que prova a fixacao antes do suporte (a reabertura com msaa_samples=4 recusada, mais abaixo na mesma execucao, independe deste valor)|SEM-PENDENCIA
gl_context_parity_test.srgb_support|ambos|mesmo motivo de msaa_support acima, mesma linha da tabela D-W6b-16|SEM-PENDENCIA
gl_context_parity_test.first_presented_attempt|ambos|(a) cadencia do compositor virtual x DWM da VM Windows determina em qual das 5 tentativas o primeiro `presented` chega; (b) o piso >=1, aplicado EM CADA lado dentro do proprio teste, e' o que prova que a apresentacao chega dentro do orcamento, nunca o numero exato da tentativa|SEM-PENDENCIA
gl_context_parity_test.open_us|ambos|"quanto" exato depende do executor (CPU/IO do runner, custo de enumerar dispositivos EGL/DXCore) - nao ha piso cross-plataforma, so o valor cru interessa para acompanhar regressao de desempenho ao longo do tempo|SEM-PENDENCIA
gl_context_parity_test.gpu_kind_raw|ambos|GL-GPU-KIND (docs/plano-w6b-fatias-5b-revisao.md sec. 4.6): a assercao `gpu().kind == software` so entra no commit seguinte ao primeiro run real que imprimir este valor (L-43, dado antes da assercao) - nenhum run assim aconteceu ainda|GL-GPU-KIND-HW-EVIDENCE
gl_context_parity_test.gpu_name_hash|ambos|mesmo motivo de renderer_hash acima (GL_RENDERER nao e' interpretado por texto, D-W6b-13) - o proprio nome muda por driver/versao, nunca comparado por valor entre os dois lados|SEM-PENDENCIA
gl_context_parity_test.gpu_enumeration_index_raw|ambos|depende de gpu_kind_raw resolver via kernel/DXCore ou pelas duas vias (D-W6b-38) - mesmo motivo, mesma pendencia|GL-GPU-KIND-HW-EVIDENCE
gl_context_parity_test.gpu_enumeration_count|ambos|quantidade de GPUs que EGL/DXCore enumeram e' fato do executor (1 em ambos os executores de CI hoje: so a placa por software), nunca promessa da biblioteca - so o piso >=1 e' asseverado, o proprio teste ja faz isso|SEM-PENDENCIA
gl_context_parity_test.gpu_entry_0_kind|ambos|mesmo motivo de gpu_kind_raw acima - depende da classificacao real ainda nao vista em hardware|GL-GPU-KIND-HW-EVIDENCE
gl_context_parity_test.gpu_entry_0_name_hash|ambos|mesmo motivo de gpu_name_hash acima|SEM-PENDENCIA

# LOOP-RUN fatia 7 (docs/plano-w6b-fatias-6-8.md sec. 8.2, D-W6b-50):
# wait_events_smoke (container) e win32_wait_events_test (ctest,
# if(WIN32)) ja tem par declarado em tests/parity_aliases.txt, mas o
# TEXTO "dono.chave" de cada MEASURED line nunca pode coincidir entre
# os dois - os donos (nomes de teste) sao literalmente diferentes por
# necessidade (um e' binario de container, o outro e' caso de ctest).
# Cada chave abaixo e' "obrigatoria" por dono ate esta linha existir
# (check_measured_parity.py's own classify_unilateral()); a razao (b)
# de cada linha aponta para o criterio JA asseverado dentro do proprio
# teste do lado que falta, nunca para um valor cru comparado por
# igualdade entre sistemas.
wait_events_smoke.no_event_ms|windows|(a) o dono ("wait_events_smoke") so existe do lado Linux - win32_wait_events_test e' o dono equivalente no Windows, nome diferente por construcao; (b) win32_wait_events_test.no_event_ms, mesma execucao do lado Windows, aplica o MESMO criterio (40..500ms, §6.1) contra MsgWaitForMultipleObjectsEx()|SEM-PENDENCIA
win32_wait_events_test.no_event_ms|linux|(a) o dono ("win32_wait_events_test") so existe do lado Windows - wait_events_smoke e' o dono equivalente no container Linux; (b) wait_events_smoke.no_event_ms, mesma execucao do lado Linux, aplica o MESMO criterio (40..500ms, §6.1) contra poll(fd, budget_ms) sobre o socket real|SEM-PENDENCIA
wait_events_smoke.event_ms|windows|mesmo motivo de wait_events_smoke.no_event_ms acima; win32_wait_events_test.event_ms aplica o mesmo criterio (<500ms, §6.1) do lado Windows|SEM-PENDENCIA
win32_wait_events_test.event_ms|linux|mesmo motivo de win32_wait_events_test.no_event_ms acima; wait_events_smoke.event_ms aplica o mesmo criterio (<500ms, §6.1) do lado Linux|SEM-PENDENCIA
wait_events_smoke.stray_event|windows|mesmo motivo de wait_events_smoke.no_event_ms acima; e' diagnostico impresso sempre (§6.2 iii), nunca criterio que reprova sozinho - win32_wait_events_test.stray_event e' o sinal irmao do lado Windows|SEM-PENDENCIA
win32_wait_events_test.stray_event|linux|mesmo motivo de win32_wait_events_test.no_event_ms acima; e' diagnostico impresso sempre (§6.2 iii), nunca criterio que reprova sozinho - wait_events_smoke.stray_event e' o sinal irmao do lado Linux|SEM-PENDENCIA
win32_wait_events_test.high_resolution_wait|linux|D-W6b-50: CREATE_WAITABLE_TIMER_HIGH_RESOLUTION e' um mecanismo exclusivamente Win32 (timer de alta resolucao do sistema, Windows 10 1803+) - o lado Wayland nao tem timer de sistema equivalente para wait_events() (poll(fd, budget_ms) direto sobre o socket real ja e' preciso o bastante, sem grao de agendador a declarar); wait_events_smoke.no_event_ms/event_ms provam o comportamento equivalente do lado Linux, sem esta chave|SEM-PENDENCIA

# LOOP-RUN, INSTRUMENTAR O TETO, PASSO 1 (15/09/2026, plano espera-
# win32.md sec. 5.2 item 3): o par novo, mesma forma dos quatro pares
# wait_events_smoke/win32_wait_events_test acima - donos diferentes por
# necessidade (um e' binario de container, o outro e' caso de ctest),
# mesmo criterio (900..1500 ms) asseverado dentro de CADA teste contra
# o MESMO par display real + frame_cap_schedule real + clock real,
# chamando wait_for_frame_cap() (platform/loop/loop_engine.hpp) DIRETO,
# sem janela nem contexto grafico - a prova do MOTOR isolada da fiacao
# que loop_parity_test.cpp's own cap30_opcao_lida cobre do outro lado
# (H2-A x H2-B, plano sec. 3).
win32_wait_events_test.cap30_motor_wall_ms|linux|(a) o dono ("win32_wait_events_test") so existe do lado Windows - wait_events_smoke e' o dono equivalente no container Linux; (b) wait_events_smoke.cap30_motor_wall_ms, mesma execucao do lado Linux, aplica o MESMO criterio (900..1500ms) sobre o MESMO motor (wait_for_frame_cap), sem janela nem contexto grafico envolvido|SEM-PENDENCIA
wait_events_smoke.cap30_motor_wall_ms|windows|(a) o dono ("wait_events_smoke") so existe do lado Linux - win32_wait_events_test e' o dono equivalente no Windows, nome diferente por construcao; (b) win32_wait_events_test.cap30_motor_wall_ms, mesma execucao do lado Windows, aplica o MESMO criterio (900..1500ms) sobre o MESMO motor (wait_for_frame_cap), sem janela nem contexto grafico envolvido|SEM-PENDENCIA

# LOOP-RUN fatia 8, sub-fatia S4 (loop-fix.md sec. S4.5): as chaves que
# os dois testes ao vivo do laco (loop_parity_test, loop_hidden_test)
# imprimem SEMPRE e que NUNCA sao asseveradas iguais entre os dois
# sistemas. Cada razao abaixo responde as duas perguntas do cabecalho
# deste arquivo: (a) por que o ambiente difere e (b) qual assercao do
# PROPRIO teste, na MESMA execucao, prova o comportamento da biblioteca
# no lugar do valor cru. Os dois testes tem par por NOME (loop_parity_
# test pela fixture do container, loop_hidden_test pelo par de arquivos
# de mesmo nome) - nenhuma linha em tests/parity_exceptions.txt nem em
# tests/parity_aliases.txt para eles.
loop_parity_test.mean_step_ns|ambos|(a) cadencia do compositor virtual (kwin --virtual) contra o DWM do executor Windows - dois pacers reais completamente diferentes, e o proprio plano (sec. S4.3 item 8) fixa que cadencia real e' medida e impressa, nunca prometida em numero; (b) a assercao elapsed <= 250 ms (k_gltfx_max_frame_elapsed, lida da constante) nos 30 tiques da mesma execucao e' o que prova o contrato de P5|SEM-PENDENCIA
loop_parity_test.max_step_ns|ambos|mesmo motivo de mean_step_ns acima - e' o pior caso da mesma medicao de cadencia; (b) a mesma assercao elapsed <= 250 ms cobre o contrato, e nenhum piso cross-plataforma existe para o pior caso de um agendador|SEM-PENDENCIA
loop_parity_test.rendered_frames|ambos|(a) quantos dos 30 tiques cada executor de fato apresentou depende do compositor virtual contra o DWM, e do renderizador por software de cada lado; (b) o veto de trivialidade >= 1, aplicado EM CADA lado dentro do proprio teste, e' o que prova que o laco desenha - um laco que nunca desenha reprova ali mesmo|SEM-PENDENCIA
loop_parity_test.cap30_wall_ms|ambos|(a) o executor divide em DOIS fatos, e o segundo foi medido em 13/09/2026, nao suposto: o grao do temporizador difere (o executor Windows sem timer de alta resolucao carrega 15,6 ms por espera, o container Linux usa poll() direto sobre o socket); e o kwin_wayland --virtual deste container NUNCA entrega o retorno de quadro do EGL - `gl_context_parity_test.vsync_on_60_swaps_ms` sai perto de 5825 para 60 trocas (~97 ms cada, o orcamento de 100 ms batendo inteiro), entao `src/platform/wayland/egl_context_adapter.cpp:751` (`pending_older_than(100 ms)`) classifica como oculta uma janela que `window_active_after_map_smoke` prova mapeada e ativa; com isso cada tique do trecho do teto paga a espera de janela oculta (100 ms) EM VEZ da espera do teto, e o valor medido foi 2975 ms contra a faixa 900..1500. (b) quem prova o teto de quadros no lugar deste valor e o teste SEM sistema operacional `frame_cap_schedule_test`, cujas seis celulas ja mordem por mutacao real (a de 'sem deriva' mata o mutante `deadline = now + period`); o proprio `loop_parity_test` imprime `cap30_wall_ms` SEMPRE e so deixa de asseverar a faixa quando o diagnostico irmao `cap30_tiques_sem_desenho` prova que o executor nao deixou o laco desenhar em nenhum dos 30 tiques - num executor que mostra a janela a faixa volta a reprovar|SONDA-OCULTA-PUNE-JANELA-VISIVEL
loop_parity_test.cap30_step_ns_min|ambos|(a) LOOP-RUN, instrumentar o teto (plano espera-win32.md sec. 5.2 item 2, 15/09/2026): a parede de CADA step() no trecho do teto depende do agendador do executor (compositor virtual x DWM) e do custo de cada despertar, exatamente como mean_step_ns/max_step_ns acima; (b) nenhuma assercao cross-plataforma depende deste valor cru - ele e' o instrumento que separa H2-A (fiacao) de H2-B (motor) quando lido ao lado de cap30_opcao_lida e cap30_wall_ms na MESMA execucao (se o teto vive, o minimo de 29 dos 30 tiques e' >= ~20 ms; microssegundos dizem que o motor nao chegou a esperar)|SEM-PENDENCIA
loop_parity_test.cap30_step_ns_mean|ambos|mesmo motivo de cap30_step_ns_min acima|SEM-PENDENCIA
loop_parity_test.cap30_step_ns_max|ambos|mesmo motivo de cap30_step_ns_min acima - e' o pior caso da mesma medicao por tique|SEM-PENDENCIA
loop_parity_test.swap_failures_tolerated|ambos|(a) o executor Windows nao tem placa de video e seu renderizador por software as vezes recusa apresentar um quadro sem o sistema relatar causa nenhuma (os_error_code=0), enquanto o llvmpipe do container nunca produziu essa falha; (b) a assercao "nenhuma falha fora dos tres fatores", na mesma execucao, e' o que prova que toda recusa foi classificada, nunca engolida - e um valor > 0 no Linux e' achado, nao ruido|SEM-PENDENCIA
loop_hidden_test.hidden_detected|ambos|(a) se o compositor estrangula ou nao o desenho de uma toplevel minimizada e decisao DELE (kwin --virtual contra o DWM), nunca da biblioteca; MEDIDO em 13/09/2026 no container, ANTES do conserto do instrumento de I5: saia 1 nos DOIS modos de sincronia, entao a hipotese de que o compositor virtual nao reportaria nada foi REFUTADA por execucao naquele momento (os dois sistemas pareciam chegar ao mesmo 1 por caminhos diferentes - bit `suspended` do xdg-shell de um lado, WM_SIZE/SIZE_MINIMIZED do outro). DEPOIS do conserto do instrumento (achado I5, `c97975b`), MEDIDO de novo pela reauditoria em 15/09/2026 (`h3_loop_hidden.log`): a chave que esta linha nomeia e' a AGREGADA (`hidden_detected_all` no codigo, impressa como `loop_hidden_test.hidden_detected`) e sai **0**, um valor so, nao um por modo. O diagnostico bruto por modo (`results[i].hidden_detected`) continua saindo **1 nos dois modos**, igual a antes do conserto (`rendered_while_hidden=0 (hidden_detected=1)` nos dois, medido de novo). O que o conserto do instrumento passou a medir, e que agora entra na agregada, e' a limpeza da fase visivel (`fase_visivel_limpa`): sai **0** no modo com sincronismo LIGADO (`vsync=on` - a sonda ja classifica a janela como oculta antes do minimizar acontecer, o mesmo defeito do item `SONDA-OCULTA-PUNE-JANELA-VISIVEL`) e **1** no modo com sincronismo DESLIGADO (`vsync=off` - a fase visivel fica limpa de fato). E' essa diferenca por modo NA FASE VISIVEL, nao na chave `hidden_detected` em si, que faz a agregada sair 0 hoje. A linha fica pelo valor medido HOJE, nunca pela leitura antiga - o registro de que a hipotese original foi refutada por execucao continua valendo como fato historico daquele momento, mas nao descreve mais o valor atual da chave; (b) a assercao rendered_while_hidden == 0 quando hidden_detected == 1, mais a assercao incondicional de suspended_while_minimized na mesma execucao, sao o que provam a fiacao da sonda - um ZERO aqui deixaria a assercao de desenho sem morder, e por isso a chave e' impressa SEMPRE, mesmo valendo zero|SONDA-OCULTA-PUNE-JANELA-VISIVEL
loop_hidden_test.suspended_while_minimized|ambos|(a) o caminho pelo qual o bit chega difere por sistema: configure do xdg-shell (>= 6) no Wayland, WM_SIZE/SIZE_MINIMIZED no Win32 - e um compositor abaixo da versao 6 nao tem o estado para enviar; (b) a assercao do proprio teste e' escrita contra xdg_wm_base_version no Linux e incondicional no Windows, e e' ela, nao o valor cru comparado, que prova a leitura|SEM-PENDENCIA
loop_hidden_test.xdg_wm_base_version|windows|(a) xdg_wm_base e' um global do protocolo Wayland - nao ha conceito equivalente no Win32, onde o estado de minimizado chega por mensagem de janela e nao por versao negociada de protocolo; (b) do lado Windows a assercao suspended_while_minimized e' INCONDICIONAL (nao depende de versao nenhuma), e e' ela que prova o mesmo efeito sem esta chave|SEM-PENDENCIA
loop_hidden_test.rendered_while_hidden_vsync_off|ambos|(a) depende de o executor ter reportado a ocultacao (hidden_detected) e de quantos tiques a sonda levou para perceber - fato do compositor, nunca promessa nossa; (b) a assercao "== 0 quando hidden_detected == 1", na mesma execucao dos dois lados, e' o que prova P8|SEM-PENDENCIA
loop_hidden_test.restored|ambos|(a) o xdg-shell NAO tem requisicao unset_minimized: restaurar e' decisao do compositor, enquanto ShowWindow(SW_RESTORE) no Win32 sempre restaura; (b) a assercao de recuperacao em <= 10 tiques, condicionada a esta chave no Linux e incondicional no Windows, e' o que prova P4 nos dois lados|SEM-PENDENCIA
loop_hidden_test.cpu_ratio_permille|ambos|(a) a razao exata depende do agendador do executor e do custo de cada despertar; (b) a borda <= 250 por mil, aplicada EM CADA lado dentro do proprio teste, e' o que prova P8 - um laco que gira em vez de dormir mede perto de 1000 em qualquer executor|SEM-PENDENCIA
loop_hidden_test.swap_interval_honored|ambos|(a) P1, excecao declarada (2): o driver EGL pode ignorar eglSwapInterval (EGL 1.5 sec. 3.10.3) e WGL_EXT_swap_control pode faltar - inferido da cadencia de cinco apresentacoes com vsync desligado, NUNCA lido do driver; (b) nenhuma assercao depende desta chave: quem prova o comportamento da biblioteca e' gl_context_parity_test.vsync_off_60_swaps_ms, com piso <= 700 ms em cada lado|SEM-PENDENCIA
