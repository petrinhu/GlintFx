# SPDX-License-Identifier: AGPL-3.0-or-later
#
# PARITY-GATE exceptions file (TODO.md item PARITY-GATE, GODS_LAWS.md
# L-04: "o comportamento deve ser igual em qualquer OS ... e cada
# fatia entregue prova isso em cada sistema"). Consumido por
# tests/tools/check_test_parity.py.
#
# Uma linha aqui declara que um nome de teste existe no inventario de
# UM sistema e nao existe no do outro, POR QUE ISSO E ACEITAVEL AGORA -
# nunca uma forma de calar o portao. Formato, um registro por linha,
# quatro ou cinco campos separados por "|" (comentarios com "#",
# linhas em branco ignoradas):
#
#   nome_do_teste | sistema_onde_falta | gemeo | item
#   nome_do_teste | sistema_onde_falta | gemeo | item | PROVA-PARCIAL=<nome_do_teste_gemeo>
#
# - nome_do_teste: nome exato do caso ctest que falta de um lado.
# - sistema_onde_falta: "linux" ou "windows" para o modo --compare (o
#   LADO, uniao de todas as distros, que nao tem o caso); a partir de
#   CI-SPLIT-PER-OS A1 (docs/plano-ci-split-per-os.md secao 4.3),
#   tambem aceita o SLUG de uma distro especifica ("fedora", "ubuntu",
#   "arch", "cachyos") para o modo --per-system, quando a lacuna e' de
#   UMA distro so' e nao do sistema operacional inteiro - "windows"
#   serve aos dois modos, e' o mesmo slug nos dois. tests/tools/
#   check_test_parity.py's own SISTEMAS_VALIDOS e' a lista fechada.
# - gemeo: nome do mecanismo que cobre o mesmo comportamento no lado
#   que falta, ou a palavra "nenhum" quando genuinamente nao ha
#   nenhum (o caso e proprio de UM sistema por natureza, ex.: uma
#   sonda do ambiente Win32 do proprio runner). "nenhum" so e honesto
#   quando a ausencia e permanente por desenho - nunca use "nenhum"
#   para uma lacuna real ainda sem par. Este campo aceita PROSA LIVRE
#   (nome de arquivo-fonte, explicacao) - nunca e conferido por
#   maquina, so o quinto campo abaixo e.
# - item: o ID do item em TODO.md que rastreia o fechamento desta
#   lacuna, OU o sentinela SEM-PENDENCIA quando a assimetria e
#   permanente por desenho e nenhum item precisa rastrea-la (exige
#   gemeo=nenhum - GODS_LAWS.md L-32 proibe criar item novo em
#   TODO.md so para isto).
# - PROVA-PARCIAL=<nome_do_teste_gemeo> (quinto campo, opcional):
#   diferente do campo "gemeo" acima (prosa livre, nunca conferida),
#   este quinto campo cita um nome de ctest DE VERDADE, que tem de
#   EXISTIR no inventario do sistema declarado em sistema_onde_falta -
#   tests/tools/check_test_parity.py confere isso por maquina
#   (validate_prova_parcial()) e reprova se o nome nao existir la.
#   Use quando o gemeo existe como ctest registrado mas so prova uma
#   PARTE do comportamento (ex.: compila limpo, nao executa a mutacao
#   real) - nunca para declarar um gemeo que e' so um arquivo-fonte ou
#   uma intencao futura (PARITY-ALIAS-HYGIENE, TODO.md W7-C, D10,
#   23/09/2026: "o gemeo tem de existir no inventario do outro lado,
#   conferido por maquina, coisa que a forma atual nunca confere").
#
# A REGRA que este arquivo existe para provar (verbatim do CTO,
# TODO.md): "exececao que aponta para item marcado como concluido
# REPROVA, porque e exatamente a regra 'concluido sem par'". Isto
# significa, em pratica: quando o item que uma linha aqui cita fecha
# (vira Concluido em TODO.md), a linha correspondente tem que ser
# APAGADA deste arquivo NO MESMO commit que fecha o item - nunca
# deixada para tras. Se ela ficar, o portao reprova, porque uma
# excecao esquecida apontando para um item concluido e exatamente o
# sintoma que motivou esta fatia inteira (retrabalho de 02/09/2026:
# tres ondas fechadas com entrega existindo em um sistema so).
#
# O QUE NAO ENTRA AQUI: quando o par existe nos dois sistemas mas com
# NOME DIFERENTE (mecanismo diferente, cobertura igual - ex.:
# display_connect_failure_test no Linux e win32_display_connect_test
# no Windows, ambos ja registrados como ctest, WIN-DISPLAY concluido
# em 03/09/2026), a linha certa e um APELIDO em
# tests/parity_aliases.txt, nunca uma excecao aqui - uma excecao
# aponta para uma AUSENCIA aceita, um apelido aponta para uma
# PRESENCA sob outro nome. Ver o cabecalho daquele arquivo.

# --- Sondas exclusivas de UM sistema, sem par possivel -----------------

# win32_runner_probe_test (tests/win32_runner_probe_test.cpp,
# tests/CMakeLists.txt:259) prova que O PROPRIO runner Windows entrega
# uma window station interativa real (CreateWindowExW/PeekMessageW) -
# e uma sonda do AMBIENTE de CI do lado Windows, nao um comportamento
# da biblioteca. Nao ha "runner Linux equivalente" para sondar do
# mesmo jeito - a garantia analoga do lado Linux (compositor Wayland
# real dentro do container) e provada por outro mecanismo inteiro
# (tests/container/check_isolation.sh, TEST-WLCONT), ja fora do
# universo de nomes ctest que este portao compara.
win32_runner_probe_test|linux|nenhum|SEM-PENDENCIA

# win32_wgl_proc_address_test (tests/win32_wgl_proc_address_test.cpp,
# docs/plano-w6b-placa-e-laco.md fatia 4, X-WGL) prova a armadilha
# ESPECIFICA do WGL - wglGetProcAddress nao resolve funcao GL 1.1
# (glGetString e ~40 outras), e o resolvedor deste projeto (wgl_proc_
# address.hpp) cai para GetProcAddress(opengl32) nesse caso. O lado
# Linux nao tem par possivel: eglGetProcAddress resolve TUDO, sem essa
# categoria de funcao com endereco fixo separada - a mesma distincao
# que a linha de apelido de win32_wgl_extension_loader_test, logo
# abaixo, ja documenta para as duas funcoes ARB.
win32_wgl_proc_address_test|linux|nenhum|SEM-PENDENCIA

# egl_probe_smoke (tests/container/egl_probe_smoke.cpp, docs/plano-
# w6b-placa-e-laco.md fatia 1, G-1) prova que O PROPRIO container
# Linux (o compositor `kwin_wayland --virtual` mais o llvmpipe por
# baixo) entrega EGL sobre Wayland e um contexto OpenGL 3.3 core - e
# uma sonda do AMBIENTE de CI do lado Linux, mesma categoria de
# win32_runner_probe_test acima (a analoga do lado Windows, X-GL-0). Nao
# ha "runner Windows equivalente" para sondar do mesmo jeito - a
# garantia analoga do lado Windows ja e provada por win32_runner_probe_
# test's own "windows_runner_reports_gl_context_creation_capability"
# case, ja excetuada acima.
egl_probe_smoke|windows|nenhum|SEM-PENDENCIA

# egl_protocol_error_smoke (tests/container/egl_protocol_error_smoke.
# cpp, docs/plano-w6b-fatias-5.md, GL-CONTEXT fatia 5, D-W6b-28) prova
# que um erro de protocolo REAL sobre este MESMO compositor nomeia a
# interface Wayland que reprovou (build_connection_failure(), connection_
# failure.cpp). O Windows nao tem protocolo de fio com erro nomeado por
# interface - "swap sobre janela morta devolve erro" no Win32 e'
# platform_failure de SwapBuffers sobre HWND destruido, ja fora do
# universo de nomes (interfaces Wayland) que este portao compara.
egl_protocol_error_smoke|windows|nenhum|SEM-PENDENCIA

# egl_error_read_inside_swap_smoke (tests/container/egl_error_read_
# inside_swap_smoke.cpp, EGL-DEAD-DISPLAY-GUARD S0, /var/tmp/glintfx-
# plan/plano-segv-swap-protocol-error.md) prova que o ramo vsync=off de
# swap_buffers() nunca responde `presented` sobre uma conexao Wayland
# ja morta - defeito ESPECIFICO do backend llvmpipe/swrast do Mesa
# (dri2_wl_swrast_swap_buffers_with_damage devolvendo EGL_TRUE
# incondicionalmente, plano sec. 1.1). O Windows nao tem fio com
# compositor nem esse backend de software especifico do Mesa - mesma
# categoria de ausencia que egl_protocol_error_smoke, acima, ja declara
# para o universo de erro de protocolo Wayland.
egl_error_read_inside_swap_smoke|windows|nenhum|SEM-PENDENCIA

# surface_size_policy_adapter_test (tests/container/surface_size_
# policy_adapter_test.cpp, TODO.md SURFACE-SIZE-POLICY-ADAPTER-GAP,
# docs/plano-w6b-placa-e-laco.md D-W6b-5) prova que resize_surface_
# if_due() (egl_context_adapter.cpp) reage a um resize REAL (fullscreen)
# chamando wl_egl_window_resize() de verdade - a linha D-W6b-5 da
# propria decisao registrada ja diz por que nao ha par: "No Win32 nao
# ha o que fazer: o DC segue o HWND". wgl_context_adapter.cpp nao tem
# NENHUMA chamada de resize (medido: `grep -n resize src/platform/
# win32/wgl_context_adapter.cpp` devolve vazio) - o mecanismo Windows
# resolve isso sozinho, sem nenhum ponto de codigo equivalente a testar.
surface_size_policy_adapter_test|windows|nenhum|SEM-PENDENCIA

# wl_proto_link_test (tests/CMakeLists.txt:427) prova que o binding
# do protocolo Wayland xdg-shell, gerado via wayland-scanner contra o
# XML do SISTEMA, linka e resolve o simbolo esperado - GODS_LAWS.md
# L-05 fecha a porta Wayland/X11 no Windows por desenho: nao existe,
# e nunca vai existir, um "protocolo Wayland" do lado Windows para
# este teste ter par.
wl_proto_link_test|windows|nenhum|SEM-PENDENCIA

# display_bind_refusal_test (docs/plano-w6a-janela.md fatia 3, W-A,
# 05/09/2026; tests/CMakeLists.txt, if(UNIX)) prova a recusa de
# wayland_display_adapter::bind() - liga-se a um global ANUNCIADO PELO
# REGISTRY, o mecanismo de descoberta de servico do proprio protocolo
# Wayland. O Windows nao tem "registry"/anuncio de globais: mesma porta
# que L-05 fecha por desenho, mesma razao de wl_proto_link_test acima -
# nao ha, e nunca vai haver, um par de NOME para este teste no lado
# Windows. O COMPORTAMENTO que bind() serve (ligar compositor, shell,
# seat) tem par por OUTRO mecanismo: window_parity_test (fatia 11)
# exige sucesso nos dois lados, cada um usando o proprio jeito de obter
# o servico que precisa (bind() aqui, a classe de janela ja registrada
# no Win32).
display_bind_refusal_test|windows|nenhum|SEM-PENDENCIA

# pkgconfig_test (tests/CMakeLists.txt:1404) prova o glintfx.pc
# instalado via um `pkg-config --cflags --libs` cru. pkg-config e uma
# convencao de empacotamento Unix/Linux; check_pkgconfig.sh e' POSIX
# sh e assume nomes de artefato Unix (libglintfx.so*/libglintfx.a),
# nunca .dll/.lib - portar de verdade exige reescrever essas
# suposicoes, nao so' tirar uma guarda de sistema.
#
# RAZAO CORRIGIDA em 06/09/2026 (docs/auditoria-decisoes-autonomas.md
# F3, achado do lider): a razao que morava aqui - "glintfx nao tem
# camada de plataforma que precise de Libs.private la ainda" - ficou
# FALSA em 03/09/2026, quando a camada win32 nasceu (src/platform/
# win32/CMakeLists.txt link a PRIVATE user32/shell32/ole32/propsys/
# opengl32/gdi32). cmake/GlintfxInstall.cmake's own
# glintfx_compute_pkgconfig_libs_private() ja foi corrigida na MESMA
# fatia que corrige este comentario - o Libs.private do glintfx.pc
# agora carrega esses seis nomes no Windows tambem. O que CONTINUA
# faltando, honestamente: nenhum portao RODA pkg-config de verdade
# contra o arquivo instalado no Windows (so' a EXISTENCIA do arquivo e'
# provada la, por tools/ci/check-pkgconfig-installed.ps1 - ver "achado
# a parte, ainda sem mecanismo" mais abaixo neste arquivo). pkgconf
# existe empacotado para Windows (vcpkg, Chocolatey - consultado
# 06/09/2026) e o proprio Strawberry Perl do executor do servidor ja
# tem um script de pkg-config (achado da fatia PKG-WIN-SCOPE); portar
# check_pkgconfig.sh para reconhecer os dois formatos de artefato e
# rodar nos dois sistemas e' a fatia que fecha isto de vez - registrada
# na TABELA UNIFICADA do TODO.md como item proprio, nao feita as pressas
# aqui sem uma maquina Windows real para verificar contra.
pkgconfig_test|windows|nenhum|SEM-PENDENCIA

# --- Achado do time-lead, 07/09/2026 (PARIDADE-3-BURACOS) -------------
#
# bounded_output_wait_test (tests/bounded_output_wait_test.cpp,
# tests/CMakeLists.txt, if(UNIX)) prova wait_for_writable_until()
# (src/platform/wayland/bounded_output_wait.hpp) - o atomo que impede
# um write() num socket AF_UNIX (a conexao wl_display do protocolo
# Wayland) de bloquear o processo indefinidamente quando o par nunca
# le e o buffer de envio do kernel enche. O proprio header do atomo
# explica a razao de ser: display_adapter.cpp::flush_with_retry() e
# egl_context_adapter.cpp::poll_and_dispatch_with_budget() carregavam
# cada um a sua copia de "poll() bounded para POLLOUT", e uma delas
# (o defeito real que esta fatia INBOX consertou) sobrevivia com
# espera INFINITA - risco que so existe porque o mecanismo de fio do
# Wayland e' um socket com buffer de envio que PODE encher.
#
# O Windows nao tem par possivel, e nao por falta de escrever: o
# pump_events() do lado Windows (src/platform/win32/display_
# adapter.cpp:256-273, win32_display_adapter::pump_events()) drena a
# fila de mensagens via PeekMessageW(..., PM_REMOVE) em loop ate
# devolver zero - CHAMADA QUE, PELO CONTRATO DA PROPRIA API DO
# WINDOWS, NUNCA BLOQUEIA esperando espaco de buffer (a documentacao
# da Microsoft para PeekMessage e' clara: ela so' testa a fila e
# retorna, nunca espera por escrita) - o comentario do proprio metodo
# ja nomeia isso ("never blocks, never leaves anything behind for the
# next frame"). Toda a comunicacao de janela do Win32 e' por fila de
# mensagens do sistema (SendMessage/DispatchMessage), nunca por
# escrita bloqueante num socket ou pipe - varredura de src/platform/
# win32/ inteiro (`grep -rn "::send(\|WriteFile\|::write("
# src/platform/win32/`) confirma ZERO ocorrencias de qualquer
# primitivo de escrita bloqueante naquela camada. Nao ha, portanto,
# NENHUM ponto no lado Windows sujeito ao MESMO risco (buffer de saida
# cheio travando o processo) que bounded_output_wait_test existe para
# mitigar - a ausencia e por desenho da propria API do sistema
# operacional, mesma categoria das sondas de fronteira Wayland/X11 ja
# registradas acima neste arquivo (wl_proto_link_test, display_bind_
# refusal_test - GODS_LAWS.md L-05 fecha essa porta por desenho). A
# PROVA que cobre a biblioteca no lugar do atomo que nao precisa
# existir e' o proprio corpo (nao um teste separado, por instrucao
# explicita de nao escrever teste novo nesta fatia) de win32_display_
# adapter::pump_events() - PeekMessageW com PM_REMOVE e' inerentemente
# nao-bloqueante por contrato de API, entao nao ha "atomo equivalente"
# a escrever, nem risco a mitigar.
bounded_output_wait_test|windows|nenhum|SEM-PENDENCIA

# flush_retry_policy_test (tests/flush_retry_policy_test.cpp, tests/
# CMakeLists.txt, if(UNIX)) prova flush_write_wait_is_fatal() (src/
# platform/wayland/flush_retry_policy.hpp) - o atomo puro que decide,
# a partir de um bounded_wait_outcome, se um write-wait que NAO
# resolveu `ready` derruba a conexao (LOOP-RUN fatia 7, conserto pos-
# revisao D-W6b-57, GODS_LAWS.md L-17/L-20). O parametro de entrada
# deste atomo, bounded_wait_outcome (src/platform/wayland/bounded_
# output_wait.hpp), e' o MESMO tipo que a excecao logo acima
# (bounded_output_wait_test|windows|nenhum|SEM-PENDENCIA) ja registra
# como sem par possivel no Windows - o motivo e' o mesmo, um nivel
# corrente abaixo: pump_events() do lado Windows nunca produz um
# bounded_wait_outcome porque PeekMessageW(..., PM_REMOVE) nunca
# espera por espaco de buffer de escrita (contrato da propria API,
# ja verificado e citado na entrada acima). Sem o valor de entrada,
# nao ha "decisao sobre esse valor" a testar do lado Windows - a
# ausencia deste atomo e', portanto, consequencia direta e permanente
# da ausencia ja registrada do atomo que ele consome, nao um caso
# novo a decidir. Achado do time-lead, 08/09/2026 (servidor run
# 34192811273, PARIDADE-9-BURACOS): oito das nove linhas do relatorio
# eram cascata da imagem do container falhando (inventario Linux
# incompleto), fecharam sozinhas quando tests/container/Containerfile
# ganhou o atomo que faltava no link; esta e' a unica real.
flush_retry_policy_test|windows|nenhum|SEM-PENDENCIA

# incoming_poll_outcome_test (tests/incoming_poll_outcome_test.cpp,
# tests/CMakeLists.txt, if(UNIX)) prova classify_incoming_poll() (src/
# platform/wayland/incoming_poll_outcome.hpp) - o atomo puro que decide,
# a partir do retorno bruto de um `::poll()` POSIX (int poll_result,
# short revents), o que fazer no lado LEITURA da conexao (CONT-WARMUP
# C-2, docs/plano-fecho-w7b.md D-10, GODS_LAWS.md L-17/L-20) - o gemeo
# de leitura do atomo de escrita ja registrado nas duas entradas acima.
# MESMA ausencia, MESMO motivo, um nivel abaixo do lado leitura em vez
# do lado escrita: pump_events() do Windows (src/platform/win32/
# display_adapter.cpp, win32_display_adapter::pump_events()) drena a
# fila de mensagens via PeekMessageW(..., PM_REMOVE), que nunca produz
# um par (poll_result, revents) porque nao existe `::poll()` nenhum
# naquele caminho - toda a entrega de evento do Win32 e' por fila de
# mensagens do sistema, nunca por um descritor Unix sondado com poll(2)
# (mesma varredura de src/platform/win32/ ja citada acima: zero
# ocorrencias de `poll(`/`select(`). Sem o par de entrada, nao ha
# "decisao sobre esses valores" a testar do lado Windows - a ausencia
# e' consequencia direta e permanente da ausencia de poll(2) naquela
# API, nao um caso novo a decidir.
incoming_poll_outcome_test|windows|nenhum|SEM-PENDENCIA

# incoming_poll_reaction_test (tests/incoming_poll_reaction_test.cpp,
# tests/CMakeLists.txt, if(UNIX)) prova is_incoming_poll_connection_
# fatal() (src/platform/wayland/incoming_poll_reaction.hpp) - o atomo
# puro que decide, a partir de um incoming_poll_outcome mais um
# `errno_is_eintr` booleano, se a conexao esta realmente morta
# (CONT-WARMUP C-6, revisao adversarial C-5, /var/tmp/glintfx-plan/
# revisao-cont-warmup-c5.md, achado CRITICO-2, GODS_LAWS.md L-17/L-20) -
# um nivel ACIMA do atomo ja excetuado na entrada logo acima
# (incoming_poll_outcome_test|windows|nenhum|SEM-PENDENCIA), consumindo
# o MESMO tipo (incoming_poll_outcome) que so existe do lado da leitura
# via poll(2). MESMA ausencia, MESMO motivo, um nivel acima: pump_
# events() do Windows nunca produz um incoming_poll_outcome porque nao
# ha `::poll()` naquele caminho (varredura ja citada duas entradas
# acima: zero ocorrencias de `poll(`/`select(` em src/platform/win32/).
# Sem o valor de entrada, nao ha "decisao sobre esse valor" a testar do
# lado Windows - a ausencia e' consequencia direta e permanente da
# ausencia do atomo que ele consome, nao um caso novo a decidir (o
# MESMO raciocinio, em cascata, que a entrada de flush_retry_policy_
# test acima ja registra para o lado ESCRITA).
incoming_poll_reaction_test|windows|nenhum|SEM-PENDENCIA

# incoming_poll_wiring_test (tests/incoming_poll_wiring_test.cpp, tests/
# CMakeLists.txt:1743-1792, if(UNIX)) e' a COSTURA: chama wait_for_
# incoming_data() (src/platform/wayland/display_adapter.cpp) e poll_
# and_dispatch_with_budget() (src/platform/wayland/egl_context_adapter.
# cpp) REAIS, com um `::poll()` fabricado (a costura poll_impl, src/
# platform/wayland/incoming_poll_syscall.hpp) devolvendo cada desfecho -
# um nivel ACIMA das duas entradas logo acima (incoming_poll_outcome_
# test e incoming_poll_reaction_test), exercitando os dois atomos la'
# excetuados ATRAVES do ponto de uso real, no lugar de testa-los
# isolados (CONT-WARMUP C-6, EMENDA, GODS_LAWS.md L-12/L-17/L-20/L-27).
# MESMA ausencia, MESMO motivo, um nivel acima: pump_events() do Windows
# (src/platform/win32/display_adapter.cpp) drena a fila de mensagens via
# PeekMessageW(..., PM_REMOVE) - nao ha `::poll()` nenhum naquele
# caminho pra fabricar (varredura confirmada: zero ocorrencias de
# `poll(`/`select(`/`WSAPoll` em src/platform/win32/), entao nao ha
# costura nenhuma a exercitar do lado Windows. A ausencia e' consequen-
# cia direta e permanente da ausencia de poll(2) naquela API, nao um
# caso novo a decidir - o MESMO raciocinio em cascata das duas entradas
# acima, agora no nivel de integracao.
incoming_poll_wiring_test|windows|nenhum|SEM-PENDENCIA

# connection_failure_test (tests/connection_failure_test.cpp, tests/
# CMakeLists.txt:1803, dentro do MESMO bloco if(UNIX) de incoming_poll_
# wiring_test logo acima) - EGL-DEAD-DISPLAY-GUARD S1 (docs/plano-egl-
# dead-display-guard.md): prova connection_failure_if_dead() (src/
# platform/wayland/connection_failure.cpp) hermeticamente, socketpair()
# + wl_display_connect_to_fd() real, sem compositor. O atomo que ele
# prova SO EXISTE no backend Wayland (D-S5 do plano: "o Windows nao
# ganha fatia; nao ha fio nem erro fatal externo no Win32" -
# connection_failure.cpp nunca e' compilado em src/platform/win32/).
# Ausencia permanente por desenho, nao lacuna a fechar - MESMO
# raciocinio da familia incoming_poll_* logo acima: sem o mecanismo
# (aqui, a conexao Wayland em si), nao ha "decisao sobre a conexao"
# para o lado Windows provar. MORTE DESTA LINHA: nunca.
connection_failure_test|windows|nenhum|SEM-PENDENCIA

# --- P-0 (docs/plano-w6a-janela.md fatia 1, D-W6a-19, 05/09/2026) ------
#
# Os quatro nomes abaixo sao os fixtures que ja rodavam dentro do
# job `wayland-container`, agora publicados no inventario do
# container (parity-inv-linux-container) que o job `parity` passou a
# consumir como terceira fonte Linux (ver .github/workflows/ci.yml,
# job `wayland-container`, secao "P-0"). Sao mecanismo Wayland puro
# (compositor real dentro do container: bind/registry, pump
# nao-bloqueante, erro fatal do compositor, conecta/desconecta via o
# adaptador ARCH-PORTS) - o Windows nao tem "compositor Wayland" para
# nenhum deles ter par por natureza (GODS_LAWS.md L-05 fecha essa
# porta por desenho, mesma razao de wl_proto_link_test acima). O
# comportamento que cada um exercita ja tem cobertura Windows por
# OUTRO mecanismo (o adaptador de conexao de display, win32_display_
# connect_test, ja apelidado a display_connect_failure_test em
# tests/parity_aliases.txt) - mas o NOME de cada fixture e' proprio
# do container, sem gemeo de nome possivel, entao a linha certa aqui
# e' excecao SEM-PENDENCIA, nunca apelido.
arch_ports_connect_smoke|windows|nenhum|SEM-PENDENCIA
registry_smoke|windows|nenhum|SEM-PENDENCIA
pump_smoke|windows|nenhum|SEM-PENDENCIA
fatal_error_smoke|windows|nenhum|SEM-PENDENCIA

# alloc_cycle_growth_smoke (tests/container/alloc_cycle_growth_smoke.
# cpp, CONTAINER-LEAK-COUNTER sub-fatia S4, /var/tmp/glintfx-plan/leak-
# counter.md sec. 5 S4) - MESMA assimetria dos quatro nomes acima
# (mecanismo Wayland puro dentro do job `wayland-container`, que so
# existe do lado Linux), e mais uma camada: o que este fixture prova
# (que abrir e fechar display/shell/janela/contexto GL tres vezes nao
# faz a memoria de TERCEIRO - llvmpipe/mesa, o driver software deste
# container - crescer sem limite entre ciclos) nao tem NENHUM conceito
# equivalente do lado Windows deste projeto: nao ha classificador
# ours/third_party por endereco de retorno la, nem gancho de alocacao,
# nem job de container algum. Nome proprio, sem gemeo possivel -
# SEM-PENDENCIA, nunca apelido, mesma razao dos quatro acima.
alloc_cycle_growth_smoke|windows|nenhum|SEM-PENDENCIA

# --- WL-WINDOW fatia W-E (docs/plano-w6a-janela.md fatia 8), lacuna
# real, ATUALIZADA por WL-WINDOW-HANDLE (fatia 11/P-1, 06/09/2026) -----
#
# P-1 (tests/parity/window_parity_test.cpp) EXISTE agora, mesmo nome
# nos dois lados via P-0 (ctest no Windows, fixture do container no
# Linux) - a condicao que a versao anterior desta nota esperava para
# apagar esta linha inteira ("window_smoke vira window_parity_test...
# sem nome proprio a mais", docs/plano-w6a-janela.md tabela da fatia 8).
#
# ⚠️ NAO APAGADA MESMO ASSIM, achado ao escrever P-1: window_parity_
# test so fala a API PUBLICA (gltfx_display/gltfx_window), e essa
# superficie ainda NAO TEM metodo nenhum de buffer/contexto grafico
# (window.hpp's own "NAO CONGELADO AQUI... contexto grafico" - W6b, nao
# esta fatia) - logo P-1 nao pode reproduzir a prova que window_smoke.cpp
# faz hoje: anexar um wl_shm de verdade e fazer o compositor reprovar
# um ack_configure() removido (D-W5-9, xdg_surface.error.
# unconfigured_buffer). "Substituir" window_smoke por P-1 HOJE apagaria
# essa prova sem substituto nenhum, uma perda de cobertura real que o
# texto anterior desta nota nao previa quando foi escrito. window_smoke
# continua a UNICA prova real desta metade do protocolo ate W6b dar a
# gltfx_window um jeito publico de anexar um buffer.
#
# MORTE DESTA LINHA, atualizada: apagar quando W6b der a gltfx_window
# uma superficie publica de buffer/contexto grafico E window_smoke.cpp
# for reescrito para usa-la (ou explicitamente aposentado por decisao
# do lider/CTO) - nunca antes, e nunca so porque P-1 passou a existir.
window_smoke|windows|AUSENCIA MANTIDA, MAS A JUSTIFICATIVA ANTIGA FOI MEDIDA FALSA EM 08/09/2026. A linha anterior afirmava que window_smoke reprova quando ack_configure() e removido, e que por isso ele provava algo que window_parity_test (P-1) nao prova. Mutacao real, contra kwin 6.7.4 em dois containers isolados independentes, com a mutacao confirmada ate o binario: NEM window_smoke NEM gl_context_parity_test reprovam. O teste passa imprimindo o texto que ele so deveria imprimir quando nao ha erro de protocolo. Portanto window_smoke NAO se aposenta (a condicao de D-W6b-12 nao pode nem ser avaliada, porque o antigo nao pega mais nada para o novo ter que igualar), e a razao de ele existir esta sob investigacao. Dono trocado de GL-CONTEXT (concluido em 08/09) para o item que investiga o achado.|WL-ACK-SMOKE-BLUNT

# --- Lacuna real, TEMPORARIA por falta de runner Windows proprio
# (09/09/2026, CORE-LOG CL-7) ---------------------------------------
#
# gpu_kind_report_smoke (tests/container/gpu_kind_report_smoke.cpp)
# prova, contra um compositor kwin_wayland REAL, que report_gpu_kind_
# resolved() (src/platform/gl/gpu_kind_report.cpp) chega ao recebedor
# publico quando egl_context_adapter.cpp resolve a placa de video -
# inclusive a mutacao obrigatoria desta fatia (remover a chamada faz
# o fixture nao receber o evento e sair != 0), medida fora da arvore
# (GODS_LAWS.md L-27). O gemeo Win32 (wgl_context_adapter.cpp) tem a
# MESMA linha, no MESMO ponto, chamando a MESMA funcao compartilhada -
# e PROVADO QUE COMPILA limpo (zero avisos) sob o cl.exe REAL da
# Microsoft (tools/msvc-container). O que NAO foi provado: a mutacao,
# por execucao real, no lado Windows - abrir uma janela WGL de verdade
# exige CreateWindowExW, que tools/msvc-container/README.md ja
# documenta como falho sob a camada de compatibilidade headless desta
# maquina (GetLastError=1400/ERROR_INVALID_WINDOW_HANDLE), nao uma
# limitacao inventada agora para esta fatia.
#
# ⚠️ MORTE DESTA LINHA: quando WIN-LAB-INSTALAR (TODO.md, W9) fizer a
# instalacao real dentro do convidado Windows (V-7, dono trocado de
# WIN-RUNNER-PROPRIO por decisao do main em 24/09/2026 - V-6b/V-7 saem
# do item ja concluido e vao para WIN-LAB-INSTALAR, ainda pendente),
# um fixture Windows real (mesma forma de facade_pin_smoke/window_
# active_after_map_smoke, mutacao incluida) fecha o par - so entao esta
# linha sai, nunca antes por presuncao de que a compilacao limpa
# bastava.
#
# NAO usa o quinto campo PROVA-PARCIAL= (PARITY-ALIAS-HYGIENE, D10,
# 23/09/2026): o "gemeo" acima cita um ARQUIVO-FONTE (wgl_context_
# adapter.cpp), nunca um nome de ctest - `grep -n gpu_kind
# tests/CMakeLists.txt` medido em 23/09/2026 confirma que nao existe
# hoje nenhum ctest de compilacao-so registrado para o lado Windows
# deste fixture. D10 e explicito: "se o gemeo nao existir [como ctest
# no inventario do outro lado], a linha e uma ausencia e fica como
# esta" - converter aqui sem esse ctest existir de verdade seria
# reintroduzir a mesma prosa nao conferida que o quinto campo existe
# para banir.
gpu_kind_report_smoke|windows|wgl_context_adapter.cpp tem a mesma chamada, mesmo ponto, mesma funcao compartilhada (gpu_kind_report.cpp) - PROVADO que compila limpo sob o cl.exe real da Microsoft; a mutacao por execucao real NAO foi provada no Windows nesta maquina (CreateWindowExW falha sob Wine headless, ja documentado em tools/msvc-container/README.md)|WIN-LAB-INSTALAR

# --- Historico: os sete nomes que faltavam par, todos fechados ---------
#
# Os sete nomes que a estreia deste portao (05/09/2026) reprovou sem
# excecao possivel fecharam por PAR DE VERDADE, nao por excecao - o
# proprio motivo pelo qual nenhuma linha deles aparece aqui embaixo:
#
# 1. consume_test, embed_test, port_privacy_test, port_privacy_
#    selftest, visibility_test, preci_selftest - tinham par so como
#    step de shell cru do job `windows` (nunca registrado como ctest).
#    Viraram ctest de verdade (tests/CMakeLists.txt, if(WIN32):
#    consume_win_test, embed_win_test, port_privacy_win_test, port_
#    privacy_win_selftest, exports_win_test, preci_win_selftest) - o
#    par agora e um APELIDO (tests/parity_aliases.txt).
#
# 2. install_includedir_test, install_packager_layout_test, output_
#    name_test, no_target_collision_test - nao tinham NENHUM mecanismo
#    Windows. Ganharam um cada (tools/ci/*-win.ps1, if(WIN32)) - tres
#    checagem real, um (install_packager_layout_win_test) ausencia
#    DECLARADA por desenho (convencao Debian-multiarch que o MSVC nao
#    tem). Apelidados em tests/parity_aliases.txt.
#
# 3. dep_zero_binary_win_test/_selftest e exports_win_selftest -
#    existiam no Windows e faltavam no Linux (o achado INVERSO): o
#    lado Linux fazia a mesma verificacao embutida noutro teste (dep_
#    zero_test) ou nunca tinha ganho autoteste proprio (check_
#    exports.sh). Ganharam nome/autoteste proprio do lado Linux
#    (dep_zero_binary_test/_selftest, visibility_selftest) - tambem
#    apelidados, nunca excetuados (os itens que os cobririam,
#    DEPZERO-PARITY-WIN e EXPORTS-PARITY-WIN, ja estavam Concluidos -
#    excecao teria reprovado pela propria regra deste arquivo).
#
# --- Achado a parte, ainda sem mecanismo (nao vira nome novo aqui) -----
#
# tools/ci/check-pkgconfig-installed.ps1 (PKG-WIN-SCOPE) NAO virou
# ctest, e por isso nao aparece nem aqui nem em tests/parity_
# aliases.txt - o gate so compara NOMES de `ctest -N`, e este script
# nunca ganhou um. A causa: ele depende de um valor (o prefixo de
# instalacao) gerado em TEMPO DE EXECUCAO por outro passo do job
# `windows` (`steps.consumo_instalado.outputs.prefix`), que o
# `configure` do CMake nao tem como conhecer. O mecanismo que falta
# para consertar isto de verdade e o CMake ja tem, so esta arvore nunca
# usou: `FIXTURES_REQUIRES`/`FIXTURES_SETUP` do ctest, que declaram uma
# dependencia de ORDEM/DADO entre dois casos sem precisar de um valor
# de tempo de execucao de fora do ctest. Ate isso ser construido, o
# passo cru "Consumo instalado" (.github/workflows/ci.yml) continua
# existindo SO para produzir esse prefixo - o que faz `check-consume.
# ps1` rodar DUAS VEZES por leg Windows (uma vez ali, outra via
# consume_win_test de ctest) - redundante, nao quebrado, e documentado
# aqui e no proprio ci.yml para o proximo leitor nao achar sozinho.
#
# tests/hostile_gawk_macros_shim.hpp (header_hygiene_test.cpp, CORE-
# LOG-CI, run 34352638143) - o shim que reproduz a colisao real do
# gawk (macro-objeto `warning`) agora se guarda por `#ifndef _MSC_VER`
# em vez de rodar sem condicao. NAO vira uma linha de quatro campos
# aqui: `header_hygiene_test` continua existindo, e passando, nos
# CINCO alvos - nao ha nome de ctest ausente para declarar. O que
# reduziu de verdade foi a CLASSE DE COLISAO exercitada dentro desse
# teste: cinco plataformas viraram quatro (Fedora, Ubuntu, Arch,
# CachyOS), porque o proprio shim, sem guarda, quebrava os cabecalhos
# padrao da Microsoft (`#pragma warning(push)` de <cstdint>/<stdint.h>
# vira lixo sintatico quando `warning` ja e macro-objeto - warning
# C4081 em seis pontos diferentes, depois error C2220 sob /WX). A
# razao completa da guarda, e por que o criterio e `_MSC_VER` e nao
# `_WIN32`, mora no proprio header do shim - GODS_LAWS.md L-04 exige
# que a reducao fique registrada, nao silenciosa dentro de um #ifdef;
# este paragrafo e essa a marca.

# --- Sonda exclusiva de UM sistema, sem par possivel (10/09/2026,
# achado GATE-SELFTEST-ORFAO, run 34509142938 commit 5243dd5) --------
#
# win_vm_lab_isolamento_selftest (tools/win-vm-lab/provar-isolamento.sh
# --selftest, registrado em tests/CMakeLists.txt dentro de if(UNIX))
# prova, contra fixtures XML hermeticas versionadas (tools/win-vm-lab/
# fixtures/dominio-{sabotado,limpo}.xml, nunca libvirt/QEMU real), o
# ISOLAMENTO da maquina virtual Windows-convidada que tools/win-vm-lab/
# hospeda contra a sessao de trabalho do lider (GODS_LAWS.md L-09/
# L-50) - e uma sonda do proprio LABORATORIO de teste, nao um
# comportamento da biblioteca, mesma categoria de win32_runner_probe_
# test/egl_probe_smoke ja registrados acima neste arquivo.
#
# O laboratorio e' host Linux (libvirt/QEMU) hospedando uma VM
# Windows-convidada isolada; nao ha, e nunca vai haver, um "laboratorio
# Windows hospedando VM Windows" do lado Windows, pela mesma razao ja
# registrada no comentario de tests/CMakeLists.txt (conserto de
# 10/09/2026, commit 32b7044, GODS_LAWS.md L-04/L-17): quando o proprio
# runner JA E' Windows, nao ha maquina virtual Windows a isolar - nao e'
# lacuna de protecao, e' a fronteira correta. Reforca a mesma ausencia:
# o script e' POSIX sh (`#!/usr/bin/env bash`), sem interprete que os
# runners Windows da matriz reconhecam, razao pela qual o proprio
# add_test entrou condicionado a if(UNIX) e nao a uma guarda de
# ausencia de ferramenta em tempo de execucao.
#
# MORTE DESTA LINHA: nunca - a assimetria e' permanente por desenho,
# nao lacuna temporaria esperando maquina/runner novo (distinto de
# gpu_kind_report_smoke acima, cujo dono e' WIN-LAB-INSTALAR desde
# 24/09/2026 - antes WIN-RUNNER-PROPRIO, trocado quando esse item
# fechou concluido e V-6b/V-7 saíram para WIN-LAB-INSTALAR). Sem
# item para rastrear, mesmo padrao das demais linhas SEM-PENDENCIA
# desta secao inicial do arquivo.
win_vm_lab_isolamento_selftest|windows|nenhum|SEM-PENDENCIA

# --- Tres sondas exclusivas de UM sistema, sem par possivel (22/09/2026,
# item WIN-RUNNER-PROPRIO, sub-fatias V-1 e V-2 - reprovacao real do run
# 35767005619, commit 5bf505a, conserto neste commit) -------------------
#
# As tres a seguir vivem em tools/win-vm-lab/, registradas em
# tests/CMakeLists.txt dentro do MESMO if(UNIX) que ja protege
# win_vm_lab_isolamento_selftest acima - motivo estrutural identico
# (script POSIX `#!/usr/bin/env bash`, sem interprete que os runners
# Windows da matriz reconhecam), mas cada uma prova uma coisa diferente
# e por isso ganha seu proprio paragrafo em vez de reaproveitar o texto
# do irmao.
#
# win_vm_lab_rodar_caminho_selftest (tools/win-vm-lab/rodar-caminho.sh
# --selftest) prova, contra um dublê de `virsh` (nunca libvirt/QEMU
# real), que o script traduz os quatro desfechos possiveis de
# `guest-exec`/`guest-exec-status` (sucesso, o convidado termina com
# erro, o prazo estoura, o `guest-exec` falha ao iniciar) no PROPRIO
# codigo de saida do script - e o conserto do falso verde achado em
# 22/09/2026 (o corpo antigo tinha `set -u` sem `set -e` e sempre saia
# 0). E' um roteiro que RODA NO HOSPEDEIRO Linux e fala com a VM
# Windows-convidada pelo canal do agente QEMU; nao ha, e nunca vai
# haver, um roteiro simetrico rodando dentro do runner Windows que
# dirija uma VM a partir de la - o proprio ponto do laboratorio e' o
# host Linux/libvirt hospedar e dirigir o convidado, nao o inverso.
#
# win_vm_lab_rodar_um_selftest (tools/win-vm-lab/rodar-um.sh --selftest)
# e' o MESMO mecanismo do paragrafo acima, so que resolvendo o alvo por
# NOME de arquivo (para `C:\Users\glintfx\<nome>`) em vez de caminho
# completo - mesma ausencia, mesma razao estrutural, mesmo host que
# dirige, nunca o inverso.
#
# win_vm_lab_sessao_selftest (tools/win-vm-lab/sessao.sh --selftest,
# sub-fatias V-2 e V-5b, docs/plano-fecho-w7b.md secao 2.2.2) e' o
# motorista de sessao: trava de exclusao mutua (`flock` sobre
# .vm.lock), segunda verificacao independente por `virsh domstate`,
# sobreposicao qcow2 descartavel (`qemu-img create -b/-F`) que nasce e
# morre dentro da mesma posse da trava, e (V-5b, 22/09/2026) o mecanismo
# de ligar pela copia avulsa (D-2/D-3/D-4: fonte do disco, nvram e
# estado do TPM trocados, nunca a definicao permanente), provado inteiro
# contra dubles de `virsh`. As quatro ferramentas que ele usa (`flock`,
# `virsh`, `qemu-img`, `xmlstarlet` desde V-5b) sao do HOSPEDEIRO
# libvirt/QEMU; nao existe, por desenho, um "motorista de sessao" do
# lado Windows porque o lado Windows aqui e' sempre o CONVIDADO sendo
# dirigido, nunca quem dirige.
#
# MORTE DESTAS TRES LINHAS: nunca - mesma classe de assimetria
# permanente por desenho que win_vm_lab_isolamento_selftest acima ja
# registra, nao lacuna temporaria. Sem item para rastrear, gemeo=nenhum
# nos tres, mesmo padrao SEM-PENDENCIA desta secao inicial do arquivo.
win_vm_lab_rodar_caminho_selftest|windows|nenhum|SEM-PENDENCIA
win_vm_lab_rodar_um_selftest|windows|nenhum|SEM-PENDENCIA
win_vm_lab_sessao_selftest|windows|nenhum|SEM-PENDENCIA

# win_vm_lab_coletar_resultados_selftest (tools/win-vm-lab/coletar-
# resultados.sh --selftest, sub-fatia V-3, /var/tmp/glintfx-plan/win-runner-
# local.md secao 1.1 e 6) prova, contra um dublê de `virsh` (nunca libvirt/
# QEMU real), o coletor de resultado por `guest-file-read`: piso de
# varredura nao-vazia (E6, diretorio sem arquivo -> recusa com
# encontrados=0 coletados=0 impressos), md5 conferido nas duas pontas (E8,
# copia integra aceita, copia truncada de proposito grita e nao deixa
# arquivo parcial), e lista de PERMISSAO para nome de arquivo (achado do
# team-lead no mesmo dia: o convidado escolhe cada nome devolvido pela
# listagem e e' NAO CONFIAVEL por desenho - nome fora do padrao
# `^[A-Za-z0-9][A-Za-z0-9._-]*$` e' rejeitado ANTES de virar caminho no
# hospedeiro, nunca por `continue` calado, com codigo de saida proprio).
# MESMA razao estrutural dos tres vizinhos logo acima
# (script POSIX `#!/usr/bin/env bash`, sem interprete que os runners
# Windows da matriz reconhecam - add_test tambem condicionado a if(UNIX));
# o proprio laboratorio e' o host Linux/libvirt hospedando e dirigindo o
# convidado Windows, nunca o inverso.
win_vm_lab_coletar_resultados_selftest|windows|nenhum|SEM-PENDENCIA

# win_vm_lab_consolidar_selftest (tools/win-vm-lab/consolidar.sh
# --selftest, sub-fatia V-6a, docs/plano-fecho-w7b.md secao 2.2.2) prova,
# contra um dublê de `virsh` (nunca libvirt/QEMU real) e um par
# base/sobreposicao qcow2 de BRINQUEDO que o proprio autoteste cria e
# apaga, o caminho de CONSOLIDACAO da sobreposicao (`qemu-img commit`):
# recusa se a maquina nao estiver desligada, recusa sem a trava de
# exclusao mutua, recusa (e apaga) uma copia de seguranca que saia
# corrompida antes de chegar perto do commit, e confere que a marca escrita
# na sobreposicao migra para o base e que a copia de seguranca, restaurada,
# reproduz o base de ANTES do commit byte a byte. MESMA razao estrutural
# dos cinco vizinhos acima (script POSIX `#!/usr/bin/env bash`, sem
# interprete que os runners Windows da matriz reconhecam - add_test tambem
# condicionado a if(UNIX)); o proprio laboratorio e' o host Linux/libvirt
# hospedando e dirigindo o convidado Windows, nunca o inverso.
win_vm_lab_consolidar_selftest|windows|nenhum|SEM-PENDENCIA

# --- Sonda exclusiva de UM sistema, sem par possivel (11/09/2026,
# item CONTAINER-LOG-SIGNAL-FIRST) -------------------------------------
#
# container_exec_fixture_selftest (tests/container/exec_fixture.sh
# --selftest, registrado em tests/CMakeLists.txt dentro de if(UNIX))
# prova, via `--runner "sh -c"` (sem Docker nenhum), os quatro
# controles do atomo que os 18 passos `docker exec ... <fixture>` do
# job `wayland-container` (.github/workflows/ci.yml) agora usam -
# meca com `grep -c "tests/container/exec_fixture.sh glintfx-wltest-clean"
# .github/workflows/ci.yml`, descontando o controle negativo cn5 (que
# usa o mesmo atomo, mas nao e' um passo de fixture do job):
# sucesso limpo, morte por sinal (rc=139/signal=SEGV), reprovacao
# comum (rc=1/signal=none) e o piso de varredura vazia (bytes=0)
# de GODS_LAWS.md L-40.
#
# O job `wayland-container` que este atomo serve so' existe do lado
# Linux (`runs-on: ubuntu-latest`) - o lado Windows nao tem, e nunca
# vai ter, um `docker exec` contra um compositor Wayland dentro de
# container, pela mesma razao que este projeto nao tem backend X11
# nenhum do lado Linux (CLAUDE.md: "Linux e' Wayland puro"). Nao ha
# comportamento equivalente a espelhar do lado Windows - a assimetria
# nao e' lacuna esperando fixture nova, e' a fronteira do proprio
# mecanismo (mesma categoria de win_vm_lab_isolamento_selftest acima:
# sonda de UM laboratorio de teste, nunca comportamento da biblioteca).
# Reforca a mesma ausencia: o script e' POSIX sh (`#!/usr/bin/env sh`),
# usa `kill -l`/`mktemp`/`sh -c` como runner alternativo, nenhum deles
# nativo nos runners Windows desta matriz - razao pela qual o proprio
# add_test entrou condicionado a if(UNIX), mesma forma de tests/
# CMakeLists.txt que win_vm_lab_isolamento_selftest ja usa.
#
# MORTE DESTA LINHA: nunca - a assimetria e' permanente por desenho.
# Sem item para rastrear, mesmo padrao SEM-PENDENCIA das demais linhas
# desta secao do arquivo.
container_exec_fixture_selftest|windows|nenhum|SEM-PENDENCIA

# container_alloc_report_selftest (tests/container/check_alloc_report.sh
# --selftest, registrado em tests/CMakeLists.txt dentro de if(UNIX),
# CONTAINER-LEAK-COUNTER sub-fatia S3) - MESMA assimetria que container_
# exec_fixture_selftest logo acima, pelo mesmo motivo: prova, com dois
# arquivos sinteticos (sem Docker), os seis controles do criterio que o
# job `wayland-container` aplica ao relatorio do gancho de alocacao das
# fixtures desse job - um job que so' existe do lado Linux. POSIX sh, `mktemp`/
# `awk` plain POSIX, nenhum nativo nos runners Windows desta matriz;
# add_test tambem condicionado a if(UNIX). MORTE DESTA LINHA: nunca -
# mesma assimetria permanente por desenho, sem item para rastrear.
container_alloc_report_selftest|windows|nenhum|SEM-PENDENCIA

# container_run_compositor_selftest (tests/container/run_compositor.sh
# --selftest, registrado em tests/CMakeLists.txt dentro de if(UNIX),
# item CONT-WARMUP) - MESMA assimetria que container_exec_fixture_
# selftest/container_alloc_report_selftest logo acima, pelo mesmo
# motivo: prova, com uma sonda dublada (sem Docker/kwin_wayland),
# a logica do laco que espera o compositor aceitar conexao real antes
# de declarar pronto - laco que so' existe porque run_compositor.sh e'
# o ENTRYPOINT de uma imagem que so' o job `wayland-container` (Linux)
# sobe. POSIX sh (`#!/usr/bin/env sh`), redefine funcao dentro de
# subshell `nome() ( ... )` como mecanismo de isolamento entre casos -
# nenhum runner Windows desta matriz tem `sh` nativo; add_test tambem
# condicionado a if(UNIX), mesma forma dos dois vizinhos acima. MORTE
# DESTA LINHA: nunca - mesma assimetria permanente por desenho, sem
# item para rastrear.
container_run_compositor_selftest|windows|nenhum|SEM-PENDENCIA

# container_wait_for_ready_marker_selftest (tests/container/wait_for_
# ready_marker.sh --selftest, registrado em tests/CMakeLists.txt dentro
# de if(UNIX), WL-ACK-SMOKE-BLUNT A3e passo 2) - MESMA assimetria que
# container_run_compositor_selftest logo acima, pelo mesmo motivo:
# prova, com uma sonda dublada (marker_probe() redefinida dentro de
# subshell, sem Docker), a logica do script que ESPERA o marcador de
# prontidao que run_compositor.sh publica - script que so' existe
# porque a imagem que ele espera e' a que so' o job `wayland-container`
# (Linux) sobe. POSIX sh, nenhum runner Windows desta matriz tem `sh`
# nativo; add_test tambem condicionado a if(UNIX), mesma forma do
# vizinho acima. MORTE DESTA LINHA: nunca - mesma assimetria permanente
# por desenho, sem item para rastrear.
container_wait_for_ready_marker_selftest|windows|nenhum|SEM-PENDENCIA

# drm_device_facts_oom_test (tests/drm_device_facts_oom_test.cpp,
# tests/CMakeLists.txt dentro de if(UNIX), NOEXCEPT-ALLOC-B8 fatia F2,
# /var/tmp/glintfx-plan/plano-conserto-noexcept.md sec. "F2") prova que
# read_drm_device_facts() (src/platform/wayland/drm_device_facts.cpp),
# sob falta de memoria forcada, nao mata o processo do consumidor ao
# abrir um NO DE DISPOSITIVO DRM identificado por CAMINHO DE ARQUIVO
# (open(2) sobre algo como /dev/dri/renderD128) - o mecanismo que a
# varredura de 17/09/2026 achou desprotegido (GL-GPU-KIND).
#
# O Windows NAO TEM, e nunca vai ter, esse mecanismo: a enumeracao de
# placa de video do lado Win32 (src/platform/win32/dxcore_adapter_
# enumeration.cpp) e' inteiramente LUID/COM via IDXCoreAdapter - medido
# por leitura do arquivo inteiro, a unica ocorrencia da palavra "path"
# ali e' um comentario sobre caminho de EXECUCAO de codigo, nunca um
# caminho de SISTEMA DE ARQUIVOS. Nao existe, no lado Windows, nenhuma
# chamada que abra um dispositivo grafico por uma string de caminho -
# a API inteira de descoberta de adaptador e' outra, por construcao do
# proprio DXCore (mesma distincao que wl_proto_link_test/display_bind_
# refusal_test, acima neste arquivo, ja registram para o mecanismo de
# globais do protocolo Wayland: "porta que L-05 fecha por desenho").
# read_driver_description() (dxcore_adapter_enumeration.cpp:108), o
# unico ponto daquele arquivo que copia string dentro de funcao
# noexcept, ja e' guardado por try/catch (comentario no proprio
# arquivo cita a mesma disciplina) - nao e' um ponto da familia B, e
# nao tem relacao com esta linha.
#
# MORTE DESTA LINHA: nunca - abrir um no de dispositivo por caminho de
# arquivo e' conceito do kernel Linux (DRM/UAPI), permanente por
# desenho, nao lacuna temporaria esperando mecanismo Windows novo. Sem
# item para rastrear, mesmo padrao SEM-PENDENCIA das demais linhas
# desta secao do arquivo.
drm_device_facts_oom_test|windows|nenhum|SEM-PENDENCIA

# --- Sondas exclusivas de UM sistema, sem par possivel (19/09/2026,
# achado do time-lead, server run 35449851650, PARITY-GATE contra o
# commit f8da8c3/LINT-CONTAINER-SMOKES) -------------------------------
#
# container_smokes_in_compile_db_test e container_smokes_in_compile_db_
# selftest (tests/tools/check_container_smokes_in_compile_db.py,
# registrados em tests/CMakeLists.txt dentro de if(UNIX), junto do
# proprio `add_subdirectory(container)` logo acima deles no mesmo
# arquivo) provam que a OBJECT library de tests/container/*.cpp -
# compilada apenas para o banco de compilacao (compile_commands.json),
# nunca ligada nem executada no host, GODS_LAWS.md L-09 - realmente
# entra nesse banco, para que run-clang-tidy nao pule nenhum fixture em
# silencio.
#
# O Windows nao tem, e nunca vai ter, par: tests/container/ inteiro
# (o subdiretorio que tests/CMakeLists.txt so' adiciona dentro de
# if(UNIX)) existe para compilar fixtures Wayland/EGL contra o
# adaptador ARCH-PORTS - GODS_LAWS.md L-05 fecha essa porta por desenho
# do lado Windows, mesma razao ja registrada acima para wl_proto_link_
# test e display_bind_refusal_test. Sem OBJECT library nenhuma do lado
# Windows, nao ha banco de compilacao dela para este gate comparar -
# nome proprio do mecanismo Linux, sem gemeo possivel por natureza.
#
# MORTE DESTA LINHA: nunca - a assimetria e' permanente por desenho,
# mesma categoria de wl_proto_link_test/display_bind_refusal_test. Sem
# item para rastrear, mesmo padrao SEM-PENDENCIA das demais linhas
# desta secao do arquivo.
container_smokes_in_compile_db_test|windows|nenhum|SEM-PENDENCIA
container_smokes_in_compile_db_selftest|windows|nenhum|SEM-PENDENCIA

# wire_relay_selftest (WL-ACK-SMOKE-BLUNT, sub-fatias A1/A2, docs/
# plano-w7c.md SS3.A e docs/plano-w7c-adendo-revalidacao.md SS3.A) -
# tests/CMakeLists.txt registra este alvo dentro de if(UNIX), porque
# ele prova o rele estrito do protocolo Wayland (transporte, decoder,
# tabela de objetos, motor de regras R1/R2, injetor de erro) - o wire
# format que WL-ACK-SMOKE-BLUNT existe para exercer so' faz sentido do
# lado Wayland (GODS_LAWS.md L-05 fecha X11 e Windows por desenho, mesma
# razao ja registrada acima para wl_proto_link_test e os fixtures de
# tests/container/). O autoteste em si NUNCA sobe compositor nem
# container (GODS_LAWS.md L-09: par de soquetes e montante falso).
#
# MORTE DESTA LINHA: nunca - assimetria permanente por desenho, mesmo
# padrao SEM-PENDENCIA das demais linhas Wayland-only desta secao.
wire_relay_selftest|windows|nenhum|SEM-PENDENCIA

# wire_relay (WL-ACK-SMOKE-BLUNT, sub-fatia A3b, docs/plano-w7c-
# adendo-revalidacao.md SS3.A item 2) - NAO confundir com wire_relay_
# selftest logo acima: aquele e' o autoteste do CODIGO do rele (roda
# no host, par de soquetes, sem compositor); este e' o BINARIO
# compilado dentro do container (Containerfile, COPY --from=arch-
# ports-builder /build/wire_relay_bin /usr/local/bin/wire_relay) que
# run_compositor.sh sobe como daemon de longa duracao entre o cliente
# e o KWin real. Mesmo padrao dos quatro fixtures P-0 logo acima
# (arch_ports_connect_smoke e irmaos): nome proprio do container, sem
# par possivel do lado Windows (GODS_LAWS.md L-05 fecha essa porta por
# desenho), agora alimentando parity_inventory.txt porque tests/
# container/check_isolation.sh (job wayland-container, passo
# "Isolamento provado") prova ali mesmo que o binario esta vivo e
# funcionando (PID + socket + lsof) antes de qualquer interacao.
#
# MORTE DESTA LINHA: nunca - mesma assimetria permanente por desenho,
# mesmo padrao SEM-PENDENCIA das demais linhas Wayland-only desta
# secao.
wire_relay|windows|nenhum|SEM-PENDENCIA

# CI-SPLIT-PER-OS A1 (24/09/2026), categoria (b) da classificacao dos 215
# problemas do run 36085750444 (job parity, d74d0b0), decisao do time-lead:
# 18 fixtures do job wayland-container (confirmadas contra a lista de 24
# fixtures de check_container_kill_order.py --compare), presentes em fedora
# (via o leg container) e em windows, mas ausentes de arch/cachyos/ubuntu -
# essas tres distros nao rodam o job wayland-container hoje. Gap REAL, nao
# defeito de portao: pela L-04, cobertura que falta nas outras distros; a
# fatia A8 (container por sistema) resolve, e quando ela fechar estas linhas
# morrem pela regra de morte (item CI-SPLIT-PER-OS concluido).
alloc_cycle_growth_smoke|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
alloc_cycle_growth_smoke|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
alloc_cycle_growth_smoke|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
arch_ports_connect_smoke|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
arch_ports_connect_smoke|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
arch_ports_connect_smoke|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
egl_error_read_inside_swap_smoke|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
egl_error_read_inside_swap_smoke|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
egl_error_read_inside_swap_smoke|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
egl_protocol_error_smoke|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
egl_protocol_error_smoke|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
egl_protocol_error_smoke|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
fatal_error_smoke|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
fatal_error_smoke|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
fatal_error_smoke|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
gl_context_parity_test|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
gl_context_parity_test|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
gl_context_parity_test|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
gpu_kind_report_smoke|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
gpu_kind_report_smoke|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
gpu_kind_report_smoke|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
loop_hidden_test|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
loop_hidden_test|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
loop_hidden_test|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
loop_parity_test|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
loop_parity_test|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
loop_parity_test|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
pump_smoke|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
pump_smoke|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
pump_smoke|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
registry_smoke|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
registry_smoke|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
registry_smoke|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
seat_test|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
seat_test|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
seat_test|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
surface_size_policy_adapter_test|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
surface_size_policy_adapter_test|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
surface_size_policy_adapter_test|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
two_displays_test|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
two_displays_test|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
two_displays_test|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
two_seats_test|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
two_seats_test|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
two_seats_test|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
window_parity_test|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
window_parity_test|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
window_parity_test|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
window_smoke|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
window_smoke|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
window_smoke|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
wire_relay|arch|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
wire_relay|cachyos|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
wire_relay|ubuntu|CI-SPLIT-PER-OS A1, categoria (b) da classificacao dos 215 problemas do run 36085750444 (job parity, d74d0b0): fixture do job wayland-container, hoje so' roda no Fedora (FATO do CTO, docs/plano-ci-split-per-os.md secao 2, "O container e' so' Fedora"); a fatia A8 de CI-SPLIT-PER-OS leva o container a cada sistema|CI-SPLIT-PER-OS
