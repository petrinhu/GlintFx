# Plano da onda W6b: a placa gráfica e o laço principal, Linux e Windows juntos

STATUS: decidido, 06/09/2026 08:46:58 (hora real de `date`). Autor: CTO (fable), em modo autônomo (L-34 do projeto; ordem do líder de 05/09/2026, verbatim: *"eu já mandei modo autonomo! vc fica parando o tempo todo! deixe o clevel responder!"* e *"pode seguir até w10"*). Só planejamento e decisão. Nada implementado, nada commitado.

Este plano sucede `docs/plano-w6a-janela.md` (o modelo desta casa; §4 e §5 daquele plano continuam válidos onde este não os altera). O `TODO.md` é do orquestrador: **nenhum item novo é criado aqui** (L-32 do projeto, teto de escopo); o que este plano precisa e não tem linha própria é **fatia dentro de item existente**, dito onde acontece.

Frase-guarda (L-21 global), que entra verbatim em todo briefing de agente desta onda: **produto para distribuição, base de consumidores aberta e desconhecida. Nada sai do escopo porque "o jogo do líder não usa".**

Regra de leitura deste documento, herdada da onda passada e agora cobrada por portão (`tests/tools/check_plan_scope_diff.py`): **todo caminho entre crases na coluna "Nasce / muda" da tabela da §3 é promessa**, conferida contra a árvore no fechamento. Caminho que não nascer precisa de linha datada em `tests/parity_absences.txt` com item dono, ou a onda não fecha. Toda frase que diz "medido", "idêntico" ou "nunca" cita o teste que prova, ou não entra.

---

## 0. O estado, medido agora contra `main` em `2be7893` (não de memória)

| # | Fato medido | Onde | O que muda no plano |
|---|---|---|---|
| F1 | **A W6a fechou**: run `34028525955`, 22 trabalhos verdes, inclusive `Paridade Linux x Windows`. | `gh run view 34028525955 --json jobs` | O consumidor abre display e janela nos dois sistemas pela mesma chamada; esta onda parte daí. |
| F2 | **A superfície pública da janela é**: `gltfx_display::{open,is_open,pump_events}`, `gltfx_window::{open,is_open,logical_size,pixel_size,state,close_requested,set_title}`. Internamente `window_impl` guarda `selected_window_adapter`; `display_impl` guarda conexão + shell no Wayland, só conexão no Win32. **Nenhum seat na composição** (ausência declarada, item `WL-DISPLAY-BACKEND-SEAT`, `tests/parity_absences.txt`). | `include/glintfx/platform/window/{display,window}.hpp`, `src/platform/window/display_impl.hpp`, `src/platform/window/window_facade.cpp` | O contexto gráfico precisa alcançar o `wl_surface` (Wayland) e o `HWND` (Win32) da janela: os dois adaptadores já expõem isso (`surface()`, `native_handle()`), e a fachada chega neles pelo mesmo passkey que `gltfx_window::open()` já usa para o display. Nada público novo é preciso para essa costura. |
| F3 | **O executor Windows tem prova gráfica 3.3 core desde 05/09/2026**: `tools/ci/install-mesa-opengl32.ps1` põe o `opengl32.dll` do Mesa (D3D12, versão e sha256 pinados) ao lado dos executáveis de teste em **cada perna** Windows (`ci.yml` linha 750, `matrix.modo`); a sonda `win32_runner_probe_test` imprimiu `wglCreateContextAttribsARB(3.3 core) ok=true` (run `33994059308`). Sem o aparato: `GDI Generic 1.1`, sem a extensão. | `DECISOES_AUTONOMAS.md` 05/09 18:00 e 18:56, `ci.yml` 733-752 | O desfecho (ii) da árvore de decisão da W6a aconteceu e já está executado. `WIN-GL` é provável no servidor. **Pendente do líder, já registrado:** confirmação retroativa do download de binário de terceiro no servidor. |
| F4 | **A imagem do container Linux já tem `kwin`, `mesa-dri-drivers` (llvmpipe) e `mesa-libEGL`**, e `wayland-devel` (que a imagem instala) traz `libwayland-egl` no Fedora (medido nesta máquina: `rpm -ql wayland-devel` lista `libwayland-egl.so` e `wayland-egl.pc`; `pkg-config --modversion wayland-egl egl` = `18.1.0` / `1.5`). | `tests/container/Containerfile` 105, 239-242; host | O lado Linux do contexto gráfico é provável **no container**, por software, sem `/dev/dri`. ⚠️ **Não medido dentro da imagem**: que `eglGetPlatformDisplay` no `kwin_wayland --virtual` devolve um display EGL utilizável e que o llvmpipe cria contexto **3.3 core** ali. Nasce a sonda G-1 (§3, fatia 1), primeiro push, gêmea da X-GL-0 da W6a. |
| F5 | **O carregador de GL existe e é interno**: `glintfx::render::load_gl_functions(gl_proc_address_fn)` devolve `gltfx_rslt<gl_function_table>` com as 344 funções 3.3 core, gerado em build a partir do `gl.xml` vendorizado; `include/glintfx/render/` está **vazio**. | `tools/gl_registry_codegen/loader_codegen.cpp` 88-114, `src/render/CMakeLists.txt` | `GL-CONTEXT` é o primeiro chamador real do carregador. O `gl_proc_address_fn` que ele exige vem do contexto (EGL: `eglGetProcAddress`; WGL: `wglGetProcAddress` com fallback em `GetProcAddress(opengl32)` para as funções 1.1, que o WGL **não** resolve, armadilha conhecida, ver §1 busca). |
| F6 | **`pump_events()` existe nos dois lados e não bloqueia** (Wayland: `prepare_read`/`flush`/`poll` zero/`read_events`/`dispatch_pending`; Win32: laço `PeekMessageW(PM_REMOVE)` da thread inteira). **Nenhum dos dois tem "espere por evento com orçamento de tempo"**: não há `poll` com timeout positivo no Wayland nem `MsgWaitForMultipleObjects` no Win32. | `src/platform/wayland/display_adapter.cpp` 243-312, `src/platform/win32/display_adapter.cpp` 275-293 | O laço precisa dormir quando a janela está oculta ("janela oculta não computa nada", `ESCOPO.md` §4, requisito do líder de 28/08). Isso exige um `wait_events(orçamento)` nos dois adaptadores de display, **interno**, atrás da porta (fatia 6). |
| F7 | **`CORE-TIME` não entregou leitura de relógio**: o header público tem `gltfx_duration`, `gltfx_time_point` e três funções (`between`, `to_seconds`, `from_seconds`); **não existe `gltfx_now()`**. | `include/glintfx/core/time.hpp` 180-230 | O laço precisa ler o relógio monotônico. Entra como **adição compatível** ao header de tempo (`gltfx_now()`, sobre `std::chrono::steady_clock`, biblioteca padrão, não API de SO; o portão de camada não cita `chrono`). Não é reabertura do congelamento de `CORE-TIME`: é o método que a descrição do item já prometia ("relógio monotônico") e nunca nasceu. |
| F8 | **Protocolos presentes nesta máquina** (`/usr/share/wayland-protocols/`): `staging/cursor-shape/cursor-shape-v1.xml`, `staging/fractional-scale/fractional-scale-v1.xml`, `stable/viewporter/viewporter.xml`. Headers EGL em `/usr/include/EGL/`. `kwin 6.7.4` no host. O binding gerado por `wayland-scanner` a partir do XML do sistema é precedente estabelecido (`cmake/GlintfxWaylandProtocols.cmake`). | host | Nada desta onda precisa de protocolo além de `xdg-shell` (já ligado) e do `wl_surface.frame`, que é do núcleo do protocolo. Os três XML acima são de `WL-POINTER` e `WL-SCALE`, que **não** executam nesta onda (§2). |
| F9 | **O portão de análise estática do Windows olha 2 de 7 arquivos, por lista escrita à mão** (`ci.yml` 1682: `display_adapter.cpp` e `selected_display_adapter_check.cpp`). `ls src/platform/win32/*.cpp \| wc -l` = **7** (o achado de 06/09 dizia 6; cresceu). **4 supressões `NOLINTNEXTLINE`**, e **2 delas** (`window_message_route.cpp:151`, `seat_adapter.cpp:73`) moram em arquivos que o portão nunca analisou. | `.github/workflows/ci.yml`, `grep -rn NOLINT src/platform/win32/` | Esta onda põe **mais dois ou três `.cpp` em `win32/`** (WGL). Deixá-los nascer sob um portão que não os vê é a forma exata do "passa verde e está quebrado". Fatia 0 (§3), antes de qualquer código Windows desta onda. **Não é trilha paralela** (ver §2). |
| F10 | **Lista fechada de dependência zero, lado Linux**: `PKG_CHECK_MODULES_ALLOWLIST = {"wayland-client"}` e a lista de sonames `{libwayland-client.so.0, libgcc_s, libstdc++, libm, libc}`. `Libs.private` do `.pc` é gerado. | `tests/tools/check_dep_zero.py` 173, 289; `cmake/glintfx.pc.in` 39 | `libEGL.so.1` e `libwayland-egl.so.1` passam a ser `DT_NEEDED` da biblioteca. **As duas são API do sistema pela mesma régua que já admitiu `libwayland-client`** (`ESCOPO.md` §6: "por EGL no Linux, por WGL no Windows", decisão do líder de 21/08). As duas listas fechadas crescem **com justificativa por linha**, e o `.pc` ganha `-lEGL -lwayland-egl` em `Libs.private`. Decisão D-W6b-3 formaliza. |
| F11 | **A recusa de janela 0x0 é decisão autônoma pendente de confirmação** (06/09 02:27), e o contrato do instante de `open()` também (06/09 03:26). | `DECISOES_AUTONOMAS.md` | Nada aqui os reabre. O contexto gráfico herda o tamanho de `pixel_size()` no instante em que abre. |
| F12 | **Os sete itens de W6b no `TODO.md`** (linhas 306, 308, 310, 311, 316, 360, 362): `LOOP-RUN` (17.67, PMU), `GL-CONTEXT` (10.00), `WL-KEYBOARD` (6.00), `WL-POINTER` (6.00), `WL-SCALE` (3.33), `WIN-GL` (23.00), `WIN-INPUT` (7.67). ⚠️ **A coluna `Pré-requisito` de `WIN-INPUT` diz `WIN-WINDOW, INPUT-EVENTS`, e `INPUT-EVENTS` é W7, ⏳ Pendente.** O briefing contou "0 bloqueados"; por dependência declarada, `WIN-INPUT` está **bloqueado** e numa onda **anterior** à do próprio pré-requisito (a forma exata do CHK-07 que o desvio 14 consertou para outras linhas). A própria linha, no texto de 02/09, diz que ele "anda em duas metades, cada uma junto do irmão do Wayland", o que implica pré-requisito `WIN-WINDOW, WIN-SEAT`, não `INPUT-EVENTS`. **A coluna e a onda se contradizem dentro da mesma linha.** | `TODO.md` 362 | Conserto é do orquestrador (a tabela é dele): ou o pré-requisito vira `WIN-WINDOW, WIN-SEAT` (leitura que eu recomendo, porque é o que o texto de 02/09 diz), ou a onda de `WIN-INPUT` vai para depois de `INPUT-EVENTS`. Nenhuma das duas muda a execução desta onda (§2: os itens de entrada não executam agora). |
| F13 | **`registry_smoke` imprime só a contagem de globals** ("catalog has 61 global(s)"), não a versão anunciada de cada um. | log do job `Container Wayland isolado`, run `34028525955` | A versão de `wl_compositor`/`wl_seat`/`xdg_wm_base` no `kwin --virtual` continua **não medida** (a W6a já dizia isso na §6 dela, e ninguém leu porque ninguém imprimiu). A sonda G-1 imprime a lista `interface@versão` inteira. |

**Busca prévia (L-43), com fonte e separada do medido em casa.** Procurei: laço principal com passo fixo, `frame callback` do Wayland, EGL sobre Wayland, criação de contexto WGL moderno, `swap control`, os callbacks de laço do SDL3, e as armadilhas de entrada (que não executam nesta onda mas influenciam o que o laço promete).

- **Laço com passo fixo e a "espiral da morte":** acumulador que consome passos de `dt` fixo, interpolando o resto; se o passo custa mais que `dt`, o acumulador cresce sem parar. Remédio universal: **limitar o tempo decorrido antes de acumular** (250 ms é o valor da referência) e aceitar perder simulação a manter a máquina travada ([Gaffer On Games, Fix Your Timestep](https://gafferongames.com/post/fix_your_timestep/), [GameDev.net, thoughts on the spiral of death](https://www.gamedev.net/blogs/entry/2262574-thoughts-on-fix-your-timestep-and-the-spiral-of-death/), [André Leite, Taming Time](https://andreleite.com/posts/2025/game-loop/fixed-timestep-game-loop/)). Técnica aprendida, não copiada: o laço **entrega o decorrido já limitado** e o acumulador fixo é um átomo puro separado, que o consumidor usa ou não (D-W6b-9).
- **Laço de render no Wayland:** `eglSwapBuffers` **bloqueia** esperando o `wl_surface.frame` quando a superfície está oculta (outro workspace, minimizada, coberta), por desenho do protocolo, e isso congela entrada, rede e tudo o mais do processo. A saída canônica é `eglSwapInterval(0)` **mais** gerência própria do `frame callback` ([emersion, Writing a Wayland rendering loop](https://emersion.fr/blog/2018/wayland-rendering-loop/), [wayland-book, Frame callbacks](https://wayland-book.com/surfaces-in-depth/frame-callbacks.html), [glfw #1350](https://github.com/glfw/glfw/issues/1350), [Mozilla 1542808, frame callbacks como fonte de vsync](https://bugzilla.mozilla.org/show_bug.cgi?id=1542808)). O Mesa, com intervalo 1, instala o callback ele mesmo e bloqueia; com 0, não instala ([patch Neil Roberts, eglSwapInterval](https://lore.freedesktop.org/wayland-devel/1382712617-20880-1-git-send-email-neil@linux.intel.com/)). **Isto é o requisito do líder de 28/08 ("janela oculta não computa nada"; "passo medido, nunca assumido") visto pelo lado do mecanismo**, e decide D-W6b-6.
- **EGL sobre Wayland:** `eglGetPlatformDisplay(EGL_PLATFORM_WAYLAND_KHR, wl_display*)` (EGL 1.5, sem extensão; medido 1.5 nesta máquina), `eglBindAPI(EGL_OPENGL_API)` é **por thread** e vale para o próximo `eglCreateContext`; perfil por `EGL_CONTEXT_OPENGL_PROFILE_MASK` com `EGL_CONTEXT_MAJOR/MINOR_VERSION` ([Khronos EGL_KHR_platform_wayland](https://registry.khronos.org/EGL/extensions/KHR/EGL_KHR_platform_wayland.txt), [eglIntro](https://registry.khronos.org/EGL/sdk/docs/man/html/eglIntro.xhtml)). `wl_egl_window_create(surface, w, h)` é da `libwayland-egl` (parte do projeto Wayland, mesma família de `libwayland-client`), e o tamanho dado é **em pixels do buffer**, então tem de ser `pixel_size()` e não `logical_size()`, casado com `wl_surface_set_buffer_scale` (obrigação escrita em D-W6a-17).
- **WGL moderno:** as funções `wglChoosePixelFormatARB`/`wglCreateContextAttribsARB` só existem via `wglGetProcAddress` **com um contexto legado corrente**, o que exige **janela descartável** com formato de pixel próprio; **`SetPixelFormat` só pode ser chamado UMA vez por janela** (documentação da Microsoft), então o formato ARB vai direto na janela real, nunca um formato legado antes; classe com `CS_OWNDC` (já é, F9 da W6a: `display_adapter.cpp:211`); `wglGetProcAddress` **não resolve funções do OpenGL 1.1** (`glGetString`, `glClear`, `glViewport`...), que vêm de `GetProcAddress(GetModuleHandleW(L"opengl32"))` ([nickrolfe, gist de contexto core sem biblioteca](https://gist.github.com/nickrolfe/1127313ed1dbf80254b614a721b3ee9c), [Mariusz Bartosik, OpenGL 4.x init](https://mariuszbartosik.com/opengl-4-x-initialization-in-windows-without-a-framework/), [SetPixelFormat, learn.microsoft.com](https://learn.microsoft.com/en-us/windows/win32/api/wingdi/nf-wingdi-setpixelformat)). **Ponto que o `gl_proc_address_fn` do carregador precisa saber:** um resolvedor que só chame `wglGetProcAddress` devolve nulo para ~40 funções 1.1 e o carregador reprova com `not_found`. O átomo `wgl_proc_address` faz os dois passos.
- **`swap control` no Windows:** `wglSwapIntervalEXT(1)` sincroniza ao monitor, **não ao DWM**; GLFW chama `DwmFlush()` antes de `SwapBuffers` quando o DWM está ativo, e há relatos de "1 vira adaptativo" em Windows 10 ([SDL #5797](https://github.com/libsdl-org/SDL/issues/5797), [WGL_EXT_swap_control](https://registry.khronos.org/OpenGL/extensions/EXT/WGL_EXT_swap_control.txt)). Janela **minimizada** no Windows: `SwapBuffers` não bloqueia (não há o problema do Wayland), mas desenhar para janela minimizada é CPU jogado fora; `IsIconic()` é a pergunta certa. Decide o lado Win32 de D-W6b-6: mecanismo diferente, efeito igual.
- **SDL3, os callbacks de laço:** `SDL_AppInit/AppIterate/AppEvent/AppQuit`, `AppIterate` "chamado repetidamente, possivelmente na taxa de atualização da tela ou noutra métrica que a plataforma dita", com `SDL_HINT_MAIN_CALLBACK_RATE` (Hz, "0" = o mais rápido possível, "waitevent" = só quando chegar evento). A razão declarada do desenho é plataforma que **é dona do laço** (iOS, Emscripten); onde não é, "fingimos com um laço simples" ([SDL3 README-main-functions](https://wiki.libsdl.org/SDL3/README-main-functions), [SDL_HINT_MAIN_CALLBACK_RATE](https://wiki.libsdl.org/SDL3/SDL_HINT_MAIN_CALLBACK_RATE)). Técnica aprendida: **os dois modos, laço do consumidor e laço da biblioteca, convivem**, e o segundo é açúcar sobre o primeiro (D-W6b-8). O `"waitevent"` do SDL é exatamente a forma "janela oculta não computa" quando a cadência é ditada pelo compositor.
- **Entrada (não executa nesta onda, mas a busca foi feita para o plano dela não nascer errado depois):** `wl_keyboard.keymap` é fd mapeado com `MAP_PRIVATE` desde a v7, `repeat_info` chega antes de qualquer `key` (v4+); `wl_pointer.frame` (v5) agrupa eventos, `axis_value120` (v8) é a roda de alta resolução ([wayland-book, Keyboard input](https://wayland-book.com/seat/keyboard.html), [wayland.app, wl_pointer](https://wayland.app/protocols/wayland#wl_pointer)); **`wp_cursor_shape_v1` é suportado por KWin, Mutter, Sway (v2), Hyprland, wlroots** e **pode ser misturado** com `wl_pointer.set_cursor` de imagem do cliente ([wayland.app, cursor-shape-v1](https://wayland.app/protocols/cursor-shape-v1), [Phoronix, Mutter](https://www.phoronix.com/news/GNOME-Mutter-Cursor-Shape)), o que responde a decisão 1 da L-36 sem `libwayland-cursor` nem parser de tema: o compositor desenha o tema do sistema, e o consumidor que quiser imagem própria usa `set_cursor`. No Windows, `WM_CHAR` vem de `TranslateMessage` e **não** convive com `RIDEV_NOLEGACY`; roda do mouse em múltiplos de `WHEEL_DELTA` (120), sem `LOWORD/HIWORD` para coordenadas com vários monitores ([Microsoft, WM_MOUSEWHEEL](https://learn.microsoft.com/en-us/windows/win32/inputdev/wm-mousewheel), [Molecular Musings, keyboard input](https://blog.molecular-matters.com/2011/09/05/properly-handling-keyboard-input/)). Fica registrado para o plano de `WL-KEYBOARD`/`WL-POINTER`/`WIN-INPUT`, quando o teto da L-32 cair.
- **RmlUi:** não tem laço nem contexto gráfico próprios (os backends dele recebem os dois de fora, SDL/GLFW/Win32); nada a aprender para esta onda. **SDL3 fonte:** não está nesta máquina; usei a documentação oficial. Declarado, não silenciado.

---

## 1. Item a item: efeito para o consumidor, dependência real, o que congela

| Item | O que o consumidor passa a conseguir fazer | Dependência declarada × real (conferida contra a árvore) | Congela? |
|---|---|---|---|
| `GL-CONTEXT` | Sobre uma janela aberta, obter um contexto OpenGL 3.3 core, torná-lo corrente, apresentar um quadro (`swap`) e resolver o endereço de qualquer função GL pelo nome. No Linux, o quadro apresentado acompanha a cadência real da tela e **nunca trava o programa quando a janela está oculta**. | Declarada `WL-WINDOW, GL-LOADER`; real: as duas ✅, **mais** `libEGL`/`libwayland-egl` como API do sistema (D-W6b-3) e a sonda G-1 (F4). | **SIM, porta de mão única nº 1:** o handle público `gltfx_gl_context` (nomes e tipos de retorno de `open`, `make_current`, `swap_buffers`, `proc_address`, `set_swap_interval`) e o value type `gltfx_gl_context_desc`. Revisão de API dedicada (PMU-GL, fatia 2). |
| `WIN-GL` | O mesmo, no Windows, **pela mesma chamada**, sem ramo por sistema no código do consumidor. | Declarada `WIN-WINDOW, GL-LOADER`; real: as duas ✅, mais o aparato Mesa do servidor (F3, já no ar) e `opengl32`/`gdi32` nas listas fechadas do Windows. | Não congela nada próprio: **satisfaz** a porta que `GL-CONTEXT` congela. Uma entrega só com ele (L-04, "só entrego quando os dois sistemas tiverem"). |
| `LOOP-RUN` | Escrever o programa como "prepare, depois a cada quadro faça isto" e deixar a biblioteca bombear eventos, medir o tempo decorrido (limitado, nunca negativo), dizer se este quadro deve ser desenhado, e dormir quando a janela está oculta em vez de girar a CPU. Quem prefere o próprio `while` chama `step()` e recebe o mesmo. Passo fixo com interpolação é um átomo à parte que o consumidor usa se quiser. | Declarada `WL-WINDOW, CORE-TIME`; real: `WL-WINDOW` ✅, `CORE-TIME` ✅ **mas sem leitura de relógio** (F7, adição compatível), e **`GL-CONTEXT`/`WIN-GL` desta onda**: o laço precisa do `swap` para "apresentar na cadência da tela" ter significado, e o `should_render` nasce do resultado do `swap` (D-W6b-6). O desvio 2 do `TODO.md` ("`LOOP-RUN` antes de `GL-CONTEXT`") foi escrito quando `GL-CONTEXT` estava travado por decisão pendente; **hoje é o inverso**: o laço depende do contexto. Corrigir a coluna é do orquestrador. | **SIM, porta de mão única nº 2:** `gltfx_loop` (`open`, `step`, `run`), o value type `gltfx_frame_tick`, `gltfx_now()` no header de tempo, e `gltfx_fixed_step` no núcleo. Revisão de API dedicada (PMU-LOOP, fatia 6). **Em série, depois da PMU-GL**, nunca em paralelo (regra da casa: no máximo duas por onda, em série). |
| `WL-KEYBOARD` | Receber teclas do teclado no Linux (com o keymap do compositor guardado para a trilha de keymap). | Declarada `WL-SEAT` ✅. Real: idem, **mais** `WL-DISPLAY-BACKEND-SEAT` (o seat não está na composição do display; entra quando a entrada entrar, `tests/parity_absences.txt`). | Congelaria nada público (a fila e os tipos de evento são de `INPUT-EVENTS`, W7). **NÃO EXECUTA NESTA ONDA** (§2). |
| `WL-POINTER` | Receber ponteiro, botões, rolagem e escolher o cursor (tema do sistema ou imagem própria, decisão 1 da L-36). | Declarada `WL-SEAT` ✅. Real: idem, mais `WL-DISPLAY-BACKEND-SEAT` e o binding de `cursor-shape-v1` (F8). | Idem. **NÃO EXECUTA NESTA ONDA** (§2). |
| `WL-SCALE` | `pixel_size()` correto em tela com escala fracionária (150%), o caso da máquina do líder. | Declarada `WL-WINDOW` ✅. Real: idem, mais dois protocolos (`fractional-scale-v1`, `viewporter`) e a costura com o contexto desta onda (o `wl_egl_window` tem de ser redimensionado quando a escala muda, D-W6b-5). | Não congela (a assinatura pública já existe). **NÃO EXECUTA NESTA ONDA** (§2), mas D-W6b-5 deixa a costura pronta para ele. |
| `WIN-INPUT` | Teclado e mouse no Windows, em duas metades, cada uma com o irmão do Wayland. | Declarada `WIN-WINDOW, INPUT-EVENTS`; **contradição interna** (F12): `INPUT-EVENTS` é W7 e pendente. Real, pela própria descrição de 02/09: `WIN-WINDOW, WIN-SEAT`, as duas ✅. | Idem `WL-KEYBOARD`. **NÃO EXECUTA NESTA ONDA** (§2), e a linha precisa de conserto do orquestrador antes de qualquer contagem de "bloqueados" ser verdadeira. |

---

## 2. A ordem, o caminho crítico, e o que a L-32 faz com quatro dos sete itens

### 2.1 A decisão que muda a contagem: esta onda executa TRÊS itens, não sete

**A L-32 é lei do líder com número fixado por ele** (*"caminho principal sempre, mais no máximo UMA trilha paralela"*; *"o teto cai quando a demo estiver verde"*) e **o modo autônomo não dá ao C-level a caneta das leis** (L-34, lista fechada do que o `fable` não herda). A lei define caminho principal como *"a cadeia até uma janela desenhando: janela, contexto gráfico, render 2D, loop, demo"* e lista **teclado** entre as trilhas paralelas. O slot único de trilha paralela é do **RCSS** por decisão dele de 22/08, reconfirmada com fato novo à vista.

Logo, **dos sete itens de W6b, três são caminho principal (`GL-CONTEXT`, `WIN-GL`, `LOOP-RUN`) e quatro não são (`WL-KEYBOARD`, `WL-POINTER`, `WIN-INPUT`, `WL-SCALE`)**. Os quatro estão em W6b por dominó do corte da W6 (desvio 14 do `TODO.md`, 05/09), que os **posicionou por dependência**; posicionar não é autorizar a executar. A frase do líder que nomeou esta onda foi *"a placa gráfica mais o laço"* (`DECISOES_AUTONOMAS.md`, 05/09 13:11 e 15:00). **Executar os quatro agora seria abrir uma segunda trilha paralela com a do RCSS ainda ocupando o slot, contra a letra da L-32.** Não faço isso, e não é decisão que eu possa tomar: é lei.

**Consequências, declaradas para não virarem surpresa:**

1. **`W6b` fecha com `GL-CONTEXT`, `WIN-GL` e `LOOP-RUN` ✅** e os outros quatro continuam ⏳. Para a coluna `Onda` não mentir ("W6b fechada" com quatro pendentes dentro), **recomendo ao orquestrador re-ondar os quatro para depois de `DEMO-1` (W9)**, com nota datada citando a L-32. A tabela é dele; eu só recomendo.
2. `INPUT-EVENTS` (W7) depende de `WL-KEYBOARD`, `WL-POINTER` e de `KEYMAP-*` (que já estão bloqueados pela mesma lei). **W7 executa `R2D-BATCH`** (caminho principal) e `INPUT-EVENTS` espera. Coerente com a cadeia W6b → W7 → W8 (demo), depois da qual o teto cai e a entrada flui inteira.
3. Se o líder quiser os quatro agora, é ele que suspende a L-32 para esta onda; **a pergunta vai a ele só se ele mesmo reabrir**, porque em modo autônomo *"parar para perguntar é uma forma de não trabalhar"* (registro de 05/09 15:00). Enquanto isso, a lei vale como está escrita.

### 2.2 A trilha paralela: `WIN-LINT-ENUM` NÃO é trilha paralela, é a fatia 0 do caminho principal

O briefing recomendou `WIN-LINT-ENUM` como "a paralela". Discordo da classificação, não da urgência: **a L-32 fala de trilha paralela de produto** ("núcleo puro independente: estilo, mapa, teclado, PNG, fonte"). Um portão de CI que vigia **exatamente o diretório onde esta onda vai pôr código** não é trilha: é pré-condição do caminho principal, do mesmo jeito que P-0 foi na W6a. Vai como **fatia 0**, antes do primeiro `.cpp` de WGL, com vermelho de estreia. A trilha paralela continua sendo a do RCSS, que anda no slot dela sob o orquestrador; **eu não a toco neste plano**.

### 2.3 Caminho crítico, nomeado

```
0 (WIN-LINT-ENUM) ──┐
1 (G-1 sonda EGL)  ──┼─→ 2 (C-A comum + PMU-GL) → 3 (W-EGL) ─┬→ 5 (P-GL paridade) → 6 (L-A comum + PMU-LOOP) → 7 (L-W/L-X wait_events) → 8 (P-LOOP) → 9 (fechamento)
                     │                            → 4 (X-WGL, Lote Windows) ─┘
```

**Crítico:** 2 → 3 → 5 → 6 → 8 → 9. As fatias 0, 1 andam no primeiro dia em paralelo com o começo da 2; a fatia 4 (Windows, às cegas) é escrita **enquanto a 3 anda no Linux** e empurrada junto com a 3 no mesmo push (o regime de lotes da W6a, D-W6a-22, continua). A 7 tem os dois lados pequenos e vai num lote só.

**Teto de 4 agentes vivos (L-11 global):** dois implementadores (um Linux, um Windows-às-cegas), um revisor, o orquestrador. Nunca mais.

**Regime de ramo (D-W5-12 do líder, aplicado à W6a):** ramo `w6b-placa-e-laco` a partir de `main`, pedido de incorporação aberto no primeiro push, merge só no fechamento com tudo verde (L-11 emenda de 28/08; a autorização de 01/09 cobre o merge; tag continua fora).

---

## 3. As decisões desta onda (D-W6b-1 a D-W6b-12), todas do CTO, para o líder reverter só as minhas

| # | Decisão | Opções na mesa | Escolhida e porquê | Mão única |
|---|---|---|---|---|
| D-W6b-1 | **Onde mora o contexto gráfico** | (a) dentro de `gltfx_window` ("a janela sabe desenhar"); (b) handle próprio `gltfx_gl_context`, aberto **a partir** de uma janela, em diretório público próprio `include/glintfx/platform/gl/`; (c) em `include/glintfx/render/` | **(b)**. (a) é a fachada que vira dona de tudo (L-19, armadilha 1: "um handle por assunto"). (c) mistura a camada que **toca o SO** (EGL/WGL são API de sistema) com o render, que é núcleo sobre a tabela de funções; o portão de camada existe para separar exatamente isso. Um handle, um assunto: "contexto GL sobre uma janela". | **Sim** (nome e lugar do header) |
| D-W6b-2 | **O que o handle congela na v1** | mínimo: `open(window, desc)`, `is_open`, `make_current`, `swap_buffers`; mais `proc_address(name)`; mais `set_swap_interval(n)` | **Os seis.** `proc_address` entra por **duas razões que não são "alguém usa"**: (1) é o que permite ao **teste de paridade** chamar `glGetIntegerv`/`glClear`/`glReadPixels` pela API pública **sem `#if` por sistema** (sem ele, o teste teria de escolher `eglGetProcAddress`/`wglGetProcAddress` à mão, o ramo que a L-04 proíbe); (2) regra de paridade da frase-guarda: SDL e GLFW expõem o equivalente, e um framework 2D com GL que esconde o GL de quem quer um shader próprio é escopo cortado por presunção. `set_swap_interval` entra porque o laço precisa dele (D-W6b-6) e porque sem ele o consumidor que não usa o laço não tem como escolher. **Fora da v1, adição compatível depois:** contextos compartilhados, `debug output`, MSAA, sRGB, mais de uma thread (v1 é **single-thread**, escrito no header). | **Sim** |
| D-W6b-3 | **`libEGL` e `libwayland-egl` sob a L-07** | (a) API do sistema, como `libwayland-client`; (b) pedir ao líder | **(a)**, e é aplicação de decisão dele, não decisão nova: `ESCOPO.md` §6 diz *"por EGL no Linux, por WGL no Windows"* (21/08). EGL é a interface de sistema que dá GL a uma superfície Wayland; `libwayland-egl` é do próprio projeto Wayland (mesmo pacote `wayland-devel`, F4). As listas fechadas (`check_dep_zero.py`, `.pc`) crescem com justificativa por linha. **Registrado para confirmação retroativa** porque a lista de sonames é lista fechada da lei. | Não |
| D-W6b-4 | **Formato de pixel e atributos do contexto** | (a) RGBA8 sem depth nem stencil; (b) RGBA8 + stencil 8; (c) RGBA8 + depth 24 + stencil 8 | **(b)**. Render 2D não usa depth (a ordem é chave estável de `R2D-BATCH`, decisão do líder de 27/08). Stencil 8 é o mecanismo barato de **recorte** (`overflow`/clipping do `gfui`, trilha de layout) e pedir depois mudaria o formato de pixel, que no Win32 **só pode ser fixado uma vez por janela** (busca, §0). Contexto **3.3 core, sem `forward-compatible`** (a flag derruba drivers que dão 3.3 por compatibilidade sem ganho nenhum). Igual nos dois lados por construção: um átomo puro `gl_context_desc` traduzido por cada adaptador. | Não (interno), mas o **efeito** "tem stencil" entra no header |
| D-W6b-5 | **Quem redimensiona o buffer quando `pixel_size()` muda** (escala, configure) | (a) o consumidor chama um `resize`; (b) o contexto confere `pixel_size()` da janela a cada `swap_buffers()` e redimensiona o `wl_egl_window` (e chama `wl_surface_set_buffer_scale`) antes do próximo quadro; (c) a janela avisa o contexto por callback | **(b)**. (a) é obrigação que todo consumidor esquece e o resultado é buffer esticado. (c) põe o contexto dentro da janela (D-W6b-1 recusou). (b) custa uma comparação de dois inteiros por quadro. No Win32 não há o que fazer: o DC segue o `HWND`. **Cumpre a obrigação de D-W6a-17** ("`GL-CONTEXT` chama `set_buffer_scale` com o mesmo inteiro que a janela reporta"). A política "redimensiona ou não" é átomo puro (`gl_surface_size_policy`), testada nos cinco com valores sintéticos (escala 2), porque no servidor os dois números nunca mudam (o "passa verde e está quebrado" nº 4 da W6a, de novo). | Não |
| D-W6b-6 | **Como `swap_buffers` se comporta com janela oculta, e de onde vem "este quadro deve ser desenhado"** | (a) `eglSwapInterval(1)` e deixar bloquear (o padrão do Mesa); (b) intervalo 0 no Wayland **e** o adaptador gerencia o `wl_surface.frame` sozinho: `swap_buffers` espera o callback do quadro anterior **com orçamento** (100 ms) e, se não vier, devolve `skipped_hidden` sem apresentar; no Win32, `IsIconic()` antes de `SwapBuffers` devolve o mesmo `skipped_hidden`; (c) o laço decide tudo e o contexto é burro | **(b)**. (a) é a armadilha documentada pela busca: congela o programa inteiro quando a janela sai de vista. (c) deixa o consumidor **sem laço** exposto a (a). Com (b), `swap_buffers()` devolve `gltfx_rslt<gltfx_present_outcome>` (`presented` / `skipped_hidden`), **mesmo enum nos dois sistemas, mecanismo diferente** (frame callback × `IsIconic`), e o laço deriva `should_render` do resultado anterior. Isto é o requisito do líder de 28/08 (*"janela oculta não computa nada"*, *"passo medido, nunca assumido"*) resolvido no lugar onde ele mesmo disse que se resolve: no laço e na apresentação. ⚠️ **Não medido:** se o `kwin --virtual` para de mandar frame callbacks para toplevel minimizado (`xdg_toplevel.set_minimized`). Teste `loop_hidden_test` imprime primeiro e assevera depois (memória da casa: portão que congela fato do ambiente). | O tipo de retorno de `swap_buffers` **é mão única** (entra na PMU-GL) |
| D-W6b-7 | **`set_swap_interval` default** | (a) 1 nos dois; (b) 0 nos dois; (c) 1 no Win32 e 0 no Wayland | **(a) como valor que o consumidor lê; o mecanismo por baixo é (c)** e isso fica escrito: no Wayland o "intervalo 1" é implementado pelo frame callback do adaptador (D-W6b-6), nunca pelo `eglSwapInterval(1)` do Mesa, porque o do Mesa bloqueia oculto. O que o consumidor observa é o mesmo: com intervalo 1, um `swap` por quadro apresentado; com 0, o mais rápido possível (e no Wayland o Mesa **ainda** limita o callback a um por vsync, fato da busca, registrado no header). | Não |
| D-W6b-8 | **A forma do laço** | (a) só o `while` do consumidor + utilitários; (b) só `run(callbacks)` (SDL3 puro); (c) `gltfx_loop` com `step()` **e** `run()` sobre `step()` | **(c)**, técnica aprendida do SDL3 (os dois modos convivem; `run` é açúcar de laço simples onde a plataforma não é dona do laço, e **nenhuma das duas nossas é**). `open(display&, window&, gl_context&)`: o laço **exige** contexto na v1, porque "apresentar na cadência da tela" é o que ele promete e sem `swap` a promessa não tem objeto; laço sem contexto é adição compatível se um dia fizer sentido. Callbacks de `run()` são `std::function` (CONTRACT §11.1 permite; nunca `virtual` em tipo público, L-19). `step()` devolve `gltfx_frame_tick`: `elapsed` (limitado a 250 ms, nunca negativo, provado por `frame_tick_state_test`), `now`, `frame_index`, `should_render`. **Ordem dentro de `step()`:** pump → se o `swap` anterior foi `skipped_hidden`, `wait_events(orçamento)` (dorme) → lê relógio → monta o tick. | **Sim** |
| D-W6b-9 | **Passo fixo** | (a) dentro do laço, obrigatório; (b) átomo puro no núcleo (`gltfx_fixed_step`: `accumulate(elapsed)` devolve quantos passos e o `alpha`), opcional; (c) fora do escopo | **(b)**. (a) impõe política de simulação a todo consumidor (L-02: regra de aplicação não entra na lib); (c) empurra cada consumidor a reescrever o mesmo acumulador com a mesma espiral. É núcleo puro (só aritmética sobre `gltfx_duration`), testado nos cinco sistemas com o caso da espiral (passo maior que `dt` durante 10 quadros → o limitador de D-W6b-8 segura o acumulador). | **Sim** (value type de núcleo com layout visível, L-19) |
| D-W6b-10 | **`gltfx_now()`** | (a) adição a `include/glintfx/core/time.hpp`; (b) função na camada de plataforma | **(a)**. É `std::chrono::steady_clock`, biblioteca padrão, e o núcleo pode usá-la (o portão de camada varre API de SO, não `<chrono>`). Fica ao lado do tipo que devolve. Adição compatível a uma superfície congelada (não reabre `CORE-TIME`). Prova: `time_test` ganha "duas leituras seguidas, a diferença nunca é negativa" (a mesma propriedade que a suíte já prova com sequência sintética, agora com o relógio real, nos cinco). | **Sim** (assinatura pública nova) |
| D-W6b-11 | **`wait_events(orçamento)` nos adaptadores de display** | (a) público em `gltfx_display`; (b) interno, exigido pela porta `display_backend_port`, alcançado pela fachada do laço via passkey | **(b)**. Não é o consumidor que precisa dormir com orçamento; é o laço. Público seria crescer a superfície congelada de `gltfx_display` por conveniência interna. Wayland: `poll` no fd com timeout positivo (o mesmo `prepare_read` de sempre, só o timeout muda); Win32: `MsgWaitForMultipleObjectsEx(0, nullptr, timeout, QS_ALLINPUT, ...)`. A porta ganha a exigência, o `fake_display_adapter` ganha a implementação, o `static_assert` dos dois seletores prova que os adaptadores reais satisfazem. | Não |
| D-W6b-12 | **`window_smoke` morre nesta onda?** | (a) fica; (b) aposentado quando `gl_context_parity_test` (container) provar a mesma mutação (remover `ack_configure` → o `swap` do EGL anexa buffer e o compositor mata a conexão com `xdg_surface` como `rejected_value`) | **(b)**, que é exatamente a condição de morte escrita na linha dele em `tests/parity_exceptions.txt` ("quando W6b der a `gltfx_window` uma superfície pública de buffer/contexto gráfico"). A linha, o fixture, a cópia no `Containerfile`, o `docker exec` no `ci.yml` e a linha no script de preparo saem **no mesmo commit** da fatia 5, depois de o revisor executar a mutação e ver o novo teste vermelho. Antes disso, nada sai. | Não |

**O que estas decisões NÃO reabrem (do líder ou de plano anterior, e não toco):** OpenGL 3.3 core (L-31); dois tamanhos com unidade no nome (decisão 15 da W6a); janela 0x0 recusada (06/09, pendente de confirmação, mantida); ramo com PR (D-W5-12); Windows em lotes (D-W6a-22); as três proibições da L-09.

---

## 4. As fatias, cada uma com o teste vermelho, e a coluna que o portão cobra

Legenda: **Prova Linux** e **Prova Windows** são nomes de `ctest` ou de fixture do container (que P-0 torna nome de inventário). **Par** diz como o portão de paridade vê a fatia: `mesmo nome`, `apelido` (linha em `parity_aliases.txt`), `exceção` (linha em `parity_exceptions.txt` apontando para item ABERTO ou `SEM-PENDENCIA`), ou `nenhum necessário`.

| # | Fatia | Lado | Nasce / muda | Teste vermelho de estreia (L-20, o que falha ANTES) | Prova Linux | Prova Windows | Par no portão |
|---|---|---|---|---|---|---|---|
| 0 | **G-0 `WIN-LINT-ENUM`** (fatia dentro do achado da INBOX; sem linha própria na tabela, o orquestrador decide se abre) | infra | `.github/workflows/ci.yml`: o passo "clang-tidy - src/platform/win32" deixa de listar arquivos à mão e passa a **enumerar** `src/platform/win32/*.cpp` por `Get-ChildItem`, conferir que **cada um** aparece em `build-lint/compile_commands.json` (piso: contagem do diretório = contagem na base, ambas impressas, zero reprova, L-40), e rodar o `clang-tidy` sobre a lista enumerada. | Rodar o passo novo contra a árvore de hoje: os **5 arquivos nunca analisados** entram; se algum reprovar, é achado real e se conserta (nunca se suprime). **Vermelho de estreia obrigatório:** num run de teste, apagar um `.cpp` do `compile_commands.json` de mentira e ver o passo reprovar por contagem. | (portão do lado Windows; sem par de nome, é infra) | o próprio passo | nenhum necessário (não é `ctest`) |
| 1 | **G-1 sonda EGL do container** (fatia dentro de `GL-CONTEXT`; gêmea da X-GL-0) | Linux | `tests/container/egl_probe_smoke.cpp` (sobre a janela real de `window_smoke`: `eglGetPlatformDisplay(EGL_PLATFORM_WAYLAND_KHR)`, `eglInitialize`, imprime `EGL_VERSION`/`EGL_VENDOR`/`EGL_CLIENT_APIS`, `eglBindAPI(EGL_OPENGL_API)`, escolhe config RGBA8+stencil8, cria contexto `3.3 core`, `make current`, imprime `GL_VENDOR`/`GL_RENDERER`/`GL_VERSION`/`GL_MAJOR/MINOR_VERSION`/`GL_CONTEXT_PROFILE_MASK`; **nada asseverado além de "rodou"**); `tests/container/registry_smoke.cpp` passa a imprimir `interface@versão` de cada global (F13); `tests/container/Containerfile` (uma linha de `g++` a mais, `-lEGL -lwayland-egl`), `tests/container/prepare_arch_ports_fixture.sh`, `.github/workflows/ci.yml` (um `docker exec` a mais, nome no inventário via P-0), `tests/parity_exceptions.txt` (`egl_probe_smoke\|windows\|nenhum\|SEM-PENDENCIA`: sonda de ambiente, mesma categoria de `win32_runner_probe_test`). | O fixture não compila enquanto a linha do `Containerfile` não existir (vermelho por construção); e a **árvore de decisão está fixada antes do dado**, §5. | `egl_probe_smoke` (container) | (sonda do ambiente Linux; o par de papel é `win32_runner_probe_test`, já excetuada) | exceção `SEM-PENDENCIA` |
| 2 | **C-A comum: header público, value types, porta, fachada, política de versão — e a PMU-GL** | comum (5 sistemas) | `include/glintfx/platform/gl/context.hpp` (`gltfx_gl_context_desc` {`swap_interval`}, `gltfx_present_outcome`, `gltfx_gl_context` com os seis métodos de D-W6b-2); `src/platform/gl/gl_context_desc_validation.{hpp,cpp}` (átomo puro: intervalo em {0,1}; L-22, `invalid_argument` com `rejected_value == "swap_interval"`); `src/platform/gl/gl_version_policy.{hpp,cpp}` (átomo puro: aceita `major.minor >= 3.3` **e** bit core do profile mask, recusa o resto com `platform_failure` e `rejected_value == "gl_version"`); `src/platform/gl/gl_surface_size_policy.{hpp,cpp}` (átomo puro de D-W6b-5: dado `pixel_size` da janela e tamanho atual do buffer, decide redimensionar); `include/glintfx/platform/gl/gpu_kind.hpp` (`gltfx_gpu_kind` {`unknown`, `software`, `shared`, `dedicated`} e `gltfx_gpu_preference` {`no_preference`, `prefer_dedicated`, `prefer_shared`}, §10, D-W6b-13/14); `src/platform/gl/gpu_kind_state.{hpp,cpp}` (átomo puro: o que cada adaptador soube × o que o consumidor lê; `unknown` é o default, nunca presumido); `src/platform/port/gl_context_adapter_port.hpp` (concept estreito: `open/close/is_open/make_current/swap_buffers/proc_address/set_swap_interval/gpu_kind/gpu_name`); `src/platform/gl/gl_context_impl.hpp` e `src/platform/gl/gl_context_facade.cpp` (PIMPL sobre `selected_gl_context_adapter`, passkey para alcançar o adaptador da janela); `tests/header_hygiene_test.cpp` (o header novo entra na cobertura, gate mecânico); `tests/fake/fake_gl_context_adapter.hpp`. **A revisão de API dedicada (PMU-GL) acontece aqui**, entregável: `docs/gl-loop-portability-matrix.md` nasce com as linhas de contexto (mesma forma da matriz da janela: efeito observável × Linux × Windows × status com o teste que prova, sem dígito volátil). | `gl_version_policy_test` (7 casos: 3.3 core aceita; 4.6 core aceita; 3.3 **compat** recusa; 3.2 core recusa; 2.1 recusa; 3.3 sem bit nenhum recusa; enumeração fechada {major<3, ==3 & minor<3, ==3 & minor>=3, >3} × {core, compat, nenhum} = 12 células com contagem impressa, zero reprova); `gl_context_desc_validation_test` (3 + 3: intervalo 0 ok, 1 ok, 2 recusa com o campo nomeado; `gpu_preference` `no_preference` ok, `prefer_dedicated` e `prefer_shared` **recusados** com `rejected_value == "gpu_preference"`, D-W6b-14); `gpu_kind_state_test` (4: nasce `unknown`; adaptador que só soube "software" → `software`; nome vazio permanece vazio, nunca inventado; enumeração fechada dos 4 valores com contagem impressa); `gl_surface_size_policy_test` (4: igual não redimensiona; escala 2 dobra; volta a 1; janela 0x0 nunca chega aqui, é recusada antes — asserção de que a política **não** aceita zero); `display_connection_fake_test`/`public_name_collision_test` cobrem o nome novo. Todos os quatro nascem vermelhos por **compilação** (o header não existe) e depois por valor. | os quatro, mesmo nome, mesmo arquivo, **sem `#if`** | os quatro, idem | mesmo nome |
| 3 | **W-EGL: o adaptador Wayland** | Wayland | `src/platform/wayland/egl_context_adapter.{hpp,cpp}` (átomos: `create_egl_display`, `choose_config`, `create_context`, `create_egl_window` (em **pixels**, `pixel_size()`), `attach_frame_listener`, `swap_buffers` com o orçamento de D-W6b-6, `proc_address` = `eglGetProcAddress`, `close` em ordem inversa); `src/platform/wayland/frame_callback_sequence.{hpp,cpp}` (átomo puro Wayland: "há callback pendente? orçamento esgotou? → `presented`/`skipped_hidden`", sem EGL, testável com proxy nulo); `src/platform/wayland/selected_gl_context_adapter.hpp` e `src/platform/wayland/selected_gl_context_adapter_check.cpp` (`static_assert` da porta); `src/platform/wayland/window_adapter.hpp` ganha `toplevel()` (para a fatia 8 pedir `set_minimized`; uma linha); `cmake/GlintfxEgl.cmake` (`pkg_check_modules` de `egl` e `wayland-egl`, mesma forma do `GlintfxWaylandProtocols.cmake`); `src/platform/wayland/CMakeLists.txt`; `cmake/GlintfxInstall.cmake` (`Libs.private` ganha `-lEGL -lwayland-egl`); `tests/tools/check_dep_zero.py` (allowlist de `pkg_check_modules` ganha `egl`/`wayland-egl`; sonames ganham `libEGL.so.1`/`libwayland-egl.so.1`, **uma justificativa por linha**, D-W6b-3); `tests/tools/check_port_privacy.sh` (lista fechada ganha `wayland_egl_context_adapter`). | `frame_callback_sequence_test` (5 casos com proxy nulo: sem callback pendente → apresenta; callback chegou → apresenta e rearma; orçamento esgotado → `skipped_hidden`; dois `done` seguidos não contam duas vezes; **o teste que prova que o orçamento é lido e não presumido**: orçamento 0 com callback pendente → `skipped_hidden` imediato). Vermelho antes: o header não existe. `dep_zero_test` fica **vermelho** no primeiro configure com EGL ligado (a soname nova não está na lista) — esse vermelho é o vermelho de estreia da lista fechada crescer, e se conserta com a linha justificada, nunca afrouxando o portão. | `frame_callback_sequence_test` (ctest); `gl_context_parity_test` (container, fatia 5) | mecanismo inexistente (Win32 não tem frame callback; o efeito `skipped_hidden` é provado por `win32_iconic_present_test`, fatia 4) | `frame_callback_sequence_test\|windows\|win32_iconic_present_test\|WIN-GL` (apelido depois da fatia 4; até lá, exceção apontando para `WIN-GL` aberto) |
| 4 | **X-WGL: o adaptador Win32** (Lote Windows, escrito às cegas contra a documentação da Microsoft, empurrado junto com a fatia 3) | Windows | `src/platform/win32/wgl_extension_loader.{hpp,cpp}` (átomo: janela descartável com classe por instância, contexto legado, `wglGetProcAddress` de `wglChoosePixelFormatARB`/`wglCreateContextAttribsARB`/`wglSwapIntervalEXT`, tudo destruído antes de devolver os três ponteiros); `src/platform/win32/wgl_proc_address.{hpp,cpp}` (átomo: `wglGetProcAddress` e, se nulo ou sentinela {0,1,2,3,-1}, `GetProcAddress(GetModuleHandleW(L"opengl32"))`, a armadilha da busca); `src/platform/win32/wgl_context_adapter.{hpp,cpp}` (átomos: `set_pixel_format_once` (ARB, **uma vez**, D-W6b-4), `create_context` 3.3 core, `make_current`, `swap_buffers` com `IsIconic()` → `skipped_hidden` (D-W6b-6), `set_swap_interval`, `close`); `src/platform/win32/selected_gl_context_adapter.hpp`, `src/platform/win32/selected_gl_context_adapter_check.cpp`; `src/platform/win32/CMakeLists.txt` (linka `opengl32`, `gdi32`); `tools/ci/check-dep-zero-win.ps1` (lista fechada de importações ganha `OPENGL32.dll` e `GDI32.dll` com justificativa por linha); `tools/ci/check-port-privacy-win.ps1` (adaptador novo). **`check_no_x11.py` varre: nenhum comentário destes arquivos cita termo proibido.** | `win32_wgl_proc_address_test` (sem contexto moderno: `glGetString` resolve pelo caminho de `GetProcAddress`; nome inexistente devolve nulo; **o teste que morde:** uma versão que só chama `wglGetProcAddress` devolve nulo para `glGetString`, e é isso que o caso assevera contra); `win32_wgl_extension_loader_test` (no executor com o aparato Mesa: os três ponteiros não nulos; a janela descartável **não existe mais** depois, `IsWindow` falso); `win32_iconic_present_test` (janela real + contexto real: `ShowWindow(SW_MINIMIZE)` → `swap_buffers` devolve `skipped_hidden` e **não chama `SwapBuffers`** (contador no adaptador, lido pelo teste); `SW_RESTORE` → `presented`). Vermelhos: compilação no servidor (não há compilador do servidor aqui; o cruzado local pega parte, `WIN-CROSS-STAGE`), depois valor. | `frame_callback_sequence_test` (fatia 3) | os três acima | `win32_wgl_proc_address_test\|linux\|nenhum\|SEM-PENDENCIA` (a armadilha das funções 1.1 é **só do WGL**: `eglGetProcAddress` resolve tudo, não há par de mecanismo nem de efeito); `win32_wgl_extension_loader_test\|linux\|egl_probe_smoke\|WIN-GL` apelido (os dois provam "o sistema entrega o caminho moderno de contexto"); `win32_iconic_present_test` apelido com `frame_callback_sequence_test` (acima) |
| 5 | **P-GL: teste de paridade do contexto, e a morte de `window_smoke`** | comum | `tests/parity/gl_context_parity_test.cpp`, **um arquivo, só API pública, sem `#if`**: abre display, janela 640x480, contexto; `make_current` ok; `proc_address("glGetIntegerv")` não nulo; `GL_MAJOR_VERSION`/`GL_MINOR_VERSION`/`GL_CONTEXT_PROFILE_MASK` **asseverados** pela política (≥3.3, core) e **impressos** como `MEASURED gl_context_parity_test.gl_major`/`gl_minor`/`renderer_hash`; `glClearColor(0.25,0.5,0.75,1)`, `glClear`, `swap_buffers` == `presented` (nos dois executores a janela está visível; **se o `kwin --virtual` devolver `skipped_hidden`, é medição, não falha do teste: o primeiro run imprime e a asserção sobe depois**), `glReadPixels(0,0,1,1)` **do back buffer antes do swap** devolve os bytes (64,128,191,255) com tolerância 1 (a **prova de pixel**, portável, sem screenshot); desc com `swap_interval = 2` recusada com o campo nomeado; fecha em ordem. No Windows, `ctest` comum nos dois modos (linka a fachada: o par do risco nº 1 da W6a para o modo estático); no Linux, fixture do container com o **mesmo nome** (estagiado, compilado no `Containerfile`, `docker exec` no `ci.yml`, nome no inventário). `tests/measured_exceptions.txt` (`gl_context_parity_test.renderer_hash\|ambos\|llvmpipe × D3D12, ambiente; o comportamento da biblioteca é provado pela asserção de versão/perfil do mesmo teste\|SEM-PENDENCIA`). **Aposentadoria (D-W6b-12):** `tests/container/window_smoke.cpp` sai; saem a linha dele em `tests/parity_exceptions.txt`, a cópia em `tests/container/prepare_arch_ports_fixture.sh`, o bloco no `tests/container/Containerfile` e o `docker exec` no `.github/workflows/ci.yml` — **no mesmo commit**, só depois da mutação abaixo. | Vermelho de estreia: a fachada não existe (fatia 2 entrega header; o teste linka e falha antes da 3/4). **Mutação obrigatória do revisor, no container:** remover o `ack_configure` do adaptador de janela; o `swap` do EGL anexa buffer e faz commit, o compositor mata a conexão, e `gl_context_parity_test` tem de reprovar com `rejected_value == "xdg_surface"`. **Só então** `window_smoke` sai. Segunda mutação: `pixel_size` copiado de `logical_size` (W6a nº 4) — `gl_surface_size_policy_test` reprova. Terceira: `swap_buffers` sem `IsIconic` — `win32_iconic_present_test` reprova. | `gl_context_parity_test` (container) | `gl_context_parity_test` (ctest) | mesmo nome, via P-0 |
| 5b | **G-KIND: a placa compartilhada × dedicada, os dois lados juntos** (fatia dentro de `GL-CONTEXT`/`WIN-GL`, ordem do líder de 06/09, §10) | Wayland + Windows | `src/platform/wayland/egl_device_query.{hpp,cpp}` (átomo: `EGL_EXT_device_query` → `EGL_DEVICE_EXT`; `EGL_MESA_device_software` → `software`; `EGL_EXT_device_drm` → caminho `/dev/dri/cardN`); `src/platform/wayland/drm_gpu_kind.{hpp,cpp}` (átomo puro sobre o que o kernel responde: `amdgpu` `AMDGPU_IDS_FLAGS_FUSION` → `shared`, senão `dedicated`; `i915`/`xe` região de memória de classe `DEVICE` → `dedicated`, sem ela → `shared`; `nvidia`/`nouveau` → `dedicated`; driver não listado → `unknown`; quem **lê** o ioctl é átomo separado de quem **classifica**, e só o segundo é testado nos cinco); `src/platform/win32/dxgi_gpu_kind.{hpp,cpp}` (átomo puro sobre `DXGI_ADAPTER_DESC1` + a ordem de `IDXGIFactory6::EnumAdapterByGpuPreference`: flag `SOFTWARE` → `software`; dois ou mais adaptadores de hardware → o primeiro por `HIGH_PERFORMANCE` é `dedicated` e o primeiro por `MINIMUM_POWER` é `shared`, critério do próprio sistema; um só adaptador de hardware → `unknown` na v1, D-W6b-15); `src/platform/win32/dxgi_adapter_match.{hpp,cpp}` (átomo: casa o adaptador com o contexto corrente por `GL_RENDERER` contra `Description`; sem casamento único → `unknown`, nunca "o primeiro"); `src/platform/win32/CMakeLists.txt` (linka `dxgi`); `tools/ci/check-dep-zero-win.ps1` (`DXGI.dll`, justificado); `tests/tools/check_dep_zero.py` (cabeçalhos `<drm/*.h>` do `kernel-headers` são API do SO, justificado por linha); `tests/parity/gl_context_parity_test.cpp` ganha `gpu_kind() == software` **asseverado** (os dois executores são software, F3/F4) e `MEASURED gl_context_parity_test.gpu_name_hash`; `tests/measured_exceptions.txt` (`gpu_name_hash`, `ambos`). | `drm_gpu_kind_test` (enumeração fechada: {amdgpu FUSION, amdgpu sem FUSION, i915 com DEVICE, i915 sem DEVICE, xe com, xe sem, nvidia, nouveau, driver desconhecido, sem ioctl} = 10 células, contagem impressa; vermelho por compilação); `dxgi_gpu_kind_test` (enumeração fechada: {0, 1, 2} adaptadores de hardware × {com, sem} flag SOFTWARE × ordem HIGH/MIN {igual, diferente}, contagem impressa); `dxgi_adapter_match_test` (nome igual casa; diferente → `unknown`; dois iguais → `unknown`). Vermelho de integração: `gl_context_parity_test` passa a exigir `software`; a versão da fatia 3/4 devolve `unknown` e reprova. **Mutação do revisor:** `FUSION → dedicated`; `drm_gpu_kind_test` reprova. | `drm_gpu_kind_test`, `dxgi_gpu_kind_test`, `dxgi_adapter_match_test` (ctest, puros, rodam nos cinco: não tocam API nenhuma, a struct de entrada é nossa); `gl_context_parity_test` (container) | os mesmos três (ctest); `gl_context_parity_test` (ctest) | mesmo nome por construção (os três puros existem nos dois inventários); nenhum apelido necessário |
| 6 | **L-A comum: o laço, o tick, o relógio, o passo fixo — e a PMU-LOOP** | comum | `include/glintfx/core/time.hpp` ganha `gltfx_now()` (D-W6b-10) e `src/core/time.cpp` a implementação; `include/glintfx/core/fixed_step.hpp` + `src/core/fixed_step.cpp` (`gltfx_fixed_step`: `dt`, `accumulate(elapsed) -> {steps, alpha}`, D-W6b-9); `include/glintfx/platform/loop/loop.hpp` (`gltfx_frame_tick` {`elapsed`, `now`, `frame_index`, `should_render`}, `gltfx_loop::{open(display&, window&, gl_context&), is_open, step, run(callbacks)}`; `gltfx_loop_callbacks` com dois `std::function`: `on_frame(const gltfx_frame_tick&) -> bool` (falso encerra) e `on_render(const gltfx_frame_tick&)`); `src/platform/loop/frame_tick_state.{hpp,cpp}` (átomo puro: dado `now` anterior, `now` atual, resultado do último `swap` → tick com `elapsed` limitado a 250 ms, nunca negativo, `frame_index` +1, `should_render`); `src/platform/loop/loop_impl.hpp` e `src/platform/loop/loop_facade.cpp` (`step` = pump → dormir se oculto (`wait_events`, fatia 7) → relógio → tick; `run` = `while (!window.close_requested()) { tick = step(); if (!on_frame(tick)) break; if (tick.should_render) { on_render(tick); swap; } }`); `tests/header_hygiene_test.cpp` (dois headers novos na cobertura). **A revisão de API dedicada (PMU-LOOP) acontece aqui**, em série depois da PMU-GL; entregável: as linhas de laço em `docs/gl-loop-portability-matrix.md`. | `time_test` ganha `now_is_monotonic` (duas leituras, diferença nunca negativa, 100 pares) — vermelho por compilação; `fixed_step_test` (6: elapsed < dt → 0 passos e alpha certo; elapsed == 2dt → 2 passos e alpha 0; resto acumula entre chamadas; **a espiral**: 10 chamadas com elapsed = 4dt limitado a 250 ms → o número de passos por chamada é limitado e o acumulador não cresce sem fim; dt zero recusado; alpha sempre em [0,1)); `frame_tick_state_test` (7: primeiro tick tem elapsed 0; relógio que anda 16 ms dá 16 ms; **relógio que "volta"** (now menor) dá 0, nunca negativo; 3 s viram 250 ms; `frame_index` cresce de 1 em 1; `skipped_hidden` anterior → `should_render` falso; `presented` → verdadeiro). Todos nascem vermelhos por compilação (headers não existem). | os três, mesmo nome, sem `#if` | idem | mesmo nome |
| 7 | **L-W / L-X: `wait_events(orçamento)` nos dois displays** (lote único, os dois lados juntos) | Wayland + Windows | `src/platform/port/display_backend_port.hpp` (a porta exige `wait_events(std::uint32_t budget_ms) noexcept -> gltfx_rslt<bool>`, verdadeiro se chegou evento); `src/platform/wayland/display_adapter.{hpp,cpp}` (`poll` com timeout positivo no fd, mesma sequência `prepare_read`/`flush`/`read`/`dispatch`, só o timeout muda; **átomo separado** de `pump_events`, sem duplicar: os dois chamam o mesmo `dispatch_ready_events(timeout)`); `src/platform/win32/display_adapter.{hpp,cpp}` (`MsgWaitForMultipleObjectsEx(0, nullptr, budget, QS_ALLINPUT, MWMO_INPUTAVAILABLE)` seguido do mesmo laço de `PeekMessageW`); `tests/fake/fake_display_adapter.hpp` (a fake ganha o método; o `static_assert` dos dois seletores prova os adaptadores reais); `tests/display_connection_fake_test.cpp` (caso do orçamento na fake). | `display_connection_fake_test` ganha `wait_events_with_budget` (vermelho por compilação: a fake não tem o método, e o `static_assert` da porta reprova os dois adaptadores reais até ganharem o método — **é o portão da porta mordendo**, o mesmo que a W6a usou); `wait_events_smoke` (container: sem evento na fila, `wait_events(50)` retorna falso e **leva pelo menos 40 ms e no máximo 500 ms**, lido de `gltfx_now()`; com evento provocado por um `roundtrip` de outra conexão no mesmo processo (a fixture `two_displays_test` já abre duas), retorna verdadeiro); `win32_wait_events_test` (idem com `PostMessageW` de uma mensagem à janela de mensagens do próprio display). | `wait_events_smoke` (container) | `win32_wait_events_test` | `wait_events_smoke\|win32_wait_events_test` apelido (mesmo fato: "o orçamento é lido, e evento acorda antes dele") |
| 8 | **P-LOOP: teste de paridade do laço, e o teste do oculto** | comum | `tests/parity/loop_parity_test.cpp`, **um arquivo, só API pública, sem `#if`**: abre display, janela, contexto, laço; 30 `step()` desenhando `clear` + `swap` quando `should_render`; assevera `frame_index` 1..30 sem buraco, `elapsed` nunca negativo e ≤ 250 ms, pelo menos 1 `should_render` verdadeiro (executores visíveis), `run()` com `on_frame` que devolve falso no 5º tick termina com exatamente 5 ticks; imprime `MEASURED loop_parity_test.rendered_frames`/`mean_step_ns`/`max_step_ns` (a cadência **medida, nunca assumida**, do requisito de 28/08). `tests/loop_hidden_test.cpp` (**interno** nos dois lados, como `win32_window_close_request_test`: abre pelos adaptadores, minimiza — Wayland `xdg_toplevel_set_minimized` via `toplevel()` da fatia 3, Win32 `ShowWindow(SW_MINIMIZE)` —, 10 `step()`, **imprime** `MEASURED loop_hidden_test.rendered_while_hidden` e o tempo total; asserção mínima "não travou e voltou em menos de 5 s" — o `kwin --virtual` pode continuar mandando frame callback a toplevel minimizado, hipótese não medida (D-W6b-6); a asserção `rendered_while_hidden == 0` sobe no commit seguinte à medição). Fixtures no container com o mesmo nome (`Containerfile`, `prepare_arch_ports_fixture.sh`, `ci.yml`, inventário). | Vermelho: a fachada do laço não existe até a fatia 6; `loop_hidden_test` vermelho por compilação (`toplevel()` não existe até a fatia 3). **Mutações do revisor:** (a) `elapsed` sem limitador → `frame_tick_state_test` reprova; (b) `step()` sem `wait_events` no oculto → `loop_hidden_test` continua verde **por valor** mas o tempo de CPU medido (`MEASURED loop_hidden_test.cpu_ns`, `std::clock`) dispara: **é aí que a asserção de CPU entra, na segunda rodada, com o número da primeira na mão**; (c) `run()` que ignora o retorno de `on_frame` → `loop_parity_test` reprova (5 ticks viram infinitos com orçamento). | `loop_parity_test`, `loop_hidden_test` (container) | os dois (ctest) | mesmo nome, via P-0 |
| 9 | **Fechamento** | comum | `docs/gl-loop-portability-matrix.md` (a matriz do consumidor, com as duas PMUs consolidadas; **sem dígito volátil**, mesma regra da matriz da janela); `docs/wayland-display.md` ganha as seções "contexto EGL" e "espera com orçamento" (documento interno, L-21: leitor é quem estende a camada); `README.md` (seção de status, em inglês: contexto e laço existem; **`README-STATUS-ROT` da INBOX morde aqui**, o texto "not implemented: a visible window" já está falso desde a W6a); `tests/tools/check_port_privacy.sh` (lista fechada final); `TODO.md` pelo orquestrador (`GL-CONTEXT`+`WIN-GL` e `LOOP-RUN` para 🔍 no mesmo commit; ✅ só com `parity` verde no push de fechamento; as quatro linhas de entrada re-ondadas, §2.1); `tests/parity_absences.txt` (só se algum caminho desta tabela não nascer — cada linha com item dono); `check_plan_scope_diff.py` rodado contra **cada linha** desta tabela (marcador: o nome em negrito da fatia), contagem impressa. | (o próprio portão de fechamento; `parity` verde com as três fontes Linux; `check_measured_parity.py` com "divergentes não declaradas" e "obrigatória" em **zero**) | idem | (o próprio portão) |

**Ordem de trabalho, em lotes de servidor:** push 1 = fatias 0 + 1 (primeiro dia; a resposta da sonda G-1 chega enquanto a fatia 2 é escrita). Push 2 = fatias 2 + 3 + 4 (Lote Windows 1 junto com o Linux). Push 3 = fatia 5 (paridade GL, morte do `window_smoke`). Push 4 = fatias 6 + 7 (Lote Windows 2 é só `wait_events`). Push 5 = fatia 8. Push 6 = fechamento. **Cinco voltas no mínimo; cada reprovação acrescenta uma; na segunda reprovação pelo mesmo motivo, L-42 (busca antes da terceira).**

---

## 5. A sonda G-1: árvore de decisão fixada ANTES do dado (L-43 global)

| O que `egl_probe_smoke` imprimir no container | O que decido |
|---|---|
| (i) EGL inicializa sobre o `wl_display` do `kwin --virtual`, `EGL_CLIENT_APIS` contém `OpenGL`, contexto **3.3 core** criado e corrente (esperado: llvmpipe anuncia 4.5 core e aceita 3.3) | Nada muda. Fatias 3 e 5 como escritas. |
| (ii) EGL inicializa mas só oferece `OpenGL_ES`, ou o contexto core 3.3 falha (`EGL_BAD_MATCH`/`EGL_BAD_ATTRIBUTE`) | A imagem do container ganha o que faltar (`mesa-libGL`? `libglvnd-opengl`?) — **pacote na imagem de teste é aparato, não dependência** (mesma categoria de `mesa-dri-drivers`), e `check_isolation.sh` não muda. Sem `/dev/dri`, continua software. Se ainda assim não criar, o caso vai à L-42 (busca) antes da terceira tentativa. |
| (iii) `eglGetPlatformDisplay` devolve `EGL_NO_DISPLAY` no compositor virtual | O lado Linux do contexto **não tem prova no container**, e pela L-04 a fatia gráfica não fecha. Isso sai do meu poder: muda o que a biblioteca promete no Linux em CI, e vai ao líder com o log na mão. Não é hipótese esperada: o Mesa fala com qualquer `wl_display` que ofereça `wl_shm`/`zwp_linux_dmabuf_v1`, e o `registry_smoke` já listou 61 globals (F13 imprime quais). |

**O que a sonda não responde, declarado:** desempenho, correção de shader e comportamento com GPU real. Isso só a máquina de um usuário mede, e fica escrito no header quando ele nascer.

---

## 6. O que a L-09 impede esta onda de provar

Tudo executa em container (Linux) ou no executor (Windows). Nenhum quadro é visto por ninguém: o `kwin --virtual` desenha num framebuffer que ninguém olha, e o executor Windows desenha por D3D12 de software. **A única prova de pixel é a leitura de volta (`glReadPixels`) do próprio back buffer**, que é portável e não precisa de screenshot; screenshot e checklist do `qa-engineer` (L-13 global) continuam **não se aplicando** até `DEMO-1` (W8), que é a primeira observação visual, no container, com o `qa-engineer`.

**Fica sem prova, e o header diz "medido em CI, não em máquina de usuário":** vsync real contra um monitor (os dois executores não têm monitor; a cadência medida é a do compositor virtual e a do DWM de VM); `wglSwapIntervalEXT` contra driver de GPU real (o Mesa D3D12 aceita, o driver de um usuário pode dar "adaptativo", fato da busca); frame callback do KWin real do líder com escala fracionária (`WL-SCALE` é quem mede, depois); troca de monitor com o contexto aberto.

---

## 7. Onde esta onda tem a forma "passa verde e está quebrado", e o que torna cada uma vermelha

| # | Forma do risco | Como passa verde | O que o torna vermelho |
|---|---|---|---|
| 1 | **`proc_address` só por `wglGetProcAddress`**: as ~40 funções 1.1 voltam nulas | O carregador interno não é chamado por teste nenhum no Windows; a lib linka | `win32_wgl_proc_address_test` assevera `glGetString` resolvido; `gl_context_parity_test` chama `glClear`/`glReadPixels` (1.1) pelo `proc_address` público |
| 2 | **`SetPixelFormat` duas vezes** (legado e depois ARB na mesma janela) | Passa em alguns drivers, falha noutros, e o executor com Mesa pode aceitar | `set_pixel_format_once` é átomo com contador; `win32_wgl_extension_loader_test` assevera que a janela descartável morreu e que a real recebeu **um** `SetPixelFormat` |
| 3 | **`eglSwapInterval(1)` do Mesa em vez do frame callback próprio** | Visível, nada difere; oculto, o processo trava, e nenhum executor esconde janela sozinho | `loop_hidden_test` (minimiza de verdade) com orçamento de 5 s: travou, reprova |
| 4 | **`pixel_size` copiado de `logical_size` no `wl_egl_window`** | Escala 1 nos dois executores | `gl_surface_size_policy_test` com escala 2 sintética; mutação obrigatória (§4, fatia 5) |
| 5 | **`elapsed` sem limitador** (espiral) | Nenhum executor demora 250 ms por quadro | `frame_tick_state_test` (3 s viram 250 ms) e `fixed_step_test` (a espiral) |
| 6 | **`ack_configure` removido** com o `window_smoke` já aposentado | O compositor só reclama quando há buffer anexado, e o EGL anexa | Mutação de estreia **antes** de aposentar (`window_smoke` só sai depois de `gl_context_parity_test` reprovar com `xdg_surface`) |
| 7 | **Símbolo público sem definição no modo estático do Windows** (W6a nº 1, de novo) | A `.lib` não reclama | `gl_context_parity_test` e `loop_parity_test` são `ctest` nos **dois modos** do Windows e linkam as fachadas; `LIB-NO-UNDEF-WIN` continua sendo a lacuna estrutural (item aberto, não desta onda) |
| 8 | **Lista fechada de dependência que cresce sem justificativa** (`libEGL`, `opengl32`) | O portão passa assim que a linha entra | Cada linha nova nas quatro listas (`check_dep_zero.py` ×2, `check-dep-zero-win.ps1`, `.pc`) leva a razão e a decisão (D-W6b-3) no comentário; o revisor lê as quatro |
| 9 | **Medição sem leitor** (W6a: 56 pontos, zero lidos) | `MEASURED` impresso e ninguém compara | Toda chave nova desta onda (`gl_major`, `gl_minor`, `renderer_hash`, `rendered_frames`, `mean_step_ns`, `rendered_while_hidden`, `cpu_ns`) entra em `check_measured_parity.py` pelo mecanismo existente; a divergente por ambiente (`renderer_hash`) nasce **com** a linha de exceção citando o teste que prova a biblioteca no lugar, ou reprova |

---

## 8. O que esta onda NÃO faz, declarado

1. **Entrada** (`WL-KEYBOARD`, `WL-POINTER`, `WIN-INPUT`) e **escala fracionária** (`WL-SCALE`): fora, pela L-32 (§2.1). Nenhuma linha de código de teclado, ponteiro, cursor ou `fractional-scale` nasce aqui. A costura para eles fica pronta (D-W6b-5, `toplevel()`), e a busca de referência já está feita (§0).
2. **`WL-DISPLAY-BACKEND-SEAT`**: continua ausência declarada em `tests/parity_absences.txt`; nasce com a entrada, não aqui. ⚠️ Conferir com o orquestrador que o item existe **como linha da tabela** e não só na INBOX: a regra de morte do arquivo de ausências consulta o status do item, e um item que não está na tabela não tem status.
3. **Render 2D** (`R2D-BATCH`, W7): nenhum shader, nenhum quad. A única chamada de desenho desta onda é `glClear` num teste.
4. **Demo** (`DEMO-1`, W8): nenhum exemplo. A "primeira imagem na tela" continua sendo a demo.
5. **Contexto compartilhado, várias threads, `debug output`, MSAA, sRGB, `swap` com danos** (`EGL_KHR_swap_buffers_with_damage`): adição compatível, depois, se e quando.
6. **Vsync real com monitor**: não há monitor em nenhum executor (§6).
7. **Portão genérico de diff de escopo para todo plano** (`PLAN-SCOPE-COLUMNS`): o `check_plan_scope_diff.py` é usado como está, linha a linha, com marcador; generalizar continua sendo proposta pendente, não trabalho desta onda.
8. **Cache de `proc_address` público em tabela para o consumidor**: o consumidor que quer GL cru resolve nome a nome (é o que SDL/GLFW fazem); a tabela de 344 é interna do render.

---

## 9. O que ficou sem medição, e o que eu decidi assim mesmo

| Sem medição | Decidi | Medir quando |
|---|---|---|
| EGL sobre o `kwin --virtual` do container, e GL 3.3 core no llvmpipe de lá | árvore da §5 | G-1, primeiro push |
| Versões de `wl_compositor`/`wl_seat`/`xdg_wm_base` anunciadas no container | (não decide nada nesta onda; a fatia 3 usa `wl_surface.frame`, do núcleo) | `registry_smoke` passa a imprimir (fatia 1) |
| `kwin --virtual` para de mandar frame callback a toplevel minimizado | `loop_hidden_test` imprime, assevera no commit seguinte | primeiro run da fatia 8 |
| `MsgWaitForMultipleObjectsEx` no executor com orçamento de 50 ms devolve dentro da janela [40, 500] ms | asserção com margem larga; se reprovar, o número real entra e a margem se ajusta **uma** vez, com o log | `win32_wait_events_test`, fatia 7 |
| `GL_MAJOR/MINOR_VERSION` que o Mesa D3D12 devolve para pedido 3.3 core (4.6? 3.3?) | política aceita ≥3.3 core; valor **medido**, não asseverado igual entre sistemas | `gl_context_parity_test`, fatia 5 |
| `glReadPixels` do back buffer no D3D12 de software devolve os bytes exatos (tolerância 1) | asserção com tolerância 1; se reprovar por conversão de formato, a tolerância sobe **com o valor medido na mão**, nunca antes | fatia 5 |
| `clang-tidy` do Windows sobre `<GL/gl.h>` e `wglext` (macros hostis, `APIENTRY`) | os `.cpp` de WGL incluem só `<windows.h>` com `WIN32_LEAN_AND_MEAN`/`NOMINMAX` e `<GL/gl.h>`; os tipos WGL ARB são **declarados à mão** no `wgl_extension_loader.hpp` (são quatro `typedef` de ponteiro de função e uma dúzia de constantes, com o número da extensão na fonte, `WGL_ARB_create_context`/`WGL_ARB_pixel_format`/`WGL_EXT_swap_control`, nunca copiado do `wglext.h`, L-29) | primeiro push do Lote Windows 1 |
| Se o compilador cruzado local (`WIN-CROSS-STAGE`) compila `opengl32` imports | o espelho local tenta; se não linkar, **declara** e o servidor prova | antes do push 2 |

**Ordem de serviço, separando fato de inferência (L-27 do projeto):** tudo em §0 é fato com caminho ou fonte; §1 a §4 são decisão minha; a coluna "esperado" da §5 e as hipóteses da §9 são inferência marcada. O orquestrador passa a cada agente **o caminho deste arquivo e o de `docs/plano-w6a-janela.md`**, nunca só o resumo.

**Sobre versionar este plano:** recomendo **sim**, como `docs/plano-w6b-placa-e-laco.md`, pela mesma razão da onda passada: a onda atravessa sessões, o Windows é escrito às cegas contra o que está escrito aqui, e o `check_plan_scope_diff.py` precisa do arquivo na árvore para cobrar a coluna. A decisão é do orquestrador.

---

## 10. Adendo de 06/09/2026 12:00: "o framework deve perceber placa gráfica compartilhada e dedicada"

**Ordem do líder, verbatim, chegada pelo orquestrador em 06/09/2026:** *"o framework deve perceber placa grafica compartilhada e dedicada"*. Chega **antes** de a PMU-GL (fatia 2) congelar, então entra na fatia 2 sem quebra. Este adendo altera a tabela da §4 (fatia 2 e a fatia 5b nova) e acrescenta três decisões.

### 10.0 Busca (L-43), com fonte, antes de decidir

- **Escolher a placa no Windows, com OpenGL, é do EXECUTÁVEL, não da biblioteca.** Os drivers NVIDIA e AMD leem duas variáveis **exportadas pelo `.exe`** (`NvOptimusEnablement`, `AmdPowerXpressRequestHighPerformance`); a AMD varre a string no binário **antes de executar**, e "pôr isso na DLL não faz nada" ([gist statico](https://gist.github.com/statico/6809850727c708f08458), [SFML #1192](https://github.com/SFML/SFML/issues/1192), [ebiten #1252](https://github.com/hajimehoshi/ebiten/issues/1252)). Intel Arc não tem equivalente documentado ([Intel Community](https://community.intel.com/t5/Graphics/Intel-Arc-equivalent-for-NvOptimusEnablement-and/m-p/1565928)). E **desde o Windows 10 20H1 o próprio sistema decide por aplicativo** (Configurações → Gráficos: "economia de energia" / "alto desempenho") e **sobrepõe** o painel da NVIDIA ([NVIDIA custhelp 5035](https://nvidia.custhelp.com/app/answers/detail/a_id/5035/), [tenforums](https://www.tenforums.com/tutorials/103965-set-preferred-gpu-apps-windows-10-a.html)). Conclusão: "escolher" no Windows é **pedido** que a lib não consegue cumprir sozinha nem garantir.
- **Escolher no Linux/Wayland é variável de ambiente lida na inicialização do EGL, e diferente por driver**: `DRI_PRIME=1` (Mesa) ou `__NV_PRIME_RENDER_OFFLOAD=1` (NVIDIA, com regressões documentadas em drivers recentes) ([egl-wayland #98](https://github.com/NVIDIA/egl-wayland/issues/98), [ArchWiki PRIME](https://wiki.archlinux.org/title/PRIME), [NVIDIA forums 257783](https://forums.developer.nvidia.com/t/535-54-03-regression-gl-wayland-applications-can-not-use-prime-render-offload/257783)). O dispositivo padrão de um cliente Wayland é o `main_device` do `linux-dmabuf` feedback do compositor ([egl-wayland #178](https://github.com/NVIDIA/egl-wayland/pull/178)). Também aqui "escolher" é best-effort e por driver.
- **Perceber qual placa está em uso é possível nos dois sistemas com API do SO:** no Linux, `EGL_EXT_device_query` dá o `EGLDeviceEXT` do display, `EGL_MESA_device_software` marca render por software, `EGL_EXT_device_drm` dá o `/dev/dri/cardN` ([Khronos EGL_EXT_device_query](https://registry.khronos.org/EGL/extensions/EXT/EGL_EXT_device_query.txt)); daí o **kernel** responde se a placa é integrada: `amdgpu` expõe `AMDGPU_IDS_FLAGS_FUSION` (APU) em `AMDGPU_INFO_DEV_INFO`, `i915`/`xe` expõem as regiões de memória (classe `DEVICE` só existe em placa com VRAM própria), os cabeçalhos UAPI (`/usr/include/drm/*_drm.h`) vêm do pacote `kernel-headers` nesta máquina (medido: `rpm -qf`), portanto **API do SO** pela L-07. No Windows, **o DXGI não tem flag "integrada"** ([Microsoft Q&A 2134992](https://learn.microsoft.com/en-us/answers/questions/2134992/how-do-i-filter-gpus-by-type-in-dxgi-(integrated-v), [wgpu #683](https://github.com/gfx-rs/wgpu/issues/683)); o que ele tem é a flag `SOFTWARE` e a **ordem por preferência** (`IDXGIFactory6::EnumAdapterByGpuPreference`, `HIGH_PERFORMANCE`/`MINIMUM_POWER`, DXGI 1.6, Windows 10 1803+) ([DXGI_GPU_PREFERENCE](https://learn.microsoft.com/en-us/windows/desktop/api/dxgi1_6/ne-dxgi1_6-dxgi_gpu_preference)), que é exatamente o critério que a tela de Configurações usa. Heurística por `DedicatedVideoMemory` é conhecida por errar (Intel reportado como discreta, wgpu #683). A resposta definitiva (`D3D12_FEATURE_ARCHITECTURE.UMA`) exige criar um dispositivo D3D12 só para perguntar; **fica fora da v1**, declarado.
- **Medido nesta máquina, por leitura de sysfs, sem executar nada:** `card0` vendor `0x10de` device `0x25a2` driver `nvidia`; `card1` vendor `0x8086` device `0x46a6` driver `i915`. **A máquina do líder é exatamente o caso híbrido** (dedicada NVIDIA + compartilhada Intel). Os dois executores de CI são **software** (llvmpipe; "Microsoft Basic Render Driver", que o DXGI marca `SOFTWARE`).

### 10.1 As decisões

| # | Decisão | Opções na mesa | Escolhida e porquê | Mão única |
|---|---|---|---|---|
| D-W6b-13 | **O que "perceber" promete na v1** | (a) **informar** qual placa está em uso; (b) **escolher** entre elas; (c) as duas | **(a), com o valor `unknown` como resposta honesta e legítima.** Pelo consumidor (autor de jogo 2D): o que ele faz com a informação é registrar no log de suporte, escolher um preset de qualidade (partículas, resolução interna) e avisar "rodando na placa integrada/por software". Isso (a) entrega. (b) é promessa que a lib **não consegue cumprir sozinha** em nenhum dos dois sistemas (busca acima: exe no Windows, variável de ambiente por driver no Linux, e o sistema operacional sobrepõe os dois). Prometer (b) e entregar "às vezes" é a forma "aceito em silêncio e não acontece". **Contrato:** `gltfx_gl_context::gpu_kind() -> gltfx_gpu_kind` {`unknown`, `software`, `shared`, `dedicated`} e `gpu_name() -> std::string_view` (o `GL_RENDERER`, sempre disponível, para o log). Regra escrita no header: **`unknown` significa "o sistema não disse", nunca "provavelmente integrada"** — a lib não chuta. | **Sim** (enum e dois métodos) |
| D-W6b-14 | **Reservar a porta de "escolher"** | (a) nada no descritor, acrescentar depois; (b) campo `gpu_preference` no descritor **agora**, com só `no_preference` aceito; (c) campo com comportamento best-effort já | **(b).** `gltfx_gl_context_desc` é value type de layout visível (L-19): acrescentar campo **depois** é quebra de ABI, então o campo entra agora e fixa o layout. `prefer_dedicated`/`prefer_shared` são **recusados** com `invalid_argument` e `rejected_value == "gpu_preference"` até existir fatia que os honre — assim nenhum consumidor vive o "pedi e nada aconteceu". (c) é exatamente esse defeito. Quando a fatia de escolha nascer (proposta ao orquestrador: `GL-GPU-PREFERENCE`, fora desta onda), ela terá **duas metades já conhecidas pela busca**: no Windows, uma macro pública `GLINTFX_DECLARE_GPU_PREFERENCE(...)` que o consumidor põe numa TU do executável (única forma que funciona), e no Linux, `setenv` de `DRI_PRIME`/`__NV_PRIME_RENDER_OFFLOAD` **antes** de `eglGetPlatformDisplay` — as duas best-effort, as duas sujeitas à configuração do sistema, e o header vai dizer isso. | **Sim** (layout do descritor) |
| D-W6b-15 | **Como se classifica, e onde a v1 diz `unknown`** | (a) heurística por memória de vídeo; (b) só o que o sistema afirma; (c) tabela de identificadores de placa | **(b).** (a) erra em caso conhecido (busca). (c) é banco de dados de hardware, que o líder já recusou para gamepad pela mesma razão (L-36.3) e que apodrece a cada geração. Regras fechadas: **Linux:** `EGL_MESA_device_software` → `software`; `amdgpu` com `FUSION` → `shared`, sem → `dedicated`; `i915`/`xe` com região `DEVICE` → `dedicated`, sem → `shared`; `nvidia`/`nouveau` → `dedicated`; driver não listado (`virtio`, `vc4`, `panfrost`, ...) → `unknown`. **Windows:** adaptador com flag `SOFTWARE` → `software`; **dois ou mais** adaptadores de hardware → o primeiro por `HIGH_PERFORMANCE` é `dedicated` e o primeiro por `MINIMUM_POWER` é `shared` (critério do sistema, o mesmo da tela de Configurações); **um só** adaptador de hardware → `unknown` na v1 (o DXGI não distingue; a pergunta ao D3D12 fica para depois, declarado); adaptador que não casa com o `GL_RENDERER` do contexto corrente → `unknown`, nunca "o primeiro da lista". **Os dois lados chegam juntos** (fatia 5b): perceber num sistema só é paridade parcial (L-04). | Não (interno; o que congela é D-W6b-13) |

### 10.2 Como se prova, e o que só a máquina de um usuário mede

- **Provável em CI, asseverado:** `software` nos dois executores, pela API pública, no `gl_context_parity_test` (a mesma chamada, os dois lados, mesmo nome via P-0). É a única classe que os executores produzem.
- **Provável em CI, por valor sintético nos cinco sistemas:** os três classificadores são átomos puros alimentados por struct (o que o ioctl/DXGI devolveu), com enumeração **fechada** de células e contagem impressa (`drm_gpu_kind_test`, `dxgi_gpu_kind_test`, `dxgi_adapter_match_test`). A leitura real do ioctl/DXGI é átomo separado, exercitado só na integração.
- **Só a máquina de um usuário mede:** `shared`/`dedicated` de verdade. A máquina do líder é o caso híbrido (10.0); a primeira leitura real acontece na `DEMO-1` (W8), que imprime `gpu_kind`/`gpu_name` na saída e o `qa-engineer` registra — **no container ela vai dizer `software`, e isso é o esperado**, não defeito. Fica escrito no header: "classificação medida em CI só para `software`; `shared`/`dedicated` provados por valor sintético contra o que o kernel/DXGI documentam, nunca contra hardware real".
- **`MEASURED gl_context_parity_test.gpu_name_hash`** nos dois lados, divergente por ambiente, com linha em `tests/measured_exceptions.txt` (`ambos`) citando a asserção de `software` como a prova da biblioteca.

### 10.3 Cabe na fatia 2?

**O contrato cabe; a classificação não.** A fatia 2 ganha o header `gpu_kind.hpp`, os dois métodos, o campo do descritor com a recusa e o átomo `gpu_kind_state` (D-W6b-13/14). As fatias 3 e 4 preenchem o **mínimo honesto** (`software` quando o sistema diz, senão `unknown`). A classificação de hardware é a **fatia 5b**, própria, depois de o contexto existir nos dois lados e antes do laço, porque precisa de um contexto corrente para saber qual placa o driver escolheu. Dois `.cpp` a mais em `win32/` — mais uma razão para a fatia 0 (portão de lint) vir antes.

### 10.4 O que este adendo NÃO faz

Não escolhe placa (D-W6b-14 reserva a porta e recusa o pedido até existir fatia); não cria dispositivo D3D12 para perguntar `UMA`; não lê tabela de identificadores; não promete `shared`/`dedicated` em driver que o kernel não descreve (`unknown` é a resposta); não mede hardware real em CI (não há).
