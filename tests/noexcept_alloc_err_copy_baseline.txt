# SPDX-License-Identifier: AGPL-3.0-or-later
#
# NOEXCEPT-ALLOC-B8 familia C baseline (TODO.md ERR-COPY-GATE,
# ESCOPO.md Decisao 16/17, 18/09/2026). Consumido por
# tests/tools/check_noexcept_alloc.py.
#
# Familia C: copia de `gltfx_err` dentro de `noexcept` sem `try`
# eficaz, via `gltfx_rslt<T>::err(...)` (fabrica que recebe o
# parametro POR VALOR - err.hpp:299/:391). Mata nos CINCO sistemas,
# Release incluido - NAO depende de modo de depuracao nenhum (ao
# contrario da familia A). Ver o paragrafo dedicado no cabecalho de
# check_noexcept_alloc.py para a forma exata do defeito.
#
# ESVAZIADO NESTA FATIA (ERR-COPY-GATE fecha o ciclo aberto em
# 18/09/2026, apos ERR-COPY-FIX - commit f0dbea6 - tornar a copia de
# `gltfx_err` `noexcept` de verdade, ESCOPO.md Decisao 17). Este
# arquivo chegou a guardar 129 sitios reais como catraca temporaria
# para o CI nao ficar vermelho no intervalo entre a fatia que ensinou
# o portao a ver o defeito (ERR-COPY-GATE, F3) e a fatia que consertou
# a causa raiz (ERR-COPY-FIX, F4) - GODS_LAWS.md L-67: o que perde
# validade e' apagado, nunca arquivado com o texto morto dentro.
#
# A partir de agora, check_noexcept_alloc.py MEDE (nao assume) se a
# copia de `gltfx_err` ainda e' noexcept, lendo
# include/glintfx/core/err.hpp em toda execucao real
# (parse_err_copy_ctor_noexcept - ver o cabecalho do script). Com o
# tipo noexcept, nenhum sitio de familia C e' achado - por isso este
# arquivo fica VAZIO, e nao mais "vazio pela ausencia dos 129 antigos
# terem sido convertidos em excecao": qualquer sitio novo de familia C
# que aparecer aqui em diante e' regressao de verdade (a copia deixou
# de ser noexcept) e reprova NA HORA, sem tolerancia - a garantia
# permanente e' o `static_assert(std::is_nothrow_copy_constructible_v
# <gltfx_err>)` de TIPO em err.hpp, guardiao irmao deste portao de
# texto.
#
# Formato, um registro por linha, tres campos separados por "|"
# (comentarios com "#", linhas em branco ignoradas):
#   caminho|linha|forma
#
# Vazio por desenho - nao acrescente sitio aqui sem antes confirmar
# que a regressao e' legitima (o `static_assert` de err.hpp quebraria
# o build primeiro, nesse caso).
